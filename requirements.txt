# deploy-reboot marker: bump the comment below to force Streamlit Cloud to
# rebuild the environment and restart the python process (a plain code push
# reloads app.py but can keep an old backend module cached in memory)
# reboot: 2026-07-20-b
streamlit==1.58.0
anthropic==0.112.0
chromadb==1.5.9
python-dotenv==1.2.2
onnxruntime==1.27.0
huggingface_hub==1.21.0
tokenizers==0.23.1
numpy==2.5.0
pymupdf==1.27.2.3
gspread==6.1.4
