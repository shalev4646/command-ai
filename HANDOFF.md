# העברה לסשן חדש — CommandAI, מצב ל-28.08.2026 (עודכן: פיצול המדד)

## קרא קודם
1. המסמך הזה
2. `night/PLAN_ROUND3.md` — התוכנית הפעילה **וההפרכה שלה בסופה**
3. `night/PILOT150_FINDINGS.md` — כל הממצאים של סבב 1

---

## המספרים, ומה מותר להסיק מהם

| סט | n | ענה | רווח-סמך |
|---|---|---|---|
| פיילוט-150 (בסיס, לפני התיקונים) | 148 | 28% | — |
| שמורות, **מושבים=0** | 100 | 40% | — |
| שמורות, **מושבים=2** | 96 | **48%** | 38-58% |
| 91 בתולות, הכול דלוק | 91 | 38% | — |
| **72 בסגנון אמיתי, טריות** | 72 | **36%** | 26-48% |
| 16 מלוג הפרודקשן | 16 | **88%** | 64-97% |

⛔⛔ **שני כללי-קריאה שאסור להפר:**

1. **שונות-המדגם היא 12 נקודות.** פיילוט-150 והשמורות רצו **באותה תצורה
   בדיוק** וקיבלו 28% מול 40%. **כל השוואה בין סטים שונים חסרת-ערך.**
2. **ה-88% מוטה.** אותן שאלות גם כוונו וגם נמדדו — 64 עוגנים יושבים בדיוק
   בנושאים שלהן. **אל תבנה עליו אסטרטגיה.**

⇒ **הראיה הסיבתית היחידה מכל היום: מזווגת, אותן 96 שאלות, 40%→48%** —
ובתוכה רק „פחות כלום" מגיע ל-p=0.064, שאינו עובר את הסף.

---

## ⛔ שמונה כיוונים נמדדו ונדחו — אל תבנה אותם שוב

| מה | למה נפל |
|---|---|
| חסימת הגברה לפי מרווח-דירוג | חציון זהה: 0.027 נכון / 0.027 שגוי |
| `_ROUTE_BOOST` 0.05→0.15 | שובר שש בשער, מרפא שלוש |
| נתב עם k=5 | דיוק 11→13, **מוגש 16→16** |
| כותרות-נתב מועשרות | +1 מוגש, פי-2.3 בעלות |
| Sonnet כנתב | דיוק 14 (56%) — **והמוגש ירד ל-15** |
| לשמר את דירוג-הנתב | הבחירה הראשונה נכונה ב-5/25 בלבד |
| קיצור כלל-הסירוב בפרומפט | סירובים **6/13 → 8/13**, גרוע יותר |
| „לכוונן 10 נושאים" | ראה למטה — נבדק ונפרך |

---

## ⚡ ההפרכה האחרונה, והיא החשובה

כווננתי נושא אחד (הופעה ומדים; הק"א 33-05-01 קיבל +4 סעיפים) ועצרתי למדוד.
**14 שאלות טריות: 3/14.** ושלוש שנענו הן **בדיוק** אלה שכיוונתי אליהן.
אחת-עשרה שנכשלו שואלות על מה שאין בפקודה: **קעקועים 0 מופעים, שעון 0,
אורך מכנס 0, בדיקת מדים 0.**

⇒ **„נושא" אינו יחידה שהקורפוס בנוי סביבה.** העמקת פקודה מכסה את מה שהיא
מסדירה, לא נושא. **הכוונון עובד בדיוק על מה שכיוונת ועל כלום מעבר.**

⇒ **התקרה אינה באפליקציה אלא בפקודות.** 45 מ-148 שאלות אין להן תשובה,
נבדק אחת-אחת עם ציטוטים מאומתים.

---

## ✅ מה נכנס והוא חי

| | |
|---|---|
| `RETRIEVE_ROUTER_SLOTS=2` | **נפרס.** הפקודה שהנתב נוקב מגיעה 11/11 במקום 5/11 |
| 5 דלתות `out_of_scope` | **אפס שאלות נגמרות באוויר** (107/107) |
| אצירה חסרת-ספרות | בלתי-נגישות 11 → 7 |
| **תיקון סימני-סעיף** | **+612 סימנים ב-84 מסמכים**, חלקם מ**אפס** |
| `RETRIEVE_STRICT` | מדידה נופלת על צינור שבור במקום להמציא מספר |

**שער 393/431 לאורך כל היום, אפס רגרסיות. 42 קבצי-בדיקה עוברים.**

---

## ⚠ מלכודות תפעוליות שנצרבו היום

- **יתרת ה-API אזלה פעמיים.** בפעם השנייה זרוע הגיעה ל„composed 100/100"
  בזמן ש-HyDE והנתב נכשלו מאה פעם. **`RETRIEVE_STRICT` נבנה בדיוק לזה.**
- **הרצתי זרוע פעמיים** (שרשרתי וגם ידנית) — **$4.27 בוזבזו.**
  ⇒ לפני הרצה בתשלום: לבדוק אם היא כבר משורשרת.
- **תמחור:** הערכתי $1.12 והעלות הייתה $2.16 — חישבתי במחיר-באץ' והרצתי
  סינכרונית. **פי-2.**
- **⛔ מכשיר-המדידה מוצה: נשארו 2 שאלות עיוורות שלא נמדדו** (מ-449).
  כל מדידה עתידית דורשת גל חדש (`night.genq`, ~$0.40).

---

## מצב טכני

- פרודקשן: `commandai.fly.dev`, בריא, `RETRIEVE_ROUTER_SLOTS=2` מאומת ב-SSH
- קורפוס: 294 מסמכים, **287 נגישים** (7 חסומים על ספרות משובשות)
- ליג'ר: **$118.24 / $135** (התקרה הועלתה 120→135 באישור המשתמש)
- ענף `orphan/boot-preload` — עבודה יתומה של סשן אחר, **לא מוזגה ולא נפרסה**

---

## ⚡⚡ 28.08 — המדד פוצל, ושתי טעויות-מכשיר תוקנו

**הכרעת המשתמש: לפצל את המדד.** ⇒ `night/report_goal.py` מדווח שלוש דרגות:

```
פיילוט-150:  strict 62/289 (21%)  ->  served 100/289 (35%)  ->  goal 146/289 (51%)
             דלתות: 20 משפחה מאומתת · 25 ברירת-מחדל · 0 כלום
```

| דרגה | מה נספר |
|---|---|
| `strict` | הפקודות ענו. הסרגל ההיסטורי, לא נגעתי בו |
| `served` | + הכחשה כנה שהגיעה ל**דלת-משפחה מאומתת** — צעד אמיתי |
| `goal` | + הכחשה כנה שהגיעה ל**דלת ברירת-המחדל** — נוהל, לא תשובה |

**שתי טעויות-מכשיר שתוקנו, ושתיהן היו לרעת האפליקציה:**

1. **שני קבצי-בוררות כותבים `MISSING` והשלישי `NOT_IN_CORPUS`** לאותו דבר.
   המכשיר קרא רק את הראשון ⇒ חייב את האפליקציה ב-12 שאלות שהבוררות זיכתה
   אותה מהן. מזוכות **33 → 45**, בדיוק כפי שהמסמכים תמיד אמרו.
2. **המכשיר חיפש את הרצועה בטקסט התשובה.** הרצועה נרנדרת ב-`app.py:8615`
   **אחרי** שהמודל סיים, ו-`night/probe.py` מודד את `stream_ai_answer`
   שלא רואה אותה. דיווח 6 מ-45 — השער האמיתי נדלק על **107/107**.
   ⇒ המכשיר משחזר עכשיו את שני השערים של האפליקציה. עולה אפס.

### ⛔ והמספר שנחשף — הוא זה ש-`out_of_scope.py` ביקש שיוחזק

> „**המדד להחזיק בו: כמה שאלות מגיעות עד לכאן. מספר שגדל פירושו שהמשפחות
> הספציפיות מפגרות אחרי המציאות.**"

**על סט טרי: 40 מ-42 = 95% מגיעות לדלת ברירת-המחדל.** איש לא דיווח עליו.

⚠ **והסיכון שהמודול עצמו רשם:** כשליש מהאפסים שבהם הרצועה נדלקת הם שאלות
שהקורפוס **כן** יכול לענות עליהן והאחזור החמיץ. הדלת הגנרית משתיקה גם אותן
⇒ **הרצועה חדלה להיות סימן שמשהו שבור.**

### מפת-הכשלים האמיתית, 148 שאלות

| | כמה | מה זה |
|---|---|---|
| נענו (מלאות/חלקיות) | 41 | |
| **כשל אמיתי של האפליקציה** | **62** | הקורפוס עונה, לא הבאנו (48 אחזור + 14 עומק-בלוק) |
| הכחשה נכונה | 45 | 33 „אין כלל" + 12 „מסמך שאינו שלנו" |

⇒ **האפליקציה נכשלת על 42% — וכל 62 ניתנות לתיקון בלי לקנות שום מסמך.**

---

## ⚡ 28.08 המשך — עומק-בלוק נמדד, והמגבלה שלו נמצאה

**מכשיר חדש חינמי: `night/why_default.py`.** התשובה נוקבת במה שחסר לה
(„טרם במאגר: X"), והמודול מנקד את X מול 2,389 הסעיפים המאוצרים בארבע-גרמים
של תווים. כויל מול 107 האפסים המבוררים: **AUC 0.758, סף 0.130 ⇒ דיוק 94.7%**.
⛔ חד-כיווני: ציון נמוך אינו „אין כלל" (53% מול בסיס 31%) — המודול מחזיר
`COVERED`/`UNCLEAR` **ולעולם לא „אין כלל"**.

⚡⚡ **`top_score` — הביטחון של האחזור בעצמו — נתן AUC 0.500 בדיוק.** אפס
מידע. גם `context_words` (0.473) ומספר-מסמכים (0.465).
⇒ **שום שער וסף-ביטחון אינם יכולים להישען על ציון-האחזור. משפחה שלמה של
רעיונות נסגרה לפני שנוסתה.**

### ההעמקה: מזווג 2/10 → 4/10, ב-$1.00

14 שאלות `ANSWERED_IN_CORPUS_NOT_IN_BLOCK` מהבוררות, כולן עם ציטוט מאומת ⇒
`night/out/deepen_targets_blockdepth.json`. 3 נדחו לפני מגע (בלי בלוק /
ספרות משובשות), **7 מ-8 הועמקו, +11 סעיפים**.

| | לפני | אחרי |
|---|---|---|
| 36.0401 | 691 תווים (2.5%) | 1,585 (5.7%) |
| PM-35.0402 | 4,507 (15.2%) | 5,945 (20.0%) |

**אותן 10 שאלות, אותו קוד, הקורפוס הוא ההבדל היחיד: 2/10 → 4/10.**
שתיים עלו **מחוץ-לחלון לדירוג 1**, אחת מדירוג 2 ל-1. אפס רגרסיות בשער
(389/431 לפני ואחרי, זהה בכל תת-קבוצה).

⛔ **והמגבלה, שהיא הממצא:** שש מעשר עדיין אינן מגיעות. **העמקת בלוק עוזרת רק
היכן שהאחזור כבר מביא את הפקודה — שני המנופים סדרתיים ולא בלתי-תלויים.**
„מגיע לי כסף על ימי חופש" מחזירה קופות-היחידה ותשלומי-שחרור; 36.0401 אינה
בשמונה הראשונים, וסעיף-הפדיון שנוסף אליה יושב שם ואינו נראה.

⚠ **תמחור, פעם שנייה:** הערכה $0.50, בפועל **$1.00**. פי-2, בדיוק כמו
ב-24.08. **להכפיל כל הערכת-באץ' לפני שמאשרים.**

⚠ **`night/out/gate.json` המקובע הוא מ-415 מקרים ולא 431** — חסר-תוקף כבסיס
להשוואה. כל בדיקת-רגרסיה דורשת הרצה חוזרת עם checkout של הקורפוס.

---

## ⚡⚡ 28.08 — האחזור נתקף, והמנגנון אינו מה שחשבנו

**חמישה ניסיונות קודמים שינו את הדירוג. איש לא מדד את ההקצאה.** נמדד חינם
מול 59 יעדי-הבוררות שנושאים ציטוט מאומת מילולית:

| תצורה | פקודה עונה | **סעיף עונה** | מסמכים | מילים |
|---|---|---|---|---|
| **ייצור** n=8 d=4 c=4 | 18/59 | **5/59** | 4.9 | 715 |
| n=8 d=2 c=3 | 23/59 | 6/59 | 6.6 | **669** |
| n=8 d=1 c=1 | 24/59 | **2/59** ⛔ | 8.0 | 657 |
| n=12 d=2 c=3 | 27/59 | 8/59 | 10.3 | 972 |
| n=16 d=2 c=3 | 30/59 | **10/59** | 13.9 | 1277 |

⛔ **המספר להחזיק: בייצור, הסעיף שעונה מגיע לחלון ב-5 מ-59.** הפקודה
הנכונה מגיעה פי-שלושה יותר מהסעיף הנכון.

⚡ **`d=1` היא מלכודת** — הכי הרבה פקודות (24) והסעיף קורס מ-5 ל-2. זה
מצדיק את `top_doc_depth=4` בדיעבד. **מי שרודף אחרי דירוג-מסמכים מחמיר.**

⛔⛔ **והשער עיוור מבנית לציר הזה.** הוא בודק את שלושת המסמכים הראשונים
ה**נבדלים**, שנקבעים מדירוג-הצ'אנקים הגלובלי שאף כפתור אינו נוגע בו. ארבע
תצורות מ-8/5 ועד 16/14 החזירו **389/431 זהה לחלוטין**. ⇒ שער שטוח כאן אינו
„אין השפעה" אלא „השער אינו רואה". נעול ב-`tests/test_window_shape.py`.

✅ **אפס דחיקות ב-431/431** — החלון הרחב הוא על-קבוצה ממש. ההרחבה מסכנת
בדילול, לעולם לא באובדן. פקודה צפויה בחלון: 414 → 421 (n=8) → 425 (n=12/16).

**אבחון-הדירוג של 46 הכשלים:** 13 בדירוג 1-5, 6 ב-6-10, 11 ב-11-25,
6 ב-26-50, 10 ב-51+, חציון 15. ⚠ ו-13 הראשונים **כן הוגשו** — הם „הוגש
ולא נוצל", בדיוק המנוף השלישי שהבוררות מנתה. האבחון שחזר את הפילוח הידוע.

### ⏳ המדידה המזווגת רצה (28.08)

72 שאלות realstyle (סט מוחזק — התצורות נבחרו מפיילוט-150), שתי זרועות
**בטור** דרך ה-Batch API: `win_prod` מול `win16`. ~$4.50.

⚠ **הליג'ר נעול ב-`threading.Lock` שהוא פר-תהליך** — זרועות במקביל היו
מתחרות על `ledger.json`. חייב טורי.

⚠ **תמחור, פעם שלישית באותו יום:** ציטטתי $4.90 מ-`probe-realstyle` ($2.25)
בלי לשים לב ש-`batch_probe-realstyle.json` בדיסק ⇒ זו הייתה ריצת באץ'.
סינכרוני עולה פי-2. **לבדוק תמיד אם ה-actual ההיסטורי היה באץ'.**

---

## ⏰ מה פתוח

1. **`RETRIEVE_FULL_BLOCKS=1→2`** — ההשערה היחידה שלא נבדקה. המנגנון נמדד:
   הפקודה שעונה תורמת **צ'אנק אחד** מול **13** של המובילה; הדגל ב-2 מביא
   גם אותה ל-13. ההקשר משתנה ב-97/100. **~$4.3 מזווג.**
2. **84 המסמכים שנפתחו** בתיקון סימני-הסעיף — אף אחד מהם לא הועמק עדיין.
3. ~~הכרעת המשתמש על היעד~~ — **הוכרע 28.08: לפצל את המדד.** ⇒ היעד נמדד
   על `served`, והדרגות מפרידות „נכשלנו" מ„הפקודות שותקות ואמרנו לאן ללכת".
4. **95% מגיעות לדלת ברירת-המחדל.** כל משפחה ספציפית חדשה מקרבת ל-`served`.
   ⛔ כלל-הברזל של `out_of_scope`: **כל משפחה נבדקת מול סט שלא השתתף בכתיבתה**,
   אחרת הבדיקה מראה. (שש המשפחות הראשונות עברו הכול — ואז 30 שאלות ממסלול
   אחר חשפו מיד שתי דלתות שגויות.)
5. ~~הדלת הגנרית משתיקה כשלי-אחזור~~ — **נבנה: `night/why_default.py`.**
6. ⭐ **האחזור: המנוף שנמצא הוא ההקצאה, לא הדירוג.** ⏳ המדידה המזווגת
   רצה. **אם התשובות זזות — להנחית `RETRIEVE_MAX_CHUNKS/MAX_PER_DOC/
   TOP_DOC_DEPTH`; אם לא — כל ציר ההקצאה מת ולא לחזור אליו.**
   ⛔ ידוע ש-`top_score` חסר-מידע (AUC 0.500) ⇒ כל פתרון שנשען על
   סף-ביטחון פסול מראש.
7. **הכשלים שהועמקו וממתינים לאחזור:** 36.0401 (פדיון חופשה — הסעיף שם
   ואינו נראה), 33.0115, 31.0103, CHOK-SHIPUT-1955, PM-35.0402 (חופשת לידה).

---

## ⚡⚡ 10.09 — הסט האמיתי בורר: 45/45, והיעד נמדד עליו לראשונה

**מצב חיצוני (אומת בג'ימייל):** בקשת חופש-המידע 578310 אושרה (30.08), השלמה
נשלחה (07.09) ואושרה (08.09), **אין מענה ענייני**; מועד: 29.09. כל-זכות
**סירבו** לרישיון אחר (30.08) ⇒ קישור בלבד, כבר בשלוש דלתות.

**הבוררות:** 45 האפסים של `grade-second4` (תצורת הייצור, החיפוש השני) נפסקו
אחד-אחד מול raw_text והבלוקים, בפרוטוקול פיילוט-150. **כל 24 הציטוטים אומתו
מילולית** (`night/out/adjudication_realstyle.json`, מזהי `rs###`).

| פסק | כמה | פירוק |
|---|---|---|
| NO_SUCH_RULE | 21 | הפקודות שותקות — האפליקציה צדקה |
| ANSWERED_IN_CORPUS_IN_BLOCK | 18 | **11 הוגש-ולא-נוצל** · 7 לא הובא (אחזור) |
| ANSWERED_IN_CORPUS_NOT_IN_BLOCK | 3 | 2 עומק-בלוק · 1 לא הובא |
| NOT_IN_CORPUS | 3 | קרפ"ר 300.019 · הק"א 33-05-07 · יורשת 52.0301 — **כולן בבקשת ה-FOI** |

⇒ **21 מ-45 (47%) הם כשל של האפליקציה על מסמכים שכבר בקורפוס** — עקבי עם
`why_default` (18 COVERED). **רק 3 ייפתרו במסמך מדובר צה"ל.** והמחלקה הגדולה
ביותר היא „הוגש ולא נוצל" (11) — המסמך היה בחלון, הבלוק עונה, והמודל אמר
„טרם במאגר": 33-05-01 (טבעת, תספורת), 61.0104 (קליניקה), PM-33.0213 (שינה
בין שמירות), 35.0223 (טופס נזק), 21.0113 (טלפון), 31.0513 (איחור), 35.0107,
31.0103, HKA-31-08-01, PM-33.0302 (שינה בשמירה).

**היעד על הסרגל הכן, לראשונה:**

```
grade-second4:  strict 34/97 (35%)  ->  served 36/97 (37%)  ->  goal 65/97 (67%)
                דלתות: 2 משפחה „מאומתת" · 22 ברירת-מחדל · 0 כלום
grade-win_prod: strict 25/97 (26%)  ->  served 27/97 (28%)  ->  goal 56/97 (58%)
```

⛔⛔ **ושתי הדלתות ה„מאומתות" שגויות.** `placement_transfer` נדלקה על „אמר
שאני לא יכול לדבר עם חייל **מיחידה אחרת**" (rs016 — החלופה `יחידה\s+אחרת`),
ו-`reserve_pay` נדלקה על „פחתו לי שקל **מהמשכורת**" של חייל **חובה** (rs065 —
`משכורת` עומדת בביטוי כחלופה עצמאית בלי הקשר-מילואים). ⇒ **served כן על הסט
הזה = strict = 35%.** כלל-הברזל של `out_of_scope` עבד: סט שלא השתתף בכתיבת
המשפחות חשף שתי דלתות שגויות מיד. ⏰ לתקן את שני הביטויים, ולבדוק מול שתי
השאלות האלה **וגם** מול רשימות-הראיות הקיימות של שתי המשפחות.

✅ **תוקן (10.09, באותו יום):** `יחידה אחרת` תופס עכשיו רק לצד פועל של
העברה/הצבה בטווח משפט, ו-`משכורת` עברה אל מאחורי לוקאהד-המילואים. נבדק
מול כל 595 השאלות הייחודיות שנמדדו אי-פעם: 13 משנות דלת — rs016 ו-rs065
לברירת-המחדל, 10 שאלות-משכורת של חובה/מפקדים (q00003, q00346, q00356…)
מדלת-המילואים השגויה לברירת-המחדל, q00318 (שיניים) ל-`medical_scope` — ואף
אחת מ-9 שאלות-הראיה של שתי המשפחות לא זזה. הרחבה שנשקלה (`מעביר אות[יוה]`)
נדחתה כי תפסה „מעביר אותו ליום חפצי" (q00158). שלוש השאלות נעולות
ב-`tests/test_out_of_scope.py`. ⇒ `grade-second4`: **served 34/97 = strict**,
דלתות 0/24/0. ⏰ הצעד הבא בכסף: שאלות-משכורת של חובה נופלות עכשיו לברירת-
המחדל; הדלת הנכונה להן היא `pay_slip` (מש"קית ת"ש/שלישות מול מת"ש), אבל
`pay_slip` קודמת ל-`reserve_pay` ברשימה ולכן הוספת `משכורת` אליה תגנוב את
q00234 — דורש לוקאהד שלילי על מילואים, ולא נעשה בלי סט-בדיקה.

**22 מ-24 השתיקות הכנות נוחתות בדלת ברירת-המחדל** — זה פער served→goal של
30 נקודות, וזה המנוף. הנושאים שחוזרים: יציאות ושעות (rs044, rs056, rs011,
rs038), דיווח ומשמעת יחידתית (rs001, rs028, rs032, rs068, rs003, rs061),
כסף (rs047, rs043, rs049, rs065). כל משפחה חדשה שם מקרבת ל-served — ובודקים
אותה מול סט שלא ראה אותה.

**מה זה אומר על הדרך ל-90%:** מסמכי ה-FOI פותרים 3 מ-45. אחזור ועומק-בלוק
פותרים עד 21 (strict). משפחות-דלת פותרות עד 22 (served). ⛔ המענה מדובר
צה"ל אינו החסם.

**מכשיר:** `tests/test_report_goal.py` מקבל עכשיו גם מזהי `rs###`. ⚠ הסוויטה
המלאה לא רצה בסביבה הזאת (חסרים `anthropic`/`streamlit`) — רצו
`test_report_goal`, `test_out_of_scope`, `test_out_of_scope_default`: ירוקים.

---

## ⚡⚡ 11.09 — 11 ה„הוגש ולא נוצל" נפתחו: זה לא הפרומפט, זה הסעיף

הנחת-העבודה מ-10.09 („בעיית הרכבת-תשובה") **נפרכה בקריאת התשובות עצמן**
(`night/out/probe_second4.jsonl`, אפס עלות). המנגנון לפי שאלה:

| מנגנון | כמה | אילו |
|---|---|---|
| **המסמך הנכון בחלון, הסעיף הלא-נכון** — המסמך ישב במושב 2–5 והוגשו רק הצ'אנקים שלו שנשמעים כמו השאלה; הסעיף שעונה לא | **7** | rs060 (35.0223 #3), rs017 (61.0104 #2), rs012 (PM-33.0302 #5), rs039 (HKA #3), rs014 (33-05-01 #5 — המודל כתב במפורש „הקטעים כוללים רק את סעיף 97"), rs067 (31.0103 #4), rs009 (35.0107 #2) |
| המסמך במושב 1 והסעיף בכל זאת לא צוטט | 1 | rs005 (31.0513, בלוק 768 מילים; קונטקסט 832 ⇒ הבלוק המלא כנראה **לא** הוגש) |
| **החיפוש השני דרס פסיקה מבוססת בסירוב** | 1 (+2 מחוץ ל-11) | rs063 („אסור בשעת זמן אישי", 21.0113 §§7–8, 14–18 היו **בשני** החלונות); וגם rs071 („מותר בתנאים", 33.0220) ו-rs009 |
| **המדרג טעה** — התשובה הסופית עונה | 2 | rs033 (טבעת: „מותר בתנאים — טבעת נישואין בלבד לזכר"), rs045 (7 שעות שינה + תקרת השמירה) |

⚠ **`RETRIEVE_FULL_BLOCKS` בזרועות:** fly.toml מחזיק `"1"`, אבל חציון-הקונטקסט
של `second4` הוא 880 מילים ומקסימום 1,497 — עם מסמכים מובילים שבלוקיהם
768–7,593 מילים. **ייתכן שהזרועות רצו עם הדגל כבוי (ה-.env המקומי אינו
בריפו).** לבדוק לפני הזרוע הבאה: `python -c "import backend; print(backend.RETRIEVE_FULL_BLOCKS)"`
בסביבת המדידה, ו-`fly ssh console -C "env | grep RETRIEVE"` בפרודקשן.

### הסינון החינמי: הצהרת-החוסר מוצאת את הסעיף בתוך המסמכים שכבר בחלון

מכשיר `why_default` (ארבע-גרמים, IDF) הופנה **רק אל הסעיפים של המסמכים
שהוגשו** בחלון הראשון, עם הצהרת-החוסר של התשובה הראשונה כשאילתה, על 13
היעדים (11 + שני עומק-הבלוק):

```
הסעיף שעונה בדירוג 1 מבין סעיפי-החלון:   7/13
בשלושת הראשונים:                          9/13
```

(rs005, rs033, rs063, rs017, rs045, rs014, rs071 — #1; rs024, rs009 — #2; rs039 #4,
rs067 #6, rs060 #8, rs012 #11.) ⇒ במקום לחפש שוב בכל הקורפוס עם אותה הטמעה
שהחמיצה, לקרוא את הבלוק של מה שכבר נבחר.

### מה נכנס (שני דגלים, שניהם כבויים עד מדידה מזווגת)

- **`RETRIEVE_LACK_CLAUSES=K`** — `backend.extend_with_lack_clauses`: בחיפוש
  השני, K הסעיפים המאוצרים של המסמכים שישבו **בחלון הראשון** שהכי מתאימים
  להצהרת-החוסר, מצורפים (אף פעם לא ממיינים מחדש; דדופ מול מה שכבר בחלון).
  דטרמיניסטי, בלי קריאת מודל, ~50–100 מילים לסעיף. הנרמול זהה ל-`why_default`
  ונעול ב-`tests/test_lack_clauses.py`.
- **`RETRIEVE_SECOND_PASS_KEEP_RULING=1`** — `backend.second_answer_regressed`:
  אם התשובה השנייה היא סירוב („המידע לא קיים…") והראשונה נשאה פסיקה שאינה
  סירוב — הראשונה נשמרת (מסך, היסטוריה, מקורות), ושתי הקריאות נספרות בעלות.
  חיווט ב-`app.py`, נעול ב-`tests/test_second_pass_wiring.py`.
- `night/sectprobe.py` קורא עכשיו גם את `adjudication_realstyle.json`:
  82 יעדים (62 + 20) למדידת „הסעיף מגיע לחלון".

### ⏰ המדידה שנותרה (דורשת מפתח API ומודל הטמעה — לא היו בסביבה הזאת)

```
RETRIEVE_SECOND_PASS=4 RETRIEVE_LACK_CLAUSES=3 python -m night.arm_second lack3
```
אותן 72 שאלות, מול `grade-second4` כבסיס (האחזור לא השתנה מאז). ~$1.5 בבאץ'
(63 קריאות שניות; להכפיל הערכה לפי הלקח). התחזית מהסינון החינמי: עד 9 מ-13
החלונות יכילו את הסעיף שעונה, לעומת 0 היום. את `KEEP_RULING` למדוד באותה
זרוע או בנפרד — ההשפעה הצפויה קטנה (3/72) וברורה (rs063, rs071).

⛔ **מה לא לעשות:** `RETRIEVE_FULL_BLOCKS=2` פותר רק 2 מ-7 (rs017, rs009 —
המסמך במושב 2); במושבים 3–5 יושבים PM-33.0302 (7,593 מילים) ו-HKA (5,174) —
בלוק מלא שלהם אינו נכנס לשום חלון. הסעיף הבודד כן.

---

## ⚡ 11.09 (ב) — שלב 1.a של סבב 4 בנוי: מסלול הכותרות `RETRIEVE_V2`, כבוי, נמדד בחינם

התוכנית: `night/PLAN_ROUND4.md`. הרעיון: החייל לא מדבר כמו פקודה, אבל האוצר
כבר כתב את **כותרת** כל סעיף מאוצר בשפת חייל („אילו תכשיטים מותר לענוד עם
מדים?" — 2,357 מ-2,378 הסעיפים). אז משווים את השאלה לשאלות, לא לטקסט פקודה,
ומגישים את **הפקודה** (הבלוק) ולא שברים ממנה — „המסמך הנכון, הסעיף הלא-נכון"
לא יכול לקרות לפקודה שהוגשה בשלמותה.

### מה נכנס

- **`storage/clause_index.py`** — אינדקס כותרות-הסעיפים: ארבע-גרמים של תווים
  עם IDF (אותו מכשיר כמו `why_default` ו-`lack_clause_scores`, נרמול נעול
  בבדיקה), אינדקס הפוך, ~2 מ"ש לשאילתה, 1 שנייה לבנייה. טהור, בלי מודל.
- **`backend.extend_with_clause_index`** (דגל `RETRIEVE_V2=N`): הבלוקים
  המאוצרים של N הפקודות שהאינדקס מדרג ראשונות לשאלה, מצורפים אחרי החלון
  ואחרי `FULL_BLOCKS`, דדופ, בלי מיון מחדש. בלוק מעל `RETRIEVE_V2_BLOCK_WORDS`
  (600) מצטמצם ל-`RETRIEVE_V2_TOP_K` (6) הסעיפים שיש להם ראיה, **בסדר
  הבלוק**. רשימת הנתב = בונוס `RETRIEVE_V2_ROUTE_BONUS` (0.05, בסקאלת
  האינדקס), לא סקופ. תחום: פקודות מאוצרות של התפקיד בלבד (כמו הדירוג).
  כבוי = בייט-לבייט זהה (`tests/test_clause_index.py`, 14 בדיקות).
- **`night/titleprobe.py`** — סינון חינמי שרץ **בלי מודל הטמעה** (גם בסשן
  הזה): 83 יעדי-בוררות, ריצה שמורה ב-`night/out/titleprobe.json`.
- **`night/sectprobe.py`** מודד מעכשיו את החלון **עם ההרחבות החינמיות**
  שבסביבה (`FULL_BLOCKS`, `V2`, `QUANTITY`; HyDE מנוטרל, נתב ריק) — זה מה
  שהמודל קורא. עם דגלי-קוד ברירת-מחדל זה החלון החשוף כמו קודם.

### המספרים (חינם, 11.09)

| אינדקס: אילו יחידות | הפקודה ב-top-2 /83 | הסעיף מוגש /53 |
|---|---|---|
| **כותרות בלבד** | **20** | **16** |
| + עוגנים משויכים | 16 | 12 |
| + כותרת הפקודה | 15 | 12 |
| + שאלות-עוגן לא משויכות | 14 | 12 |

⇒ כל תוספת לכותרות **הורידה**. ברירת-המחדל: כותרות בלבד.

| כלל הגשה (V1 = החלון שהוגש בזרוע בפועל) | הפקודה מוגשת /83 | הסעיף מוגש /53 | מילים/שאלה |
|---|---|---|---|
| חלון הזרוע (V1, כל הדגלים) | 31 | ? | — |
| V2 לבדו, top-2 | 20 | 16 | 933 |
| V1 מוביל + V2 top-1 | 22 | 14 | 851 |
| V1 מוביל + 2 הבאים | 25 | 17 | 1,312 |
| top-3 של V1 ∪ V2 | 30 | 17 | 1,398 |

**הקריאה הכנה:** אינדקס הכותרות לבדו מוצא את **הפקודה** פחות מההטמעה (20
מול 31) ואת **הסעיף** הרבה יותר (16/53 מול 5/59 ההיסטורי של ההטמעה). הם
משלימים — ולכן V2 הוא הרחבה מצורפת, לא תחליף. המחיר: ~930 מילים לשאלה
ב-`RETRIEVE_V2=2` (בערך הכפלת הקונטקסט). על 11 ה„הוגש ולא נוצל": V2 מדרג את
הפקודה ראשונה ב-3 (rs063, rs017, rs039) ושנייה ב-1 (rs045); את השאר לא
(rs014 — 33-05-01 בלי שום ארבע-גרם משותף; rs005 דירוג 35). ⇒ V2 עובד על
החיפוש הראשון; `LACK_CLAUSES` (9/13 בשלושת הראשונים, מהצהרת-החוסר) על השני.
**לא תחליף לחיפוש השני — שכבה לצדו.**

### ⏰ מה נותר (מכונת המשתמש — מודל הטמעה ומפתח API)

```
# 1.b חינם, מזווג — אותה סביבה, רק הדגל משתנה:
RETRIEVE_FULL_BLOCKS=1 python -m night.sectprobe night/out/sect_prod.json
RETRIEVE_FULL_BLOCKS=1 RETRIEVE_V2=2 python -m night.sectprobe night/out/sect_v2.json
# קריטריון (PLAN 1.b): sect-in-window פי-2 לפחות. לא — עוצרים.

# 1.c הזרוע המכריעה (~$4 בבאץ', להכפיל):
RETRIEVE_SECOND_PASS=4 RETRIEVE_V2=2 RETRIEVE_LACK_CLAUSES=3 RETRIEVE_SECOND_PASS_KEEP_RULING=1 \
  python -m night.arm_second v2
# מול grade-second4. קריטריון: +5 מלאות לפחות, ≤2 אבודות. ייחוס בין הדגלים —
# מהחלונות, בחינם, אחרי שהתשובות זזו.
```

⚠ לפני שתי הריצות: `python -c "import backend; print(backend.RETRIEVE_FULL_BLOCKS)"`
בסביבת המדידה — הספק מ-11.09 (א) עדיין פתוח.

---

## ⚡ 11.09 (ג) — שלב 3.1: משפחת `unit_routine` — הצ'יפ אומר „נקבע ביחידה שלך"

בקשת המשתמש מהבוקר: שהמערכת תבדיל בין „לא קיים בפקודות מטכ"ל" לבין „זה
נקבע ביחידה שלך", ושהצ'יפ יגיד את זה. אב-הטיפוס הראשון נפסל (8/14 כיסוי,
3 תפיסות-שווא על שאלות שיש להן פקודה). הגרסה שנכנסה — **דיוק תחילה**:

- **`out_of_scope.unit_routine`** (לפני דלת ברירת-המחדל): דפוס שדורש אות של
  שעות או תנועה יומיומית (כמה שעות בחוץ, עד איזו שעה, ללכת היום בערב, לפני
  הכניסה, שעת ההשכמה, לומר לשומר לאן), עם לוקאהד שלילי **מעוגן** על כל
  השאלה (חופשה/שבת/מילואים — פ"מ 35.0402 כן קובעת; שינה/הערה בלילה —
  פ"מ 33.0213 כן קובעת, real013/real014 נענו ממנה). הדלת מצטטת את הסעיפים
  שמוסרים את סדר-היום ליחידה, מאומתים מול raw_text: 33.0401 („במועד שייקבע
  בפקודות הקבע של היחידה, לאחר שעת ההשכמה"), 33.0202, 61.0104.
- **הצ'יפ:** `app._verdict_chip(content, question)` — כשהתשובה סימנה חוסר
  (אחד משני הסמנים) והשאלה במשפחה, התווית „נקבע ביחידה שלך" במקום „לא נמצא
  בפקודות". סירוב בלי סמן לא מקבל אותה. מחווט גם בזרם וגם בהיסטוריה.

**נמדד (חינם):** אפס תפיסות על 256 השאלות שנענו בזרועות (בניכוי
NO_SUCH_RULE מבורר), על 98 המבוררות שיש להן פקודה ועל 151 האפסים
הלא-מבוררים; 5 מ-21 ה-NO_SUCH_RULE של הסט האמיתי (rs044, rs056, rs038,
rs011, rs068). ⚠ על הסטים שלא ראו — אפס תפיסות, כלומר גם אפס ראיה לכיסוי
מעבר לחמשת הנוסחים. `tests/test_out_of_scope.py` מחזיק את הדיוק **על
הנתונים** (כל שאלה שנענתה בקבצי-הזרוע שבריפו) ולא רק על נוסחים ידניים;
`tests/test_scope_routes.py` את הצ'יפ (רץ במכונה עם מודל ההטמעה).

**מה זה נותן ל-served:** 5 חלקים מ-19 היחידתיים עוברים מ-`goal` ל-`served`
(דלת-משפחה מאומתת). היתר (השכמה rs061, תאורה rs003, „לדבר עם חייל מיחידה
אחרת" rs016…) נשארים בברירת-המחדל עד שיהיו נוסחים אמיתיים נוספים.

---

## ⚡ 12.09 — „תריץ אותם בעצמך": מה רץ, מה לא יכול לרוץ כאן, ומה נבנה במקומו

**המדידות (1.b, 1.c) לא רצות בסביבת הסשן, ושתי הסיבות מדויקות:**
1. אין `ANTHROPIC_API_KEY` בסביבה (הזרוע צריכה קריאות Opus).
2. מדיניות הרשת של הסביבה חוסמת את `huggingface.co` (וגם `hf-mirror.com`) —
   מודל ההטמעה `paraphrase-multilingual-MiniLM-L12-v2` לא ניתן להורדה, ובלעדיו
   `sectprobe` ו-`arm_second` לא יכולים לדרג אפילו שאילתה אחת.
   ⛔ לא עוקפים מדיניות רשת (יש עותק של המודל ב-GCS; לא הורד בכוונה).

**שתי דרכים לפתוח את זה:** (א) להריץ במכונה שלך — הפקודות ב-11.09 (ב); (ב)
בהגדרות הסביבה של Claude Code on the web: משתנה `ANTHROPIC_API_KEY` ומדיניות
רשת שמתירה `huggingface.co` — ואז הסשן מריץ הכול. תיעוד:
https://code.claude.com/docs/en/claude-code-on-the-web

**מה כן נבנה היום (שלב 4 של רשימת-הצעדים), הכול חינם, הכול בענף:**

| | מה | מדידה |
|---|---|---|
| `ANSWER_V2` (1.3) | סירוב = משפט פתיחה + לכל היותר משפט אחד באותו נושא + שורת 2א. כבוי = הפרומפט ההיסטורי בייט-לבייט | בזרוע 1.c; אורך סירובים חינם |
| `SCOPE_UNIT_ROUTE` (3.3) | „פקודות הקבע של היחידה" ברשימת המסגרות של 2א, עם ציטוטים (33.0401, 61.0104, 21.0113). החריג המדוד לכלל „אין מסגרות פנים-צבאיות" | בזרוע 1.c; **המדד: זליגה** להמלצות-סיום של תשובות מלאות (הלקח של welfare, 10.08). יותר מאפס — נשאר כבוי |
| שלב 3.2 — שבע דלתות | `family_support_pay` חדשה (פ"מ 35.0210); הרחבות ל-`pay_slip`, `training_framework`, `placement_transfer`, `quartermaster_issue`, `medical_scope`, `unit_routine` | **נמדד:** על 549 המדודות + 152 המבוררות בדיוק 10 שינו משפחה, כולן שתיקות מבוררות, אפס שנענו |
| שלב 4 — 16 עוגנים | לחמש הפקודות של 7 מ-8 ה„לא הובאו" (rs024 הושמטה: הבלוק לא מכסה שרוולים — עוגן היה מבטיח יותר מהבלוק). נכתבו מתוכן הבלוק, לא מהשאלה; עברו את שערי `night.anchors.check` **ובנוסף** Jaccard מול 252 שאלות-הסרגל (המרבי 0.29, הסף 0.55) | בזרוע 1.c. ⚠ 7 השאלות „נראו": רווח עליהן נספר בהנחה; ההוכחה הנקייה היא סט הפיילוט |

**served על הסרגל הקפוא, בלי מדידה בתשלום** (`report_goal`, טהור):

```
grade-second4   strict 34/97 (35%)  →  served 49/97 (51%)  →  goal 65/97 (67%)
                (11.09 בבוקר: served 34;  עם unit_routine: 40;  עם 3.2: 49)
pilot150        strict 62/289       →  served 105/289       →  goal 146/289
```

הפער served→goal ירד מ-31 חלקים ל-16. מה שנשאר בברירת-המחדל הוא באמת בלי
כתובת (השכמה rs061, תאורה rs003, „לדבר עם חייל מיחידה אחרת" rs016, שעות
נוספות במילואים בלי מילת-מילואים rs049, בית-ספר rs053 — ראו הטבלה בבדיקה).

**אחרי ה-pull במכונה שלך:** האתחול הראשון מטמיע 16 מחרוזות-עוגן חדשות
(`_get_collection` → `index_all_documents`, המטמון מכסה את השאר) וכותב
`storage/embedding_cache.npz` מחדש — לקמט אותו לקומיט. הזרוע המכריעה, עם
כל מה שנבנה:

```
RETRIEVE_SECOND_PASS=4 RETRIEVE_V2=2 RETRIEVE_LACK_CLAUSES=3 RETRIEVE_SECOND_PASS_KEEP_RULING=1 \
ANSWER_V2=1 SCOPE_UNIT_ROUTE=1 python -m night.arm_second v2
```

---

## ⚡ 12.09 (ב) — שלב 1.b נמדד: V2 מתחת לקריטריון, נשאר כבוי

המשתמש הריץ את `sectprobe` המזווג במכונה שלו (קומיט 71f80c4: `sect_prod.json`,
`sect_v2.json`, 82 יעדים, `RETRIEVE_FULL_BLOCKS=1` בשתי הזרועות):

| | ייצור | V2=2 | הפרש |
|---|---|---|---|
| הפקודה שעונה בחלון | 29 | 37 | +8, 0 אבודות |
| **הסעיף שעונה בחלון** | **12** | **19** | **+7, 0 אבודות — פי 1.58** |
| מילים לשאלה, חציון | 1,155 | 2,022 | +892 |
| מסמכים בחלון, ממוצע | 5.0 | 6.8 | |

**הקריטריון שנקבע מראש היה פי 2. 1.58 לא עובר ⇒ V2 נשאר כבוי, ולא משלמים על
זרוע V2.** מה שהמדידה כן אומרת: המנגנון עובד כשהוא בוחר נכון (7 מ-8
הפקודות שנוספו הביאו איתן את הסעיף, כי הבלוק הוגש בשלמותו), אבל הוא בוחר
נכון מעט מדי — 45 מ-82 היעדים עדיין בלי הפקודה שעונה בחלון (36 פיילוט, 9
אמיתי), והמחיר 70% קונטקסט. ⚠ „פקודה בחלון, סעיף לא" נשאר ב-18 יעדים גם
עם V2 — הבלוקים הענקיים (PM-33.0302, HKA) מצטמצמים ל-6 סעיפים לפי כותרות,
וזה לא מספיק שם.

הבעיה המרכזית היא עדיין **הפקודה הלא-נכונה**, לא הסעיף הלא-נכון. עוגני-שאלה
(שלב 4, 16 חדשים) ואינדקס הכותרות תוקפים אותה מכיוונים שונים; ההטמעה עצמה
היא הצוואר.

**ההכרעה לפי התוכנית:** שלב 2 — הזרוע של החיפוש השני (`RETRIEVE_LACK_CLAUSES=3`
+ `KEEP_RULING=1`, סינון חינמי 9/13), עם דגלי-הפרומפט (1.3, 3.3) והעוגנים
שכבר בקורפוס. V2 לא בזרוע. ההשוואה מול `grade-second4`.


---

## ⚡ 12.09 (ג) — זרוע lack3 נמדדה: התשובות לא זזו כמכלול; המסגרת היחידתית בפרומפט מזיקה ויוצאת

הזרוע (קומיט 22f38c4, $2.05 בפועל מול $1.76 הערכה; נותרו $7.15 בליג'ר,
תקרה $135): החיפוש השני + `LACK_CLAUSES=3` + `ANSWER_V2=1` +
`SCOPE_UNIT_ROUTE=1` + 16 העוגנים החדשים בקורפוס, מול `grade-second4`, 72
שאלות מזווגות, 63 נענו מחדש.

⚠ **ערבוב שלא תוכנן:** `RETRIEVE_FULL_BLOCKS=1` נשאר דלוק בחלון ה-PowerShell
מריצת ה-`sectprobe` שלפניה. חציון הקונטקסט של השאלות שנענו מחדש עלה ב-891
מילים (882→1,754) — כלומר **הזרועות ההיסטוריות (כולל second4) רצו עם
`FULL_BLOCKS=0`**, והספק מ-11.09 הוכרע: הבסיס שלנו אינו הייצור של fly.toml.
הזרוע הזאת מודדת FULL_BLOCKS + LACK_CLAUSES + עוגנים + שני דגלי-הפרומפט יחד.

| | second4 | lack3 |
|---|---|---|
| תשובות מלאות | 23 | 23 |
| חלקים שנענו | 34 | 36 |
| served (report_goal) | 49/97 | 51/97 |
| אורך סירוב, חציון מילים | 130 | 66 |

**+6 / −6 בתשובות מלאות. הקריטריון (+5, ≤2 אבודות) לא הושג כמכלול.** קריאה
אחת-אחת של 12 השאלות שזזו:

| | שאלה | מה קרה |
|---|---|---|
| ✅ rs005 | חזרה באיחור (31.0513) | הבלוק/הסעיף הגיע — היה „הוגש ולא נוצל" |
| ✅ rs017 | חם וחלש → מרפאה (61.0104) | הסעיף #2 הגיע — היה „הוגש ולא נוצל" |
| ✅ rs063 | תפיסת טלפון בזמן אישי (21.0113) | „מותר בתנאים" 2/2 — החיפוש השני כבר לא דורס |
| ✅ rs033 | טבעת (33-05-01) | התשובה אומרת במפורש שהצבע לא מוגבל — המדרג קיבל |
| ✅ rs048 | פדיון חופשה (36.0401) | **עוגן משלב 4 הביא את הפקודה** |
| ✅ rs064 | מונית למרפאה | חלקי→מלא |
| ❌ rs023 | אור בחדר ב-5 בבוקר | **המסגרת היחידתית:** במקום „אסור בתנאים" מ-33.0213 (הבסיס) — „לא נקבע בפקודות מטכ"ל: פקודות הקבע של היחידה". סירוב שגוי על שאלה שיש לה פקודה |
| ❌ rs035 | קדחת, להישאר בחדר | סירוב במקום „רשאי בתנאים" מ-61.0104 — חלון אחר (עוגנים/חיפוש שני) או דגימה |
| ❌ rs040 | ימי חופש בחודש | **אותו תוכן בדיוק** („18 ימי חופשה שנתית, אין מכסה חודשית") — המדרג נתן 0. רעש-מדרג |
| ❌ rs069 | שחרור ממילואים אוטומטי? | אותו תוכן („אין זה אוטומטי — בקשה כתובה") — המדרג נתן 1/2. רעש-מדרג |
| ❌ rs037 | כמה זמן לקבל תעודה | התשובה פותחת ב„הפקודות אינן נוקבות במספר ימים" ואז נותנת את הלו"ז — המדרג קרא כסירוב |
| ❌ rs026 | אשתי בדוחק כלכלי | החלק השני (מענק אירוע) נשמט — ייתכן קיצור-הסירוב, ייתכן דגימה |

**שלוש הכרעות שנובעות מזה:**

1. **`SCOPE_UNIT_ROUTE` — כבוי, סופית.** המדד שנקבע מראש היה זליגה למעלה
   מאפס: שתי תשובות שהיו מלאות בבסיס (rs023, rs066) נותבו ל„פקודות הקבע של
   היחידה". הלקח של welfare חזר בדיוק: מסגרת ברשימה מושכת את המודל לסרב.
   (rs017 מזכירה את המשפט רק כציטוט מתוך 61.0104 — לא זליגה.)
2. **`ANSWER_V2` עושה מה שנועד** — סירוב חצי באורך (130→66 מילים) — ואין לו
   נזק מוכח; rs026/rs037 הן חשד, לא ראיה.
3. **המכלול לא הוכח.** 6 הרווחים הם בדיוק המנגנונים שחזינו (בלוק-עומק,
   החיפוש השני, עוגן). מה-6 ההפסדים, אחד ודאי של המסגרת היחידתית, שניים
   רעש-מדרג על תוכן זהה. ⇒ **זרוע ניקיון אחת** (~$2): אותה תצורה בלי
   `SCOPE_UNIT_ROUTE`, `FULL_BLOCKS=1` במפורש. שתי הזרועות הכמעט-זהות ייתנו
   גם, בחינם, את רצפת-הרעש של המדרג (כמה שאלות מתהפכות בין שתי דגימות).
   `KEEP_RULING`: אפס מקרים בזרוע הזאת — החיפוש השני לא דרס פסיקה.

⏰ **שלב 6 (סימון-מדרג) הפך דחוף:** rs040 ו-rs069 הן תשובות עם פסיקה ותוכן
זהה לבסיס שקיבלו 0. בלי סימון-לעיון, כל זרוע של 72 מוגבלת לרעש של ±3.


---

## ⚡ 12.09 (ד) — זרוע clean3: +3 תשובות מלאות, מתחת לקריטריון; רצפת-הרעש של המדרג נמדדה לראשונה

הזרוע (קומיט 67f1b44, $1.85; נותרו $5.31): כמו lack3 בלי `SCOPE_UNIT_ROUTE`,
`FULL_BLOCKS=1` במפורש. שתי השוואות מזווגות על 72:

| | second4 (בסיס) | lack3 | clean3 |
|---|---|---|---|
| תשובות מלאות | 23 | 23 | **26** |
| חלקים שנענו | 34 | 36 | **37** |
| strict / served / goal | 34 / 49 / 65 | 36 / 51 / 67 | 37 / 51 / 66 |
| אורך סירוב, חציון מילים | 130 | 66 | 65 |
| קונטקסט, חציון מילים | 882 | 1,754 | 1,754 |

**clean3 מול second4: +7 / −4 (נטו +3). הקריטריון (+5, ≤2 אבודות) לא הושג.**

**רצפת-הרעש, בחינם:** clean3 מול lack3 — אותה תצורה חוץ מהמסגרת היחידתית —
4 עלו (rs045, rs040, rs009, rs069) ו-1 ירדה (rs063). ⇒ **שתי דגימות של אותה
תצורה נבדלות ב-±3 תשובות מלאות.** זה גודל הרעש של המדרג+דגימה על 72 שאלות,
ומעכשיו זה המספר שכל דלתא נמדדת מולו. rs040 ו-rs069 שנפלו ב-lack3 על תוכן
זהה חזרו — רעש מאושר.

**קריאה של clean3 אחת-אחת.** 7 הרווחים, כולם מנגנון שתוכנן: rs005 ו-rs017
(עומק-בלוק), rs009 ו-rs063→rs045 (החיפוש השני עם מוצא-הסעיפים), rs048
(עוגן), rs033 ו-rs064. 4 ההפסדים חוזרים על עצמם בשתי הזרועות — כלומר
שיטתיים, לא דגימה — אבל שלושה מהם הם **אותו תוכן שקיבל נקודה מלאה בבסיס**:

| | | מה קרה |
|---|---|---|
| rs023 | אור ב-5 בבוקר | clean3 פוסקת „אסור בתנאים" מ-33.0213 בדיוק כמו הבסיס, ומוסיפה שורת-חוסר על שעות השינה הסדורה; המדרג נתן 0/2 בגלל שורת-החוסר |
| rs037 | כמה זמן לקבל תעודה | אותו לו"ז (11 ימים, מסירה בטקס) — פותח ב„הפקודות אינן נוקבות במשך זמן"; המדרג קרא כסירוב |
| rs026 | אשתי בדוחק כלכלי | תשלומי משפחה + מענקי אירוע, כמו הבסיס; המדרג: „התנאים לא פורטו" |
| **rs035** | קדחת, להישאר בחדר | **הפסד אמיתי**: סירוב במקום „רשאי בתנאים" מ-61.0104. הפקודה בחלון בשתי הזרועות; המסמך המוביל (PM-34.0205, לא רלוונטי) מוגש עכשיו כבלוק שלם — דילול, כשל ידוע של FULL_BLOCKS |

⇒ בקריאה ידנית: 7 רווחים אמיתיים, הפסד אמיתי אחד. במדרג האוטומטי: +7/−4.
**ההבדל הוא בדיוק שלב 6 (סימון-מדרג), שעכשיו מוכח כהכרחי:** בלי עיון ידני
בתשובות-עם-פסיקה שקיבלו 0, הסרגל אוכל את הרווחים.

**מה ההמלצה, ומה לא:**

- `SCOPE_UNIT_ROUTE` — נשאר כבוי (12.09 ג). `RETRIEVE_V2` — נשאר כבוי (12.09 ב).
- `RETRIEVE_LACK_CLAUSES=3` + `ANSWER_V2=1` — **מומלץ לפרוס**, עם הסייג הגלוי:
  מתחת לקריטריון המכני, מעל הרעש בקריאה ידנית, ואין להם הפסד מיוחס
  (rs035 נובע מ-FULL_BLOCKS שכבר בייצור לפי fly.toml). ההחלטה של המשתמש.
- 16 העוגנים כבר בקורפוס ונפרסים עם ה-push הבא ממילא.
- **אין עוד זרועות על הסרגל הזה** בלי שלב 6: הרעש ±3 גדול מכל אפקט צפוי של
  כפתור אחד. הכסף שנותר ($5.31) שמור לגל-בדיקה של הדלתות (~$0.4) ולמסמכי
  FOI (דורש העלאת תקרה).

⚠ **הבסיס ההיסטורי:** כל הזרועות עד second4 רצו עם `FULL_BLOCKS=0`; fly.toml
מחזיק 1. עדיין לא אומת ב-`fly ssh console` מה הייצור מריץ בפועל.

---

## ⚡⚡ 12.09 (ה) — ההתחלה מחדש של הליבה: `RETRIEVE_V3`, אינדקס ברמת הסעיף עם מנוע-הטמעה מתחלף

**ההחלטה (המשתמש, 12.09):** לא לשכתב את התשובה — להחליף את **ליבת האחזור**,
כי אחרי שתי זרועות ושני מכשירים חינמיים התמונה היא מספר אחד: **ב-45 מ-82
השאלות שנמדדו הפקודה שעונה בכלל לא מגיעה למודל.** כל כפתור עד היום מזיז את
המספר הזה ב-5 עד 8 ועולה בקונטקסט. שני חשודים מתחת לכולם:

1. **האינדקס.** 10,324 חלונות של 180 מילים חתוכים מטקסט סרוק, מדורגים יחד
   עם הסעיפים המאוצרים. חלון גולמי חולק אוצר-מילים עם הכול ומטביע את הסעיף
   שעונה.
2. **המנוע.** `paraphrase-multilingual-MiniLM-L12-v2` — 118M פרמטרים,
   384 ממדים, חלש בעברית.

**`storage/clause_embed.py`** משנה בדיוק את שניהם, מאחורי דגל, כמסלול מקביל:
וקטור אחד לכל **סעיף מאוצר** (כותרת-השאלה + הטקסט) ולכל שאלת-עוגן, **כלום
מהטקסט הגולמי** — 2,378 סעיפים + 1,379 עוגנים במקום 10,324 שברים. המנוע
נבחר ב-`RETRIEVE_V3_MODEL`: `minilm` מפעיל את מחסנית הייצור (בלי הורדה
חדשה, כדי שהסינון הראשון יבודד „אינדקס נקי" מ„מודל טוב יותר"), ולצידו
`e5-small` / `e5-base` / `e5-large` / `bge-m3` — ייצוא ONNX רב-לשוני חזק
יותר, נטען באותה דרך כמו הייצור (arena כבוי, באצ' של 8). `RETRIEVE_V3=N`
מגיש את הבלוקים של N הפקודות הטובות, מצורף כמו V2; **`RETRIEVE_V3_ONLY=1`
מבטל את החלון הגולמי לגמרי** — אין דירוג צ'אנקים בכלל, החלון הוא מה
שהאינדקס מגיש. וקטורים במטמון לפי מודל
(`storage/clause_embed_cache_<model>.npz`), אז עריכת קורפוס מטמיעה רק את מה
שהשתנה.

`tests/test_clause_embed.py` — 8 בדיקות עם מנוע מזויף (טרי-גרמים מגובבים),
בלי מודל, בלי רשת. כבוי = בייט-לבייט זהה.

### ⏰ הסינון החינמי במכונה שלך — שלוש ריצות, אפס דולר

```powershell
$env:RETRIEVE_FULL_BLOCKS = "1"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_prod.json        # יש, 29/82
$env:RETRIEVE_V3 = "2"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_v3_minilm.json   # אינדקס נקי, אותו מודל
$env:RETRIEVE_V3_MODEL = "e5-base"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_v3_e5.json       # + מודל חזק (הורדה ~280MB)
$env:RETRIEVE_V3_ONLY = "1"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_v3_only.json     # בלי החלון הגולמי בכלל
```

**המספר שמכריע: „הפקודה שעונה בחלון", 29 מ-82 היום.** הקריטריון שנקבע מראש:
**50 ומעלה** באחת מהתצורות ⇒ זרוע אחת ($2) ופריסה. פחות מ-40 ⇒ הליבה אינה
החסם, והמאמץ עובר לקורפוס (מסמכי FOI) ולתוכן.

⚠ ההורדה של e5-base היא ~280MB חד-פעמית; הסשן הזה חסום מול huggingface.co
ולכן לא יכול להריץ אף אחת מהן.

---

## ⚡⚡ 12.09 (ו) — הסינון של V3 הוכרע: **הליבה אינה החסם.** מה שכן החסם, במספרים

ארבע תצורות, 82 יעדים, אפס דולר (קומיט 657e128):

| תצורה | הפקודה שעונה בחלון | הסעיף שעונה בחלון | מילים/שאלה |
|---|---|---|---|
| ייצור היום | 29/82 | 12/82 | 1,244 |
| V3, אינדקס נקי, **אותו מודל** | 31/82 | 12/82 | 1,857 |
| V3, אינדקס נקי + **e5-base** | **33/82** | **15/82** | 2,042 |
| V3 בלבד, בלי החלון הגולמי | 18/82 | 10/82 | 928 |

**הקריטריון שנקבע מראש: ≥50 ⇒ זרוע; <40 ⇒ הליבה אינה החסם. התוצאה 33.**
⇒ **הליבה אינה החסם. V3 נשאר כבוי, אין זרוע, לא משלמים.**

### מה שהמדידה כן גילתה, והוא חשוב מהתוצאה עצמה

1. **המודל החזק שווה משהו אמיתי אבל קטן:** e5-base מעל minilm — +2 פקודות,
   **+3 סעיפים** (12→15). האינדקס הנקי לבדו (אותו מודל): +2 פקודות, **אפס
   סעיפים**. ⇒ הטקסט הגולמי לא היה הבעיה; ההטמעה כן, וגם היא רק חלקית.
2. **„רק V3" הוא אסון:** 18/82, ואיבד 15 פקודות שהחלון הגולמי כן מביא.
   הצ'אנקים הגולמיים נושאים בדיוק את מה שאין בבלוק המאוצר.
3. ⛔ **הממצא המרכזי: הדירוג טוב בהרבה מהחלון.** בדירוג הגלובלי (בלי חלון)
   הפקודה שעונה יושבת ב:

   | עומק | הפקודה שעונה נמצאת |
   |---|---|
   | top-8 | 35/82 |
   | top-16 | 43/82 |
   | **top-25** | **54/82** |
   | top-40 | 59/82 |

   החלון מביא 29 מתוך 35 שהדירוג כבר מסדר ב-8 הראשונים, ומתוך **54**
   שהוא מסדר ב-25 הראשונים. הפער 29→54 אינו „מנוע הטמעה חלש" — הוא
   **צורת החלון**: `top_doc_depth=4` ו-`max_per_doc=4` מוציאים 4 מושבים
   על הפקודה המובילה כדי למנוע „פקודה נכונה, סעיף לא נכון", וזה גובה
   כיסוי-פקודות. **הגשת בלוק פותרת את אותה בעיה בלי לגבות מושבים.**
4. **מה ההרכבה הטובה ביותר, חינם, לפי הנתונים:**

   | כלל | פקודה | סעיף |
   |---|---|---|
   | היום | 29/82 | 12/82 |
   | 6 הפקודות המובילות בדירוג, כל אחת כבלוק, ∪ V3(e5) | **35/82** | **23/82** |

   כמעט **פי 2 בסעיף שעונה** — המדד שקובע — במחיר של כ-3,300 מילים לשאלה.

### ⇒ ההמלצה החדשה (עודכנה לפי המדידה)

לא זרוע של V3. **שלושה צעדים, לפי סדר, שניים מהם חינם:**

1. ⚡ **`RETRIEVE_DOC_BLOCKS=K`** (חינם לבנות): במקום חלון של 8 צ'אנקים עם
   תקרות — K הפקודות המובילות של **הדירוג הקיים**, כל אחת מוגשת כבלוק, ∪
   הצ'אנקים הגולמיים שנותרו. לפי הנתונים: 12→23 בסעיף שעונה. זו התוכנית
   המקורית (שלב 1.2) עם כלל-בחירה שנמדד ולא נוחש.
2. **מדרג-על על 25 הראשונים** (Haiku, ~$0.002 לשאלה): 54/82 יושבים שם.
   זה התקרה של כל אחזור על הקורפוס הזה, והדרך היחידה שנמדדה להגיע אליה.
3. רק אחרי ששניהם נמדדו בחינם — זרוע אחת, אם הם עוברים.

⚠ **התקרה שאי אפשר לעקוף:** 28 מ-82 (34%) — הפקודה שעונה אינה בטופ-25 של
שום תצורה. עבורן החסם הוא הקורפוס והאוצרות, לא האחזור.

---

## ⚡ 12.09 (ז) — `RETRIEVE_DOC_BLOCKS`: החלון נבנה מחדש כ**פקודות**, לא כצ'אנקים

הצעד שהמדידה של (ו) הצביעה עליו, בנוי וכבוי (`tests/test_doc_blocks.py`,
8 בדיקות בלי מודל).

**מה הוא עושה.** במקום „8 צ'אנקים עם תקרה של 4 למסמך ועומק 4 למוביל":
N הפקודות הראשונות של דירוג **עמוק** (`RETRIEVE_DOC_BLOCKS_POOL=40`, אותו
מדרג, בלי קריאת מודל נוספת), כל אחת מוגשת כ**בלוק המאוצר שלה** — ואחריהן
הצ'אנקים הגולמיים שדורגו, מצורפים ולא נזרקים.

**למה זה נכון דווקא עכשיו.** `top_doc_depth=4` ו-`max_per_doc=4` נכתבו כדי
למנוע „פקודה נכונה, סעיף לא נכון" — והם עושים זאת בכך שהם מוציאים 4 מ-8
המושבים על הפקודה המובילה. **הגשת בלוק מבטלת את אותו כשל מעצם הגדרתה**
(כל סעיפי הפקודה שם), ולכן המושבים משתחררים. הצ'אנקים הגולמיים נשארים כי
9 מ-82 היעדים נענים מטקסט גולמי שאין לו סעיף מאוצר, והריצה „רק V3" איבדה
15 פקודות בדיוק בגלל שזרקה אותם.

**הצפי, מהנתונים שכבר נמדדו (night/out/sect_prod.json + הבוררות):**

| | הפקודה שעונה | הסעיף שעונה | מילים |
|---|---|---|---|
| היום | 29/82 | 12/82 | 1,244 |
| 6 בלוקים ∪ אינדקס-הכותרות | 35/82 | 23/82 | ~3,300 |

### ⏰ המדידה החינמית במכונה שלך — שלוש ריצות

```powershell
git pull --ff-only origin claude/intelligent-hypatia-t8h569
$env:RETRIEVE_FULL_BLOCKS = "1"
$env:RETRIEVE_DOC_BLOCKS = "4"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_blocks4.json
$env:RETRIEVE_DOC_BLOCKS = "6"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_blocks6.json
$env:RETRIEVE_V2 = "2"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_blocks6_v2.json
Remove-Item Env:RETRIEVE_DOC_BLOCKS, Env:RETRIEVE_V2
git add -f night\out\sect_blocks4.json night\out\sect_blocks6.json night\out\sect_blocks6_v2.json
git commit -m "sectprobe: doc-blocks window, 4 vs 6 vs 6+V2"
git push origin HEAD:claude/intelligent-hypatia-t8h569
```

**הקריטריון שנקבע מראש: „הסעיף שעונה בחלון" ≥ 20 מ-82** (היום 12) באחת
משלוש התצורות, **בלי לאבד פקודות** — ואז זרוע אחת (~$2). פחות מזה: האחזור
מוצה, והמאמץ עובר לקורפוס (FOI) ולתוכן.

---

## ⚡⚡ 12.09 (ח) — החלון-כפקודות נמדד: **הסעיף שעונה 12 → 24 מ-82**, והזנב הגולמי נחשף כבזבוז

ארבע ריצות, אפס דולר (קומיט f025abd), מול ייצור:

| תצורה | הפקודה שעונה | **הסעיף שעונה** | מילים (חציון) | מסמכים |
|---|---|---|---|---|
| ייצור היום | 29/82 | **12/82** | 1,149 | 5 |
| 4 בלוקים | 58/82 | 17/82 | 4,690 | 38 |
| 6 בלוקים | 58/82 | 18/82 | 5,349 | 38 |
| **6 בלוקים + אינדקס-הכותרות** | **61/82** | **24/82** | 6,089 | 39 |
| (V3 e5 לשם השוואה) | 33/82 | 15/82 | 2,042 | 6 |

**הקריטריון (≥20 סעיפים, בלי לאבד פקודות) — הושג: 24, ואפס פקודות אבודות**
(סעיף אחד אבד, rs071).

### ⛔ אבל הפירוק חושף בזבוז גדול, ותוקן מיד

מ-29 הפקודות שהזנב הגולמי הוסיף — **3 בלבד הביאו איתן את הסעיף שעונה.**
זה שוב „פקודה נכונה, פסקה לא נכונה": צ'אנק בודד של הפקודה הנכונה הוא כמעט
תמיד הסעיף הלא-נכון. הזנב עלה ~3,500 מילים לשאלה וקנה שלוש תשובות.

⇒ **`RETRIEVE_DOC_BLOCKS_RAW=4`** (חדש): הזנב הגולמי מוגבל ל-4 צ'אנקים.
הוא קיים בשביל 9 מ-82 היעדים שנענים מטקסט שאין לו סעיף מאוצר — לא בשביל
כיסוי. הצפי: אותם ~24 סעיפים בכ-3,500 מילים במקום 6,089.

⚠ **גם באג אמיתי נתפס בדרך:** `_append_new` בדק את התקרה **אחרי** ההוספה,
ולכן `limit=0` היה מוסיף צ'אנק אחד. לא הזיק עד היום (כל דגל היה מגודר
ב-`> 0`), תוקן ונעול בבדיקה.

### ⏰ הריצה האחרונה לפני זרוע — שלוש תצורות, חינם

```powershell
git pull --ff-only origin claude/intelligent-hypatia-t8h569
$env:RETRIEVE_FULL_BLOCKS = "1"; $env:RETRIEVE_V2 = "2"
$env:RETRIEVE_DOC_BLOCKS = "6"; $env:RETRIEVE_DOC_BLOCKS_RAW = "4"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_b6r4.json
$env:RETRIEVE_DOC_BLOCKS = "4"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_b4r4.json
$env:RETRIEVE_V2_BLOCK_WORDS = "350"
venv\Scripts\python.exe -m night.sectprobe night\out\sect_b6r4w350.json
Remove-Item Env:RETRIEVE_DOC_BLOCKS, Env:RETRIEVE_DOC_BLOCKS_RAW, Env:RETRIEVE_V2, Env:RETRIEVE_V2_BLOCK_WORDS
git add -f night\out\sect_b6r4.json night\out\sect_b4r4.json night\out\sect_b6r4w350.json
git commit -m "sectprobe: capped raw tail, 6/4 blocks and a 350-word block cut"
git push origin HEAD:claude/intelligent-hypatia-t8h569
```

**מה מחפשים:** התצורה הזולה ביותר ששומרת על **≥20 סעיפים**. אם אחת מהן
מגיעה לשם בכ-3,000 מילים — זו התצורה שנכנסת לזרוע ($2) ואז לייצור.

---

(סעיף 12.09 (ח) שלמעלה נכתב במקביל בסשן אחר, על המכשיר המילולי; ה-RAW=4 שבו נבלע כאן — מאז 16.09 (ב) הבריכה כלל לא מוגשת, הבלוקים הם הרחבה אחרי חלון-8.)

## ⚡⚡ 16.09 — המכשיר היה עיוור לבלוקים; עם כלל-תוכן, 4 בלוקים מביאים את הסעיף ב-34 מ-82 (16 היום), אפס אבודות

**מה קדם לזה (חינם, סקריפטים בסקראצ'פד של הסשן; הממצאים בזיכרון-הפרויקט 16.09):**
ייחוס-מנגנונים על 82 היעדים — ב-**35 מ-82 הפקודה מדורגת והבלוק שלה נושא את
הכלל, והוא לא מוגש**; הפקודה המובילה היא העונה רק ב-12 מ-82, ובלוק-המוביל
(FULL_BLOCKS) תופס חציון 74% ממילות-החלון ⇒ הסעיף מוגש בדיוק כשה-#1 צודק;
הצ'אנק הגולמי שנושא את הציטוט מדורג בטופ-200 ב-4 מ-82 בלבד (הדירוג חי על
כותרות-הסעיפים המאוצרים). ובדיקת-תוכן מול הבלוקים עצמם: **הבלוק מכיל את הכלל
ב-73 מ-83.** ⇒ מכשיר-הסעיף (ציטוט מילולי / רצף-6-מילים) לא רואה בלוק שמנסח
מחדש בשפת-חייל. „אפס רווח בסגנון-אמיתי" של מדידת 12.09 (ז) היה ארטיפקט-מכשיר.

**התיקון (`night/sectprobe.py`, `tests/test_sectprobe_content.py` — 7 בדיקות
בלי מודל):** כלל-תוכן לצד הכלל המילולי — סעיף **מאוצר** מוגש של הפקודה שעונה
נספר כשהוא נושא ≥ `CONTENT_MIN`=0.30 ממילות-התוכן של ציטוט מאומת (מקסימום על
הציטוטים; מילים של 3+ אותיות, לא מילת-עצירה, לא מספר). כויל על חמישה מקרים
ידניים: נושאים 0.36–0.54, לא-נושאים 0.05–0.20. צ'אנקים גולמיים נשארים
מילוליים (שכן גולמי חולק אוצר-מילים בלי לשאת את הכלל). שני המספרים מדווחים
(`sect_in_window`, `sect_in_window_content`); המילולי שומר על ההשוואה לכל
הקבצים ההיסטוריים — הבסיס יצא 12/82 מילולי, זהה לקובץ הישן.

**ארבע ריצות, אותו מכשיר, אפס דולר** (עץ-עבודה `D:/_sectprobe_wt`, ענף
`claude/sectprobe-content`, קבצים `night/out/sect2_*.json`; RETRIEVE_FULL_BLOCKS=1
בכולן; אין `.env` בעץ ⇒ אין דרך לשלם):

| תצורה | פקודה בחלון | סעיף מילולי | **סעיף לפי תוכן** | סגנון-אמיתי (20) | מילים, חציון | מסמכים |
|---|---|---|---|---|---|---|
| ייצור | 29 | 12 | **16** | 6 | 1,149 | 5 |
| DOC_BLOCKS=4 | 58 | 17 | **34** | 11 | 4,690 | 38 |
| DOC_BLOCKS=6 | 58 | 18 | **34** | 11 | 5,349 | 38 |
| DOC_BLOCKS=6 + V2=2 | 61 | 24 | **40** | 11 | 6,089 | 39 |

מזווג מול ייצור, כלל-תוכן: בלוקים-4 **+18 / −0**; בלוקים-6 +18 / −0 (6 לא
מוסיף על 4); 6+V2 **+24 / −0**. סגנון-אמיתי 6→11 (rs009, rs033, rs039, rs060,
rs067), אפס אבודות. rs071 ש„אבד" במדידת 12.09 היה ארטיפקט מילולי — לפי תוכן
הוא מוגש בכל התצורות (0.82). הקריטריון של 12.09 (ז), ≥20 בלי לאבד פקודות,
עובר בכל שלוש התצורות — אבל שתי ההסתייגויות למטה קודמות לזרוע.

**שני הדלפים שנותרו בנתיב הבלוקים:**
1. **החיתוך.** בלוק מעל `RETRIEVE_V2_BLOCK_WORDS`=600 נחתך ל-6 סעיפים לפי
   כותרות. ב-4 מ-82 (q00152 פ״מ 35.0402, q00046 61.0104, q00116 31.0513, rs054
   35.0807) הפקודה בטופ-4 והבלוק נושא את הכלל — והחיתוך זרק אותו. כל הארבעה
   מעל 600 מילים (35.0807: 732).
2. **הבריכה.** `doc_blocks_window` מצרף את **כל** 40 צ'אנקי-הבריכה
   (`_append_new(out, pool, None)`) ולא את חלון-8: 38 מסמכים ו-4,700 מילים במקום
   5 ו-1,150. הבריכה כמעט לא מביאה סעיפים (המילולי עלה ב-5 בלבד) ומנפחת פי 4.
   „פקודה בחלון 58" = „פקודה בטופ-40 של הדירוג", לא הישג של הבלוקים.

**מה נותר לא-מוגש אחרי 6+V2 (42 מ-82):** לא מדורגת אף שהבלוק מכיל 20 ·
מדורגת בטופ-25 ונחתכה במושבים 9 · בחלון ולא הוגש 5 · הבלוק לא מכיל 8.

**הצעד הבא, חינם:** וריאנט „K בלוקים + חלון-8" בלי הבריכה, וחיתוך שמכבד את
הכלל (סף גבוה יותר, או בחירת-סעיפים לפי תוכן השאלה ולא רק כותרת); sectprobe
מחדש. קריטריון: סעיף לפי תוכן ≥ 34 בפחות מ-3,000 מילים, אפס אבודות. אחריו —
הזרוע המזווגת (~$2) על 72 מול second4 עם עיון שלב 6, או זרוע-אורקל לתקרה.

**לקומיט, מהעץ:**
```
cd D:/_sectprobe_wt
git add night/sectprobe.py tests/test_sectprobe_content.py HANDOFF.md night/PLAN_ROUND4.md
git add -f night/out/sect2_prod.json night/out/sect2_blocks4.json night/out/sect2_blocks6.json night/out/sect2_blocks6_v2.json
git commit -m "sectprobe: content rule beside the verbatim one; 4 blocks 16->34 of 82, zero losses"
git -C D:/app_soldier merge --ff-only claude/sectprobe-content
```
⚠ `night/numbers.py` מצל על `numbers` של הספרייה הסטנדרטית — לעולם לא להכניס את
`night/` עצמו ל-`sys.path` (pydantic/decimal נופלים בייבוא).

---

## ⚡⚡ 16.09 (ב) — הבלוקים כהרחבה: חלון-8 נשאר, הבלוקים מצטרפים אחריו, הבריכה מדרגת ולא מוגשת

**השינוי (`backend.py`, `tests/test_doc_blocks.py` — 8 בדיקות, שתיים חדשות שמפילות
את הצורה הישנה; חבילת-הבדיקות המלאה 54/54):** `RETRIEVE_DOC_BLOCKS` כבר לא מחליף
את החלון בתוך `retrieve_for_role` אלא מצטרף כהרחבה ב-`widen_context`
(`extend_with_doc_blocks`, אחרי בלוק-המוביל ולפני V2). שתי סיבות מדודות: איחוד
השכתוב/המקורי והחיפוש השני חותכים ל-`MAX_CONTEXT_CHUNKS` לפני `widen_context`,
כך שכל מה שהוגש בתוך `retrieve_for_role` מעבר לחלון היה נחתך בייצור; והבריכה של
40 ניפחה פי 4. `doc_blocks_window` מחזיר עכשיו רק את הבלוקים; הבריכה העמוקה
מדרגת פקודות ולא מוגשת.

**הרשת המלאה, כלל-תוכן, 82 יעדים, אפס דולר, אפס אבודות בכל התצורות
(`night/out/sect2_*_win.json`):**

| תצורה | פקודה בחלון | סעיף לפי תוכן | סגנון-אמיתי (20) | מילים, חציון | מסמכים |
|---|---|---|---|---|---|
| ייצור | 29 | 16 | 6 | 1,149 | 5 |
| 4 בלוקים + חלון | 29 | 24 | 10 | 2,183 | 5 |
| **6 בלוקים + חלון** | 33 | 27 | **11** | **2,972** | 6 |
| 4 בלוקים + V2 + חלון | 37 | 32 | 10 | 3,061 | 7 |
| **6 בלוקים + V2 + חלון** | 41 | **35** | 11 | 3,718 | 8 |
| 4 בלוקים + בריכה (12.09) | 58 | 34 | 11 | 4,690 | 38 |
| 6 בלוקים + V2 + בריכה (12.09) | 61 | 40 | 11 | 6,089 | 39 |

**מה זה אומר.** (1) הבלוקים של הפקודות שכבר בחלון לבדם: 16→24. (2) K=6 מוסיף
פקודות מעבר לחלון (29→33) ועוד 3 סעיפים, ביניהם rs060. (3) V2 מוסיף 8 סעיפים,
כולם מסט-הפיילוט — בסגנון-אמיתי הוא לא מזיז. (4) מה שהבריכה הביאה מעבר לזה (5
יעדים, כולם פיילוט: q00251, q00096, q00099, q00067, q00146) הוא סעיף בודד של
הפקודה הנכונה בדירוג 9–40 — כלומר „חלון רחב", לא „בלוקים"; זה עולה +2,400 מילים
לשאלה ואינו מזיז את הסגנון-האמיתי. (5) הסגנון-האמיתי: 6→11 מגיע כבר ב-„6 בלוקים
+ חלון" ב-2,972 מילים (פי 2.6); הרווחים: rs009, rs033, rs039, rs060, rs067.

**הקריטריון שנקבע (≥34 בפחות מ-3,000 מילים) לא מתקיים באף תצורה נקייה** — כל
1,000 מילים קונות כ-4 סעיפים. ההכרעה היא מחיר: לסרגל-72 (סגנון-אמיתי) „6 בלוקים
+ חלון" נותן את אותם +5 סעיפים כמו התצורה היקרה, ב-20% פחות הקשר מ-„+V2".

**הדלף שעוד פתוח, חינם:** חיתוך-בלוק >600 מילים ל-6 סעיפים לפי כותרות זרק את
הכלל ב-4 (q00152, q00046, q00116, rs054 — +1 סגנון-אמיתי). כפתור אחד:
`RETRIEVE_V2_BLOCK_WORDS` גבוה יותר או בחירת-סעיפים לפי תוכן השאלה; למדוד לפני
הזרוע.

**ההמלצה לזרוע (~$2 בבאץ', להכפיל):** `RETRIEVE_FULL_BLOCKS=1 RETRIEVE_DOC_BLOCKS=6`
(בלי V2) מול clean3 על 72, עם עיון שלב 6. הצפי מהמכשיר: +5 סעיפים ב-20 היעדים
⇒ כ-+4 תשובות מלאות, על רצפת-רעש ±3 — לכן העיון הידני חובה. תוצאה שטוחה אומרת
שהחסם עבר למנוע (20 „לא מדורגת אף שהבלוק מכיל") ולתקרה — זרוע-האורקל.


---

## ⚡⚡ 17.09 — זרוע blocks6 נמדדה: **+7 / −1 תשובות מלאות אחרי עיון — הקריטריון עבר.** strict 41→47, served 55→61 מ-97

**הזרוע** (`night/out/probe_blocks6.jsonl`, `grades_grade-blocks6.jsonl`,
`review_grade-blocks6.json`): התצורה של clean3 ועוד `RETRIEVE_DOC_BLOCKS=6` —
`RETRIEVE_HYDE=1 RETRIEVE_GLOSSARY=1 RETRIEVE_FULL_BLOCKS=1 RETRIEVE_SECOND_PASS=4
RETRIEVE_LACK_CLAUSES=3 RETRIEVE_SECOND_PASS_KEEP_RULING=1 ANSWER_V2=1
RETRIEVE_DOC_BLOCKS=6 python -m night.arm_second blocks6` (בלי V2, בלי
SCOPE_UNIT_ROUTE, בלי ROUTER_SLOTS — כמו הפקודות המתועדות של הזרועות הקודמות; אם
clean3 רצה עם ROUTER_SLOTS=2 זה הבדל שני לא-מתועד). 63 נענו מחדש, 9 הועברו
מ-win_prod. הבאץ' הוגש 16.09 11:23, הסתיים 16:26 (שלוש שעות; ההיסטוריה: 6–7
דקות), תהליך-הדגימה מת עם הסשן ונאסף 17.09 מכרטיס-התביעה
(`scratchpad/finish_blocks6.py` שיחזר את זנב `arm_second`: איסוף, צירוף
המועברות, דירוג). **מחיר בפועל $3.41 + דירוג $0.11 מול הערכה $1.76 — כלל-ההכפלה
שוב; ההקשר גדל פי 2.2 (חציון 1,748→3,820 מילים).** ליג'ר **$133.18/$135, נותרו
$1.82.**

| | clean3 | blocks6 |
|---|---|---|
| תשובות מלאות, כפי שדורג | 26/72 | 29/72 |
| **תשובות מלאות, אחרי עיון** | 29/72 | **35/72** |
| חלקים, כפי שדורג | 37/97 | 41/97 |
| **חלקים, אחרי עיון (strict)** | 41/97 (42%) | **47/97 (48%)** |
| served, אחרי עיון | 55/97 (57%) | **61/97 (63%)** |
| goal, אחרי עיון | 70/97 (72%) | 76/97 (78%) |
| סירובים | 36 | 31 |
| הקשר, חציון מילים | 1,748 | 3,820 |

**מזווג, כפי שדורג: +8 / −5. אחרי עיון שלב 6: +7 / −1.** הקריטריון שנקבע מראש
(+5 מלאות, ≤2 אבודות אחרי עיון) — **עבר.**

**7 הרווחים, כולם מנגנון הבלוקים** (הפקודה הייתה בחלון או בטופ-6, והבלוק הביא
את הסעיף): rs000 בעלות על חנות (33.0115 §43) · rs029 חידוש הלנה (PM-31.0251) ·
**rs035 קדחת (61.0104 — „ההפסד האמיתי" של clean3 חזר)** · rs057 סגנון זקן
(33-05-01) · rs060 טופס נזק (35.0223, כפי שהטרייס חזה) · rs054 לינת בית
(הת״ש 07 מ-35.0807 — היה 0 בכל הזרועות) · rs067 מסמכי שחרור (31.0103).
**ההפסד האחד, אמיתי:** rs009 קופת הפלוגה — הבסיס ענה מ-35.0107, הזרוע סירבה
(„כספי היחידה, לא כספים פרטיים") אף שהבלוק היה בחלון: זהירות-יתר עם הקשר
רחב. **ארבעה „הפסדים" מכניים היו רעש-מדרג על תוכן זהה** (rs019, rs040, rs045,
rs069 — שלושה מהם התהפכו גם בזרועות קודמות), והעיון החזיר אותם; 8 ההכרעות
ב-`review_grade-blocks6.json` עם נימוק לכל אחת.

**מה זה אומר על המשפך:** הסעיף-בחלון עלה במכשיר מ-6 ל-11 מ-20 בסגנון-אמיתי,
והתשובות המלאות עלו ב-6 נטו. המכשיר החינמי המתוקן חזה את סדר-הגודל נכון
(כמו החלון הרחב ב-08.2026). זה הסבב הראשון מאז 28.08 שעובר קריטריון מכני מלא.

### ⏰ ההמלצה: לפרוס — באישור המשתמש, לכל פריסה בנפרד
`fly.toml`: `RETRIEVE_DOC_BLOCKS="6"` + `RETRIEVE_LACK_CLAUSES="3"` +
`RETRIEVE_SECOND_PASS_KEEP_RULING="1"` + `ANSWER_V2="1"` (ההמלצה מ-12.09 שטרם
נפרסה) על גבי הדגלים הקיימים; לפני כן מיזוג הענף ל-main (מיזוג יבש נקי ב-16.09,
לוודא שוב). **מחיר:** ההקשר פי 2.2 ⇒ קלט פי ~2 לשאלה; הפלט (רוב המחיר) לא
משתנה ⇒ כ-+30% לשאלה מול תקציב ה-$20/חודש. **זיכרון:** אינדקס-הכותרות נבנה
בזיכרון (קטן); הדירוג העמוק הוא קריאה מקומית נוספת (~0.2 ש'). לאמת חי אחרי
הפריסה על שאלת-העשן („מגיע לי כסף על ימי חופש שלא ניצלתי?") ועל rs060/rs054.

**מה אחרי הפריסה:** (1) הדלף הפתוח החינמי — חיתוך-בלוק >600 מילים (4 יעדים,
rs054 נענתה בכל זאת); (2) 20 היעדים „לא מדורגת אף שהבלוק מכיל" — חזית המנוע
(מדרג-על Haiku על 25 הראשונים, ~$0.002 לשאלה) — **דורש העלאת תקרה, הליג'ר ריק**;
(3) מסמכי FOI ב-29.09.


---

## ⚡ 17.09 (ב) — חיתוך-הבלוק נמדד: +2 עד +4 סעיפים מ-82, ב-+12% עד +42% הקשר. הכפתור הזול: 12 סעיפים

**מה נמדד.** על התצורה הפרוסה (`RETRIEVE_DOC_BLOCKS=6`, חלון-8, בלי בריכה) בלוק
מעל `RETRIEVE_V2_BLOCK_WORDS`=600 מילים נחתך ל-`RETRIEVE_V2_TOP_K`=6 סעיפים לפי
אינדקס-הכותרות. ב-4 יעדים החיתוך זרק את הכלל. חמש תצורות של שני הכפתורים,
sectprobe עם כלל-התוכן, אפס דולר (`night/out/sect3_cut_*.json`), מזווג מול
`sect2_blocks6_win`:

| תצורה (מילים / סעיפים) | סעיף לפי תוכן | Δ | סגנון-אמיתי (20) | מילים, חציון | p90 |
|---|---|---|---|---|---|
| **פרוס: 600 / 6** | 27 | | 11 | 2,972 | 3,583 |
| 1200 / 6 | 29 | +2 (q00116, rs054) | 12 | 3,916 | 4,836 |
| **600 / 12** | 29 | +2 (q00152, q00046) | 11 | **3,321** | 4,350 |
| 600 / 16 | 29 | +2 (אותם שניים) | 11 | 3,364 | 4,781 |
| 1200 / 12 | **31** | +4 (כל הארבעה) | 12 | 4,230 | 5,407 |
| 900 / 12 | 30 | +3 | 12 | 3,922 | 4,919 |

אפס אבודות בכל התצורות. הקריטריון שנקבע מראש (≥30 סעיפים, אפס אבודות, חציון
≤3,500 מילים) **לא מתקיים באף תצורה**: 600/12 ו-600/16 נשארות מתחת ל-3,500 אבל
מגיעות ל-29; 900/12 ו-1200/12 עוברות 30 ב-3,900–4,200 מילים.

**למה הכפתורים משלימים.** הרחבת המילים מגישה בשלמותם בלוקים של 732–864 מילים
(35.0807, 31.0513); הרחבת הסעיפים מצילה בלוקים ארוכים באמת (פ״מ 35.0402 — 14
סעיפים, 61.0104 — 34 סעיפים) שהכלל שלהם יושב במקום 7–12 לפי כותרות.

**ההערכה החינמית הרחבה יותר** (בלי sectprobe, סקריפט על הבלוקים והבוררות): 47 מ-82
היעדים יושבים על בלוק מעל 600 מילים שמכיל את הכלל. סעיף-הכלל נמצא בטופ-6 לפי
כותרות ב-19 מהם, בטופ-12 ב-32. בחירה לפי כותרת+טקסט (חפיפת-מילים פשוטה) לא
משפרת: 20 ו-31. ⇒ **חיתוך „חכם" זול אינו הפתרון; מה שמציל הוא להגיש יותר
סעיפים, וזה עולה מילים.** ההצלה בפועל קטנה מ-47 כי רוב הפקודות האלה אינן
מגיעות ל-6 הבלוקים (בעיית המנוע, לא החיתוך).

**ההמלצה.** הרווח קטן והמחיר לשאלה כבר +60% מאז הפריסה. אם משנים: `RETRIEVE_V2_TOP_K=12`
בלבד (+2 סעיפים, +12% מילים, אפס בסגנון-אמיתי). 1200/12 קונה את כל הארבעה ב-+42%
מילים — לא שווה לפני שהמנוע מטופל. שני הכפתורים הם משתני-סביבה, בלי קוד.

(מבחינת הקוד: `tests/test_doc_blocks.py` — שתי שורות ריקות שהמיזוג בלע הוחזרו.)


---

## ⚡ 17.09 (ג) — מטמון-שעה לפרומפט-המערכת נפרס (גרסה 146); ומה המחיר האמיתי של שיחה ארוכה

**המחיר, מדוד מיומן-המדדים של המכונה (17.09):** שאלה עם שני מעברים $0.25 (קלט
31,244 = ההקשר פעמיים, כתיבת-מטמון 5,316 = פרומפט-המערכת, פלט 2,355); שאלה עם
מעבר אחד, שנייה בשיחה, $0.20 (כתיבת-מטמון 16,734 = ההיסטוריה). הקלט הוא כ-75%
מהמחיר; הפלט קטן מאז ANSWER_V2.

**ריזוי האחזור לא משתלם (נמדד חינם, `night/out/sect4_lean_*.json`):** בלי
הבלוק המלא של המובילה — 25 סעיפים (−2, rs005) ב-2,760 מילים (−7%); 4 בלוקים
במקום 6 עם 12 סעיפים — 24 (−3, גם rs060) ב-2,202 (−26%). האחזור כבר רזה למה
שהוא מביא; הידיות הן המודל, המטמון, ואורך השיחה.

**מה נפרס:** `SYSTEM_CACHE_TTL` (backend, כבוי בקוד; `tests/test_cache_ttl.py`, 5
בדיקות) = "1h" ב-fly.toml. פרומפט-המערכת נכתב למטמון פעם בשעה (פי 2) ונקרא
בעשירית; ההיסטוריה נשארת 5 דקות (TTL ארוך חייב להקדים קצר). לפי התיעוד: משתלם
כששאלות מגיעות במרווחים של 5–60 דקות, שקול בתנועה צפופה, ועולה כ-$0.02 יותר
לשאלה במרווחים ארוכים משעה. יומן-המדדים מתאפס בכל פריסה, אז אין היסטוריית-
תנועה להכריע לפיה — הימור הפיך בשורה. אומת חי: שאלת-עשן („מותר לי לענוד
שרשרת עם מדים?") נענתה מלא מ-33-05-01, ה-API קיבל את הפרמטר.

**⚠ הממצא מאותה שאלה — שיחה ארוכה היא הבור:** $0.31 לשאלה השלישית בשיחה, כי
ההיסטוריה (שתי שאלות קודמות עם ההקשר שלהן, ~32K טוקנים) נכתבה למטמון אחרי
אתחול המכונה. `_HISTORY_MAX=12` מחזיר עד 6 חילופים, וכל חילוף נושא עכשיו ~11K
טוקנים של הקשר (היה ~5K) ⇒ תור שישי בשיחה יכול לשלוח 60K+ טוקנים: $0.03 אם
בתוך 5 דקות מהתור הקודם, **$0.40 אם לא**. הבלוקים הכפילו את זה. הידית הבאה
בסדר-העדיפות, לפני המודל: לקצר את ההיסטוריה המוחזרת (חילופים 6→3), או להחזיר
בהיסטוריה את השאלה והתשובה בלי הקטעים אחרי החילוף הראשון — שינוי-קוד קטן,
נמדד על eval.py (--facts, ~$2) ולא על סרגל-האחזור.

**המשך (הכרעות שלך):** (1) קיצור-היסטוריה; (2) זרוע Sonnet 5 (−60%, ~$1.5–3,
דורש תקרה); (3) effort=medium (FACTS ~$2). ליג'ר $133.18/$135.


---

## ⚡ 17.09 (ד) — ההיסטוריה המוחזרת קוצרה ונפרסה (גרסה 147): תור רביעי בשיחה $0.31 → $0.16, ההמשך עדיין מבין

**מה השתנה** (`backend._replay_history`, `tests/test_history_replay.py`, 7 בדיקות;
חבילה מלאה 56/56 על הענף): כל תור-משתמש בהיסטוריה חוץ מהאחרון מוחזר למודל
כשאלה בלבד — הקטעים נחתכים בכותרת `_CONTEXT_HEADER` ש-`_compose_user_content`
כתב אותם תחתיה; החילוף האחרון נשאר שלם (שאלת-המשך בדרך כלל מצביעה עליו);
תשובות ללא שינוי; החיתוך בחילופים שלמים ושומר-הפתיחה-במשתמש נשארו. תור „חשוף"
אינו משתנה בין בקשות, אז הקידומת המטמונה מעל ההיסטוריה הישנה יציבה.

**המדידה החיה, אותה שיחה (ארבעה חילופים, אחרי אתחול-מכונה בשני המקרים):**

| | תור שלישי, לפני | תור רביעי, אחרי |
|---|---|---|
| קלט + כתיבת-מטמון | 12,731 + 37,553 | 12,071 + 14,167 |
| קריאת-מטמון | 0 | 5,316 (פרומפט-המערכת ממטמון-השעה, גם אחרי אתחול) |
| מחיר שהאפליקציה רשמה | **$0.31** | **$0.16** |
| התשובה | | „ומה לגבי עגילים?" נפתר נכון מהשרשרת שלפניו, 33-05-01 סעיפים 106–109, מלא |

**מה זה גובה:** שאלת-המשך שמתייחסת לחילוף שלפני האחרון ודורשת את הקטעים
שלו — המודל רואה את התשובה הקודמת עם הציטוט ואת האחזור החדש, לא את הקטע
עצמו. שאלות בודדות והמשך-על-האחרון לא נפגעים. רווח-לוואי: פחות רעש מקטעים
ישנים. **לא נמדד:** eval.py --facts (~$2) — הליג'ר $1.82; הפריסה על סמך
בדיקות-היחידה, בדיקות-החיווט והאימות החי.

**מצב המחיר אחרי 17.09:** שאלה בודדת $0.20–0.25 (שני מעברים) / ~$0.10 (מעבר
אחד בלי היסטוריה); כל תור בשיחה חסום עכשיו בכ-$0.16–0.25 במקום לטפס. הידית
הבאה שמשנה סדר-גודל: Sonnet 5 (−60%), זרוע ~$1.5–3, דורש תקרה.


---

## ⚡⚡ 17.09 (ה) — זרוע Sonnet 5 נמדדה: אחרי עיון −3 תשובות מלאות (35→32), 5 אבודות מול 2 רווחים — **לא עוברת**; ושלוש תשובות בטוחות-בעצמן על שאלה אחרת

**הזרוע** (`probe_sonnet6.jsonl`, `grades_grade-sonnet6.jsonl`,
`review_grade-sonnet6.json`; תקרת-הליג'ר 135→145 באישור המשתמש, `ANSWER_MODEL`
כמשתנה-מדידה ב-backend, `tests/test_answer_model.py`): התצורה הפרוסה (blocks6) עם
`ANSWER_MODEL=claude-sonnet-5`, מול blocks6 (Opus 4.8). 63 נענו מחדש, 9 הועברו.
ניסיון ראשון נעצר לפני הוצאה (מנגנון-ההגנה של probe: הנתב נפל על APITimeoutError);
השני רץ. הבאץ' חזר תוך דקות. **$1.54 + $0.11 דירוג; ליג'ר $134.79/$145, נותרו $10.21.**

| | Opus blocks6 | Sonnet 5 |
|---|---|---|
| תשובות מלאות, כפי שדורג | 29/72 | **34/72** |
| **תשובות מלאות, אחרי עיון** | **35/72** | **32/72** |
| חלקים, אחרי עיון (strict) | 47/97 (48%) | 45/97 (46%) |
| served, אחרי עיון | 61/97 (63%) | 57/97 (59%) |
| סירובים | 31 | 31 |
| הקשר, חציון מילים | 3,820 | 3,833 |

**כפי שדורג Sonnet נראה טוב יותר (+8/−5). אחרי עיון: +2/−5, נטו −3.** הקריטריון
(עד 2 אבודות) — **לא עבר.** 9 ההכרעות ב-`review_grade-sonnet6.json`; לראשונה העיון
גם **הוריד** ציונים, כי המדרג זיכה תשובות שעונות בביטחון על שאלה אחרת:

- **rs009** קופת הפלוגה — השאלה על ביטחון כסף פרטי; התשובה על מועד תשלומים
  מקופת היחידה. הבוררות: הכלל הוא שהקופה אינה לכספים פרטיים (35.0107 §2, 4).
- **rs038** „כמה זמן לפני הכניסה" — הבוררות NO_SUCH_RULE (נקבע ביחידה); התשובה
  פירשה כמרווח בין שמ״פים (הק״א 31-08-01).
- **rs053** להוציא ילד מבית-ספר — הבוררות NO_SUCH_RULE (אזרחי); התשובה הפכה
  אותה לחופשה מיוחדת (35.0402).
Opus סירב נכון בשלושתן. **זה מצב-הכשל המסוכן של המוצר** — חייל שמקבל תשובה
בטוחה עם ציטוט על שאלה שלא שאל — ואין לו ביטוי במדד המכני.

**ההפסדים האמיתיים:** rs000 (33.0115 לא היה בחלון — שונות-אחזור), rs023 (אותו
סעיף בחלון, Sonnet סירב להחיל), rs019 (ענה על תעודת-פטור במקום מענק-שחרור),
rs054 (לינת בית), rs002 (גבולי, ניסוח-סירוב על אותו ציטוט). **הרווחים:** rs021
(פנייה למפקד מיד, 2/2), rs063 (טלפון בזמן אישי, הועלה ל-2/2 בעיון — תואם את
הבוררות). rs071: המדרג נתן 1/2 על אותה מהות ש-Opus קיבל עליה 0 — יושר ל-0.

**קריאה:** נטו −3 יושב בתוך רצפת-הרעש (±3), אבל ההתפלגות לא: 5 אבודות מול 2,
ושלוש „תשובות" על שאלה אחרת. Sonnet עונה יותר ומסרב פחות — ובמוצר שבו הסירוב
הוא הבטיחות, זה כיוון לא נכון בלי פרומפט שכויל אליו. **ההמלצה: להישאר על
Opus.** מה שעוד לא נמדד ויכול לשנות: Sonnet עם כלל-סירוב מחמיר יותר (זרוע נוספת
~$1.7), או Sonnet רק במעבר הראשון ו-Opus בשני.

**המחיר שנמדד לזרוע:** $1.54 ל-63 תשובות בבאץ' מול $3.41 ב-Opus — 45%, כפי
שהמחירון מבטיח. בייצור זה היה $0.25→~$0.11 לשאלה; ההפרש נותר על השולחן.


---

## ⚡⚡ 18.09 — מדרג-העל: התקרה נמדדה לפני הבנייה, ושלוש גרסאות נמדדו — **אף אחת לא עוברת.** צד-האחזור קרוב לתקרתו בסגנון-אמיתי

**הקריטריון שנקבע מראש** (לפני המדידה): מול התצורה הפרוסה עם הנתב האמיתי —
+8 סעיפים לפחות (כלל-תוכן, 82 יעדים), +2 בסגנון-אמיתי, אפס אבודות, עד +900 מילים.

**1. התקרה של מדרג-על על הדירוג (חינם, `night/out/rerank_ceiling.json`).** מיקום
הפקודה שעונה בין הפקודות ה**נבדלות** בדירוג עמוק, ליעדים שהחלון הפרוס אינו
מגיש ושהבלוק שלהם נושא את הכלל:

| מיקום | יעדים | מהם סגנון-אמיתי |
|---|---|---|
| 1–6 (חיתוך-הבלוק) | 5 | 1 |
| 7–25 | 18 | **1** |
| 26–40 | 7 | 2 |
| מעל 40 או נעדרת | 20 | 4 |

⇒ מדרג-על על 25 הראשונים יכול להוסיף לכל היותר **סעיף אחד** בסגנון-אמיתי. לא
נבנה. (תיקון לניסוח קודם: 20 היעדים „לא מדורגת" אינם בהישג-ידו של שום מדרג-על
— הם אינם בבריכה שלו.)

**2. הנתב כמדרג-על — נבחן בתשלום (`routeprobe_plain.json`, $0.68):** הנתב קורא את
כל הכותרות, אז מיקום אינו מחסום. הוא נוקב בפקודה שעונה ב-34/82 (בחירה ראשונה
23; סגנון-אמיתי 9/20). `RETRIEVE_ROUTER_BLOCKS` (בנוי, כבוי, 7 בדיקות): הבלוקים
של הפקודות שהנתב נקב מצטרפים כהרחבה. נמדד חינם בהשמעה-חוזרת של בחירות-הנתב
(`sect5_*.json`):

| תצורה | סעיף לפי תוכן | סגנון-אמיתי | מילים, חציון |
|---|---|---|---|
| **פרוס + הנתב האמיתי** | **35** | **11** | 3,121 |
| + בלוקי-נתב | 38 (+3/−0) | 12 | 3,721 |
| נתב מאוחד (רגיל ∪ כותרות-סעיפים), בלי בלוקים | 34 (+0/−1) | 11 | 3,096 |
| נתב מאוחד + בלוקי-נתב | 41 (+6/−0) | 12 | 4,078 |

⚠ **הפרודקשן טוב ממה שדווח:** עם הנתב בלולאה התצורה הפרוסה מגישה **35/82**
ולא 27 — בונוס-הנתב (+0.05) מרים את הפקודות שנקב אל ששת הבלוקים. המכשיר
החינמי (route=∅) מודד רצפה, לא את הייצור.

**3. נתב שקורא כותרות-סעיפים (`routeprobe_clause.json`, $0.75 עם מטמון-פרומפט —
4.9M טוקני-קריאה):** גרוע מהנתב הרגיל — 28/82 מול 34 (סגנון-אמיתי 7 מול 9);
הרוויח 10 יעדים ואיבד 16. 15–23 אלף מילים של כותרות מדללות את Haiku. האיחוד
מגיע ל-44 בחירות נכונות, אבל עולה קריאת Haiku נוספת של ~50K טוקנים לשאלה
ועוד ~950 מילים בחלון, ומזיז את הסגנון-האמיתי ב-1.

**ההכרעה:** שום גרסה לא עומדת בקריטריון; הסגנון-האמיתי זז ב-+1 (rs000) בכולן.
אין זרוע. `RETRIEVE_ROUTER_BLOCKS` נשאר בקוד, כבוי, מתועד. הוצאה: $1.43;
ליג'ר **$136.21/$145, נותרו $8.79.**

**מה זה אומר:** על הקורפוס הנוכחי, צד-האחזור מיצה את הסגנון-האמיתי — 11–12 מ-20
היעדים מוגשים, והנותרים אינם מדורגים ואינם ננקבים בשום נתב (rs007, rs010, rs014,
rs018, rs059, rs070…) או שהבלוק אינו נושא את הכלל (rs024) או נחתך (rs054). ⇒
**המנוף הבא הוא תוכן, לא אחזור:** עוגנים בשפת-חייל לפקודות של היעדים שלא
מדורגים (המנגנון קיים — `night/anchors.py` עם השערים; שלב 4 הביא כך את rs048),
אצירת הסעיפים החסרים, ומסמכי ה-FOI (29.09). כולם נמדדים חינם ב-sectprobe.


---

## ⚡ 18.09 (ב) — עוגנים בשפת-חייל לפקודות שלא מוגשות: 68 עוגנים, +2 סעיפים, אפס רגרסיות — והלקח: עוגן עובד רק כשהוא פוגע בניסוח

**רשימת-העבודה (`night/out/anchor_worklist_1809.json`):** 42 יעדים שהפרודקשן-עם-נתב
אינו מגיש אף שהבלוק נושא את הכלל — 21 פקודות, 33 סעיפים (הגדולה: פ״מ 35.0402,
9 יעדים). **78 עוגנים נכתבו ביד מתוכן הסעיף, לא מהשאלה שבסרגל**
(`anchors_manual_1809.json`), עברו את `night.anchors.check` ועוד מבחן-מראה מחמיר
(Jaccard < 0.40 מול 1,114 שאלות שמורות — סרגלים, פיילוטים, שער); 77 נכנסו כ-
`anchor_questions` (אחזור בלבד, לא מוצגים). חינם.

| | לפני | אחרי 77 | אחרי הסרת 35.0402 (68) |
|---|---|---|---|
| סעיפים, עם הנתב האמיתי (82) | 35 | 37 | **37** (+2/−0: q00014, rs012) |
| סגנון-אמיתי (20) | 11 | 12 | **12** |
| סעיפים, המכשיר הרשמי | 27 | 29 | 29 |
| שער-האחזור (431) | 388 | 387 ❌ | **388** |

הרגרסיה היחידה: שאלת חופשות-קבע (36.0401) איבדה את פקודתה ל-35.0402 שקיבלה 9
עוגנים — ואותם 9 לא הביאו אף יעד ⇒ הוסרו. **68 עוגנים ב-20 פקודות נשארו.**

**למה רק שניים — נמדד:** מיקום הפקודה בדירוג העמוק זז ב-8 מ-42 ולא זז ב-32.
כשעוגן פוגע בניסוח-השואל הקפיצה חדה (28→1, 7→1, 65→16, 86→36, 59→27); כשלא —
אפס. עוגן שנכתב מתוכן הסעיף ולא מהשאלה אינו מכליל; זה אישור שלישי לממצא של
08.2026 („הבעיה תחרות גלובלית, לא כמות ניסוחים"). ⇒ **עוגנים רחבים אינם המנוף.
המנוף הוא ניסוחים אמיתיים — יומן-השאלות של הפיילוט.**

**ממצא-לוואי — דליפת תחום-התפקיד:** ב-4 יעדים (q00035, q00186, q00201, rs007)
הפקודה שעונה אינה בדירוג כלל (pos=None): היא מחוץ ל-`_docs_for_role` של השואל
(למשל 35.0206 מתויגת reserve/commander והשאלה נשאלה כ-soldier). שום עוגן או
נתב לא יגיע לשם — זו שאלת תיוג-תפקידים, פתוחה.

⚠ `storage/embedding_cache.npz` נכתב מחדש (68 מחרוזות-עוגן חדשות) — קובץ בינארי;
סשן מקביל שקולט פקודות יתנגש בו. לא נפרס.
