# מצב הפרויקט — 17.09.2026, אחרי הפריסה

עמוד אחד. היומן המלא: `HANDOFF.md` (סעיפים 16.09 ו-17.09 בסופו). התוכנית: `night/PLAN_ROUND4.md`.

## ▶ להתחיל מכאן (נכתב 18.09 בסוף סשן מלא — הסשן הבא ממשיך מפה)

### ▶▶ 23.09 לילה — סשן ב' נסגר; הצעד הבא (הבלוק העדכני, גובר על מה שמתחתיו)

**אין הוצאה ואין פריסה בסגירה.** main = `17a1609`+ (הענף `claude/answer-levers`, כולו ב-main). סשן א' ממזג את הקורפוס
המאוצר-מחדש (`claude/paid-run`, recur8: שער 392, סרגל 30/36, dev 77, held 75, נתב 37·12) ל-main ומודיע „main = <sha>".

1. ✅ **25.09 — מדידת-החבילה רצה על main `67b4d5d` ועברה** (`night/QUOTELESS_CRITERION.md`, סעיף „מדידת-החבילה"): סרגל
   30/36, dev 77, held 75→76 (= hW3a), שער 392 (0 נופלות חדשות), נתב 0 אבודים, c 24, הסט הנעול 10, hW3a בחלון, real24
   0 שינויי-מוביל, מילים +0.2% — **אפס אבודים בכל מכשיר.** TERM_NOTE צד-תשובה, לא נמדד כאן (נמדד בתשלום 23.09).
   ⇒ **v157 מוכנה לפריסה: main = הקורפוס המאוצר-מחדש + fly.toml עם שלושת הדגלים.** הפריסה בידי המשתמש/המנהל
   (המסנן של סשן ב' דוחה deploy); אחריה סשן א' שואל שתי שאלות חיות (המזוהמות) לאימות ולמחיר.
2. **K=7 — הושלם 25.09, נכשל בתנאי-המילים בלבד:** מחזיר את שתי השורות שנדחקו (rs060 בסרגל 29→30, held 75→77) ועוד
   3 dev, **אפס אבודים בכל מכשיר** (שער 392→392, נתב 36→37, c 24, הסט הנעול 10, real24 0 שינויי-מוביל) — במחיר
   **+13.4% מילים בסרגל, +12.0% ב-head-100** (תקרה +12%; ≈ +$0.005 לתשובה). הכרעת המשתמש: K=7 + שתי הפקודות ב-v158,
   או K=6 והן בחוץ עד גל ה-FOI (`night/K7_CRITERION.md`).
3. **OCR + כלל-הקליטה לפקודות קצרות (25.09, `361abaf`):** `INGEST_SHORT_ORDERS` (כבוי, בייט-לבייט זהה) בשער הטקסט
   הדליל של `ingestion/pdf_to_json.gate_text` — מקבל קובץ דליל רק אם ≤ 2 עמודים, ≥ 80% אותיות עבריות ובלי „הפקודה
   בוטלה"; ריצה יבשה על 11 הקבצים ב-`_unextractable`: **9 מתקבלים** (כל החד-עמודיים האמיתיים + 58.0306), 32.0104
   (סטאב-ביטול) ו-30110 (5 עמודים, כפל של 3.0110) נדחים; `tests/test_short_orders.py` 4/4, סוויטה 79/79. **מה נשאר
   להכרעה: הקליטה עצמה** — להעביר את 9 הקבצים ל-`pdf-ldf_law/` ולהריץ `night.intake` עם `INGEST_SHORT_ORDERS=1`
   (קריאת-מודל לקובץ + אצירה, ≈ $1, ורק אחרי v157 ובגל אחד עם ה-FOI). לא בפרודקשן: הדגל נשאר כבוי ב-fly.toml.
   OCR של ה-FOI: 12/14 עוברים את השער (`D:/app_soldier/sources_pending/ocr/REPORT.md`).
4. **הורדות wave5a: 0/18** — אינקפסולה גם עם כותרות דפדפן; ידני בדפדפן של המשתמש (קבצי /media/ פתוחים כשידועה כתובת).
4ב. **26.09 — הכנת גל-הקליטה המשולב מוכנה, בלי הוצאה** (`night/wave8/README.md`, `night/wave8/defs/`): 9 הגדרות-אצירה
   לפקודות הקצרות (20 סעיפים, כל מספר מתמונת-העמוד) + 2 טיוטות FOI (תעודת-לוחם 30-07-01 — 9 סעיפים, „בעדכון"; קרפ"ר
   701.001 — 3 סעיפים, ספר הפרופילים עצמו נדחה במענה) + מבנה-קליטה ל-14 מסמכי ה-FOI (סוג, מסביר/פקודה, עמודי-לוואי,
   פיצול 3 קבצים ל-6 הוראות, „בעדכון"; 3 כבר בקורפוס, המפרט לא לקלוט). **עלות הגל ≈ $2.1** (≈ $0.7 אם כל הבלוקים
   ידניים). ממתין להכרעת המשתמש: מה לקלוט ומתי.
4ג. **26.09 — מסלול הקליטה-מטקסט קיים** (`ingestion/text_ingest.py`, `INGEST_FROM_TEXT` כבוי, בייט-לבייט זהה כשכבוי —
   שום דבר במסלול-העלייה קורא אותו; 5 בדיקות): ריצה יבשה על 11 ההגדרות — 11/11 עוברות שער-אינטייק ו-curate.check, 7/11 נקיות
   בשער-המספרים, 4 עם שאריות-OCR בספרות לתיקון-עין לפני ההחלה (README של wave8). הכתיבה לקורפוס — בעץ סשן א' אחרי ההכרעה.
4ה. **26.09 (ערב) — wave-8 נסגר בביקורת-הצלבה, ו-CONTINUE נגנז.** 15 ההגדרות של wave-8 כתבתי אני; סשן א' קרא אותן מול העמוד, אני
   בדקתי את שינוייו ואת מה שנכתב: 302.006, 32-02-10, 32-02-27, 30-07-01 בקורפוס = ההגדרות, שערים נקיים; 300.001 מוחזק בחוץ (כלל-
   העצירה) (`night/recurate/REVIEW_26.09_wave8.md`). fly.toml v158: `RETRIEVE_DOC_BLOCKS="7"` (`6d524b4`, לא נפרס). **CONTINUE:**
   תומחר על v158 — נקודת-איזון p=0.31, ב-head-100 −4.4% ⇒ **נגנז עד תעבורת-פיילוט** (הכרעת המנהל); `night/probe.py` רושם עכשיו
   `sent_user_content` ו-`route` כדי שהמבחן הסופי ישמש זרע בעתיד בלי מעבר ראשון נוסף.
4ו. **26.09 — v158 נפרס** (main `4890d99`, מכונה 48e4659f039578 גרסה 158, 13:49Z; המנהל): K=7 + wave-8 (302.006, 32-02-10/27,
   30-07-01, 701.001, 9 הקצרות). **ממצא לגל הבא (עשן חינמי של המנהל על v158, בלי נתב ו-HyDE):** KARPAR-302.006 במקום 1 ל„נפצעתי
   במילואים, מי מטפל בי ועד מתי?" ול„חליתי באמצע המילואים", **אבל לא בחלון** ל„…לא רוצה להגיש תביעה למשרד הביטחון" ול„חייל מילואים
   נפגע במהלך השמ"פ" — פער-ניסוח: הבלוק כותב „משהב"ט" / „אגף השיקום" ו„שמ"פ" כראשי-תיבות, החייל כותב „משרד הביטחון" ו„במילואים".
   המנוף הזול: ערכי-מילון (משרד הביטחון ↔ משהב"ט, שירות מילואים פעיל ↔ שמ"פ) ועוגני-ניסוח מהשאלות האלה — חינם, במדידה מזווגת.
   **ב-main, לא נפרס:** תור-הגיליון METRICS_OUTBOX + משפט-המדיניות (`d6bf7b2`) ו-fly.toml `[mounts]` ב-/data (`a7d81ec`) — לפריסת
   v159 **רק אחרי** `fly volumes create cai_data --size 1 --region fra -a commandai` (בלי ה-volume הפריסה נכשלת על ה-mount;
   `night/PILOT_LOGGING.md` סעיף 6). בדיקה יומית קריאה-בלבד: `python -m night.outbox_watch`.
4ד. **26.09 — פגם-דלת תוקן:** „איך מגישים בקשה לתשמש?" נתפס ב-`lone_soldier_aid` (נימוק מענקי-מזון) כי ראשי-התיבות תשמ"ש
   ישבו שם; עברו ל-`family_support_pay` (עם ובלי גרשיים, עם תחיליות; הפועל „תשמשו" לא נתפס), הנימוק עונה לפער שהוצהר
   („מי מאשר ותוך כמה זמן — לא בפקודות; המסלול 35.0210, טופס 60, רכז הת"ש"). doorgate PASS/PASS (0 תפיסות על 295 מלאות),
   אף דלת לא אבדה (q00005 → lone_soldier_aid, rs043 → family_support_pay); `tests/test_door_family_payments.py`. הענף
   `claude/doors` (2bb4115) הוא אב-קדמון של main — התיקון בענף שלי, כמו rs041.
4ז. **26.09 — כיסוי order-watch** (`ORDER_WATCH_COVERAGE.md`): הבייסליין מ-22.07 עקב רק אחרי 77 מתוך 297 המסמכים,
   ו-214 לא היו מכוסים בכלל. עכשיו: 209 ב-`pending`, 5 כינויים (4 מפתחות הפוכים + 33.0209 כפולה), 5 רשומות ידניות
   (ועוד 6 מראש ל-FOI של גל-8). `_watch_enroll.py` רושם פקודה רק כשה-PDF בפורטל זהה בבייטים לעותק שנקלט, ו-
   `tests/test_order_watch_coverage.py` נועל את הכיסוי. ✅ אחרי המיזוג ל-main רץ `_watch_enroll.py --plan` (בלי רשת) על 9
   הקצרות של גל-8: pending 209 → 218, לא מכוסים 0, הבדיקה 14/14. **ממתין למשתמש:** `--enroll` (מקומית, או
   order-watch-enroll אחרי מיזוג). issue #4 (2.0101, 21.0113) לא נקלט מחדש, והמשמעות: קובץ צורף לעמוד שלא היה בו
   קובץ, לא בהכרח נוסח חדש. ההוראות בסעיף 5 של המסמך.
5. **המבחן הסופי על הסרגל** — `night/FINAL_RULER_CRITERION.md`, ≈ **$8.5–9 בבאץ'** (לרענן בחלונות העץ המאוחד), לא הורץ;
   מהעץ עם .env, באישור המשתמש. **לא לפני** package_measure ירוק.
6. ממתין: זרוע CONTINUE ($3.5); 9 המספרים-המועמדים בבלוקים הישנים (סשן א' מתכנן גל ניקוי); הביקורת של 7 חסרות-הבלוק
   מול תמונות-העמוד טרם נעשתה (5 בקורפוס, 2 בחוץ) — כשסשן א' יבקש.

**🧭 23.09 — סשן א' („הראיה בתשלום ומה שהיא פותחת", ענף `claude/paid-run`, עץ `D:/_run_wt` עם `.env`).
הבלוק הזה הוא העדכני לקורפוס; „🧭 22.09 סשן ב'" שמתחתיו — המסלול המקביל.** היומן: `night/head100/RUN_LOG.md`
סעיפים 0–7; `night/recurate/README.md` „מצב סופי של הסבב".
- **הקורפוס (recur8 = העץ הממוזג, מול הרשומה `w6cap`):** שער 390→**392** (0 נופלות חדשות), סרגל **30/82 + 36/82**,
  head-100 dev 76→**77**, held 72→**75** (בלי 2 המזוהמות 71→74), נתב **37 · 12/20** — **אפס אבודים בכל מכשיר**;
  חבילה 78/78 על העץ הממוזג הראשון (`548c5c8`); על `4c58491` (main+fly.toml של v157) — ראו RUN_LOG 7.
- **מה בפנים:** 7 אצירות-מחדש עם מספרים (PM-33.0352, 35.0221, 35.0201, 31.0606, 33.0336, 32.0402, 32.0502),
  3 בלוקים ישנים תוקנו מול תמונות-העמוד (31.0507, 33.0144, 35.0233 — 7 מספרים), 5 בלוקים חדשים (3.0502, 31.0252,
  PM-33.0342 nodigits; 36.0301, 36.0313). ⚠ בלוק „עם מספרים" מלפני 18.09 נכתב משכבת-טקסט משובשת — לאמת בעין.
- **מה בחוץ (2 מ-7):** **PM-33.0109, 33.1010** — `sections=[]` + `held_back_23.09`; כל אחד דחק פקודה עונה (rs060 /
  שורת-held) — תחרות גלובלית, כלל אפס-אבודים. **בהמתנה ל-K=7 של סשן ב'** (`night/K7_CRITERION.md`, עץ קריאה-בלבד
  `6112fd8`); החזרה: `python -m night.recurate.apply_defs night/wave7/defs --only <id> --write` + ארבעת המכשירים.
  עוגני-הניסוחים (73) — בחוץ בהכרעת המשתמש.
- **המזהה:** `35.0502` → **`32.0502`** (אפשרות א, הכרעת המשתמש) — המסמך, `defs/32.0502.json` וכל המצביעים החיים;
  רשומות-העבר (night/out, ביקורות, RUN_LOG) עם המזהה הישן בכוונה.
- **ANSWER_TERM_NOTE:** נמדד בתשלום ($1.60, 14 × 2 זרועות, `TERM_NOTE_CRITERION.md`): במקום שהשורה נדלקה — תיקנה
  (hR1db הת"ש → שעת ט"ש), אפס הרעות ב-13; hW3a לא („תשמש" בלי גרשיים אינו מדליק) ⇒ כלשונו לא עבר; **הכרעת
  המשתמש: נדלק ב-v157 יחד עם HOMONYMS + QUOTELESS** (fly.toml `17a1609`, לא נפרס). `test_router_slots` — בדיקת-fixture
  במקום pass ריק.
- **הבא — אחרי פריסת v157 (המנהל):** שתי שאלות חיות דרך האפליקציה על שני המקרים המזוהמים („איך מגישים בקשה לתשמש?",
  „המפקד מבטל לנו את שעת הת"ש"), ~$0.25 — **רק באישור המשתמש בצ'אט של סשן א'** (סימון תיבת-התנאים + ההוצאה);
  לקרוא שורות `cost_usd` מהמכונה מיד אחרי; לדווח: המובן שנבחר, הפקודה שצוטטה, המחיר.
- **ליג'ר $142.18 / 195.** `.env` קיים רק ב-`D:/_run_wt` וב-`D:/app_soldier`. main מתקדם ff-only מ-`D:/_deploy_wt_blocks6`
  (המקום היחיד ש-main מוצא בו). ⛔ לא נדחף ל-origin, אין פריסה מהסשן הזה, אין הוצאה חדשה בלי שורת-אישור.

**🧭 22.09 — סשן ב' („מנופי-תשובה חינמיים", ענף `claude/answer-levers`, עץ `D:/_levers_wt` בלי `.env`).
הבלוק הזה הוא העדכני; „🧭 19.09 ערב" שמתחתיו — היסטוריה.** היומן: `HANDOFF.md`, סעיף 22.09 (ב').
**⚠ פרודקשן = v155, לא v154:** `fly releases` — v154 ב-19.09 20:45Z ו-v155 ב-21:14Z, שתיהן מאותו עץ
`5dcbff7` (reflog של `D:/_deploy_wt_blocks6`: העץ ישב על 5dcbff7 מ-23:37 עד 00:16 מקומי). **v156 טרם
יצאה:** main = `8f86e1d` (הדלתות + rs041 + תיקון-אינווריאנט לצ'יפ + דגל כבוי + phrasing_c), חבילה **74/74**
על עץ-הפריסה, LF בקבצי-האפליקציה — אבל מסנן-ההרשאות של הסשן דחה `flyctl deploy` („Production Deploy").
**המשתמש מריץ:** `flyctl deploy --remote-only -a commandai` מ-`D:/_deploy_wt_blocks6` (לפני: `flyctl releases`,
לאחר: `flyctl status`, עשן-אחזור חינמי). origin/main = `cdcce15` (מיזוג-המסמכים נדחף); 8f86e1d וההמשך —
נדחפים אחרי מיזוג הענף. ⛔ אל תכלול בפריסה שינויי-קורפוס לא-נמדדים (אין כאלה ב-main).
**מה נעשה, במספרים (כולם חינם, מזווג, קריטריון בכתב לפני):**
1. **rs041 → פרדיקט אחד `out_of_scope.declares_gap`** (סימן | משפט-סירוב בראש | פתיחה „אין בפקודות…")
   שהצ'יפ, הרצועה ו-`report_goal.door` קוראים: `grade-blocks6` אחרי עיון **served 65→66**, strict 47,
   goal 76→77, דלתות 15/8/0; `night.doorgate` — משפחות 0/388, **שער-סימן חדש** 0 מ-241 תשובות מלאות
   בלי סימן. הפרומפט לא נגע. (`tests/test_gap_sign*.py`.)
2. **בחירת-הסעיף בתוך הבלוק** — `RETRIEVE_BLOCK_CLAUSE_EMBED` (RRF כותרות⊕הטמעת-גוף / הטמעה בלבד):
   תקרה נמדדה לפני (סרגל 6, head-100 1+1, נתב 13); תוצאה: סרגל 30→31 (**q00267 אבוד**, q00152+rs054
   נוספו), head-100 +1/+1, שער 390, נתב 37→38, **מילים +7–10%** ⇒ **נכשל, הדגל נשאר 0, לא בונים שוב על
   minilm** (`night/BLOCK_CLAUSE_CRITERION.md`).
3. **דלת שגויה hRes1a** (ריצה בתשלום): `civilian_body` לא נדלקת על „מי משלם / הצבא או ביטוח לאומי";
   doorgate 0/431 כולל שורות head100A.
4. **עוגנים-מניסוחים** (ניסוי הלקח של 18.09; c נעול `5f81378`, 73 עוגנים ב-23 פקודות, מניסוח-dev +
   הסעיף בלבד): הוחלו ונמדדו — **c 24→28, סרגל 30→32, held 72→72, שער 390→393, נתב 37→37, real24 ללא
   שינוי-מוביל, אפס אבודים בכל מכשיר — ונכשלו באחד בשני תנאי-הרווח (+4 מול +5, +2 מול +3) ⇒ הוחזרו.**
   ⏳ **הכרעת המשתמש:** להכניס כחריג מתועד? `python -m night.wave7.apply_anchors --write` (דקה) + השערים.
   (`night/ANCHORS_PHRASING_CRITERION.md`; ⚠ dev/held ב-targets.json מחולק לפי *ניסוח*, לא נושא.)
5. **מחלקת ההומונימים** (הכשל של הריצה בתשלום — ראשי-תיבות דו-משמעיים): `storage/glossary.HOMONYMS`
   (ת"ש, יום ב', מ"מ, שליש, תב"ן; תשמ"ש, קל"ב, ש"ג) + `RETRIEVE_HOMONYMS` (אחזור) + `ANSWER_TERM_NOTE`
   (שורת „הבהרת מונחים" בתור-המשתמש, בלי לגעת בפרומפט) — **כבויים.** הסט הנעול (`7282ee0`, 12):
   **10→10 (+0; בלי 4 המונחים הדלופים 7→7)**, סרגל/head-100 ללא שינוי ⇒ **נכשל (נדרש +2), נשאר 0.**
   שתי ההחמצות: ולת"ם (הפקודה במקום 9, הפקודות עצמן כותבות ולת"ם — הרחבה לא תעזור) ו-ש"ג בהוראת
   „שומר בקניון" (מובן אזרחי שלישי — 33.0115 — לא בטבלה; הוספתו עכשיו = כוונון על הסט הנעול).
   `ANSWER_TERM_NOTE` — צד-התשובה, לא נמדד: בדיקה בתשלום ~$1 באישור המשתמש (`night/HOMONYMS_CRITERION.md`).
6. **`RETRIEVE_SECOND_PASS_CONTINUE`** (PLAN_COST ידית 1) — בנוי כבוי, 6 בדיקות בלי API, קריטריון
   (`night/SECOND_PASS_CONTINUE_CRITERION.md`); **הזרוע (~$3.5) ממתינה להחלטת המשתמש.**
7. **wave-7:** טיוטות בלוקים ל-7 חסרות-הבלוק (`night/wave7/defs`, 3 בלי מספרים; `night.wave7.check_defs`
   ירוק) — לא מוחלות, מנגנון-החלה לפקודה קיימת לא נכתב. OCR עברי: אין במחשב (tesseract אין, Windows
   OCR en-US בלבד). **hG5a** (משפט-אוצר בתוך מירכאות): סעיף „הרכב דמי הקיום" ב-35.0201 — תיקון באצירה
   אצל סשן א' (הכותב היחיד לקורפוס עכשיו).
8. **ביקורת-נאמנות של האצירה-מחדש** (`84107fd`+`713712c` של סשן א', מול תמונות-העמוד — `night/recurate/
   REVIEW_22.09_sessionB.md`): **25/25 נאמנים במספרים; 5 ליקויים לתיקון אצל סשן א'** — סמכות עיכוב-מחבוש
   שנשמטה (33.0352 §30), חובת פרטים על המחאה שנחתכה (35.0221 §25), „משקל"ר"→„מקשל"ר", „עד תום הדיון" שאינו
   במקור (§38), ושני משפטי-אוצר שנשארו בבלוק חסר-המספרים של 35.0201 (מחלקת hG5a).
9. **בדיקת-תשובה ל-`ANSWER_TERM_NOTE`** — מתוכננת ומתומחרת, לא הורצה (`night/TERM_NOTE_CRITERION.md`): 14
   שאלות × 2 זרועות, מעבר ראשון, ≈ **$1.9 בבאץ'**; אבחון: hW3a הקליד „תשמש" בלי גרשיים ⇒ ההרחבה לא נדלקה
   (מועמד גנרי: צורות בלי-גרשיים לכל הטבלה); hR1db — היעד בחלון גם בלי דגל ⇒ כשל צד-תשובה.
10. **צורות בלי-גרשיים — `RETRIEVE_QUOTELESS`, נמדד ועבר** (`night/QUOTELESS_CRITERION.md`, קריטריון לפני):
    מנוף-מילון כללי — כל מפתח מצוטט בשתי הטבלאות מתאים גם בלי גרשיים לפי כלל אחד (3+ אותיות, לא מילה שכיחה /
    מילה שהפקודות כותבות בלי גרשיים); הסט הנגזר {תשמש, תבן, קלב} נעול בבדיקה, קפץ/שקם מוחרגים. הזרוע
    HOMONYMS+QUOTELESS מול HOMONYMS: **hW3a — 35.0210 נכנס לחלון** (מקום 11 → סעיף-תוכן בחלון); הסט הנעול
    10→10, סרגל 30, dev 76, held 72→73 (= hW3a; בלי המזוהמות 71), שער 390, נתב 37, c 24, real24 זהה —
    **אפס אבודים בכל מכשיר.** ⇒ מוצע לפריסה: `RETRIEVE_QUOTELESS=1` + `RETRIEVE_HOMONYMS=1` (הכרעת המשתמש).
11. **ביקורת-נאמנות, סבב שני** (4 יעדי head-100 של סשן א', `c319771`, 55 סעיפים כולל השמורים, מול תמונות-העמוד —
    `night/recurate/REVIEW_22.09_sessionB_2.md`): **53/55 נאמנים במספרים; 5 ליקויים לתיקון** — שני מספרי-טופס
    שגויים בבלוק *השמור* של 32.0402 (112→110, 113-0→113-2; ירושה משכבת-הטקסט המשובשת, ה-raw_text המתוקן נכון),
    תקרת-שבועיים שנשמטה (32.0402 §6), ייחוס שגוי של מדרגת-הבירור (§41), הפניה שגויה לתנאי שכ"ל (35.0502 §10ב).
    **לקח:** בלוק „שכבר נושא מספרים" מלפני גל-הספרות אינו מאומת; בדיקה רוחבית חינמית: 10 מ-160 מספרים בבלוקים
    השמורים של 20 הפקודות המתוקנות אינם ב-raw_text — מועמדים לעין. ⚠ סשן א' דיווח: העוגנים על העץ המאוצר-מחדש
    איבדו שורת held (PM-33.0352 מקום 6→7) ⇒ הוחזרו, ההכרעה חזרה למשתמש; אימות מספרי-העוגנים שלי נדחה.
12. **23.09 — עבודת-ההמתנה (חינם, בלי כתיבה לקורפוס):** (א) **OCR** — `night/ocr.py` (Tesseract 5.4, heb best, psm 3,
    300dpi; דוח-איכות + שער-אינטייק יבש; מסרב לכתוב תחת pdf-ldf_law/storage), 25 קבצים → `D:/app_soldier/sources_pending/ocr/`
    (`REPORT.md`). **ממצא: 10 החד-עמודיים ב-`_unextractable` אינם סרוקים** — ה-OCR ≈ שכבת-הטקסט (545 מול 565 תווים,
    רעש 0.000); הם נדחים רק כי קצרים מ-900 תווים/עמוד ⇒ הפתרון הוא כלל-קליטה לפקודות קצרות, לא OCR. 30110.pdf = הפ"ע
    3.0110 (עותק של פקודה שבקורפוס), לא 31.0110. FOI: 12/14 עוברים את השער עם OCR (8 רק בזכותו), 2 לא (היתר-זקן 720,
    נוהל הנפקד 806 תווים/עמוד). (ב) **המבחן הסופי על הסרגל** — `night/FINAL_RULER_CRITERION.md`: זרוע אחת על 72 מול
    grade-blocks6 (כל 72 נענות מחדש — הקורפוס זז), אפס אבודות אחרי עיון / אפס בטוחות-ושגויות / head-100 לא יורד /
    real24 מדווח; ≈ **$8.3 בבאץ'** בחלונות blocks6, לרענן אחרי המיזוג; לא הורץ. (ג) **הורדות wave5a: 0/18** — דפי
    אתר-הפקודות מחזירים דף-אתגר אינקפסולה גם עם כותרות דפדפן; אין כתובות /media/ שמורות ל-18 — לא מתעקשים.
    (ד) **fly.toml v157 = שלושת הדגלים** (`17a1609`: HOMONYMS + QUOTELESS + ANSWER_TERM_NOTE, בהכרעת המשתמש „תדליק
    גם את ההערה"; TERM_NOTE צד-תשובה — package_measure מודד רק את שני דגלי-האחזור). (ה) **K=7** — קריטריון
    `night/K7_CRITERION.md` (`8efb346`) לפני המדידה; עץ קריאה-בלבד `D:/_k7_wt` = `6112fd8` + קוד QUOTELESS; k6 מול
    k7 בדגלי v157 — **חלקי (הסשן נסגר):** סרגל k6 29 → k7 **30, rs060 חזרה, אפס אבודים, אבל מילים +13.4%** (מעל
    תקרת +12%); head-100/שער/נתב/c/real24 של k7 טרם רצו — הפלטים ב-`D:/_k7_wt/night/head100/out/`, להריץ שוב משם
    (הפקודה ב-`night/K7_CRITERION.md`). הכרעה: המשתמש, אחרי השלמת המדידה.
**✅ הוכרע 23.09 („תשאיר את העוגנים בחוץ, ותדליק את שני הדגלים ב-v157"):** העוגנים נשארים מוחזרים; fly.toml
נושא `RETRIEVE_HOMONYMS="1"` + `RETRIEVE_QUOTELESS="1"` (קומיט נפרד `c3ad8e3`, לא נפרס). **v157 = הקורפוס המאוצר-מחדש
של סשן א' + שני הדגלים;** לפני הפריסה: `python -m night.package_measure REAL24=...` על עץ-הפריסה אחרי מיזוג הקורפוס
(אפס אבודים + hW3a בחלון), ואז הפריסה בידי המשתמש/המנהל. **⏰ ממתין למשתמש:** זרוע CONTINUE ($3.5).
**✅ הוכרע (22.09, דרך סשן-המנהל):** v156 נפרס (דלתות + rs041; אומת קריאה-בלבד: releases v156, המכונה
48e4659f039578 תקינה 1/1, הדלתות פעילות על עץ main — rs041 negative→medical_scope, doorgate PASS, served
66/97) · חריג-העוגנים מאושר — סשן א' מחיל (`night.wave7.apply_anchors --write`; אני מאמת מול הרשומה שלי:
c 28, סרגל 32, שער 393, dev 84, held 72) · בדיקת TERM_NOTE ($1.9) מאושרת — סשן א' מריץ מהעץ עם `.env` לפי
הקריטריון, העיון שלי · 35.0502→32.0502 אחרי הגל · Tesseract אצל המנהל.
⛔ הקורפוס סגור לסשן הזה (סשן א' = הכותב היחיד: 5 הליקויים, העוגנים, 4 היעדים הבאים ו-7 חסרות-הבלוק —
אני מבקר אותם מול תמונות-העמוד כשידווח).

**🧭 19.09 ערב — התמונה המאוחדת של שלושת הסשנים (הבלוק הזה הוא העדכני; מה שמתחתיו היסטוריה).**
**✅ פרודקשן: v154 = main `5dcbff7`, נדחף (origin/main + origin/claude/head100).** החבילה שנפרסה,
באישור המשתמש: **wave-6** (שלושה מסמכים באצירה ידנית, $0: אכיפת הופעה §5, מענה ההזנה, קידום
חוגרים כמבנה-בלי-מספרים) + **תקרת בלוק-המוביל** `RETRIEVE_FULL_BLOCK_MAX_WORDS="2000"` +
**מטמון פרומפט-המערכת `SYSTEM_CACHE_TTL="5m"`** + תיקון נוסחת-המחיר ביומן. חבילה **70/70** על העץ
הממוזג; מאגר-הידע קורא **193 פקודות**; בדיקת-בריאות עוברת. **עשן-התקרה, חי:** „שפטו אותי ונתנו לי
14 ימי ריתוק, אפשר לערער?" ⇒ „✓ רשאי להגיש ערר" מפ״מ 33.0302 — **הפקודה שהתקרה חותכת הכי חזק** —
עם מועד-הערר, הטופס בנספח ז׳, המסירה לשליש, הדיון אצל קש״ב וחובת קצין-השיפוט ליידע. התשובה שלמה.
ליג'ר **$136.75/$145, נותרו $8.25.**
**✅ 20.09 — הדלתות נבנו ונמדדו (סשן שלישי, ענף `claude/doors`, לא ב-main ולא בפרודקשן עד אחרי
הריצה בתשלום).** מזווג על **`grade-blocks6` אחרי עיון שלב 6**, לפני = origin/main `5dcbff7`:
**served 61 → 65 מ-97**, strict 47 ללא שינוי (דלת אינה עונה), **goal 76 ללא שינוי** — הפיצול לא ניפח.
דלתות מאומתות 11 → 14. שלוש משפחות: `civil_school` (rs053), `fault_report` (rs003),
`pay_entitlement` (rs049); שתי דלתות שגויות נתפסו ביום הראשון וצומצמו (אבא של ילד חולה; שעות
נוספות בעבודה אזרחית). חבילה 71/71.
⛔ **ה„+15 חלקים" שברשימת-העבודה הישנה היה ברובו חשבונאות:** שדה ה-`where` של `unit_routine` ושל
דלת ברירת-המחדל זהה מילה במילה (רק `label`/`why` שונים) — החייל ראה את אותה כתובת בשתי הדרכים.
לכן פוצלה ברירת-המחדל: `unit_level_default` שומר את הניסוח הבטוח רק היכן שהפקודות עצמן מוסרות
ליחידה (33.0401, 21.0113, 61.0104), ו-`not_in_our_orders` אינו טוען דבר. **הדוגמה החיה: rs058**
(לבוש אזרחי בעיר) — הק"א 33-05-01 §5 של wave-6 מדורגת ראשונה אבל **אינה מכילה את הכלל** (היא
על אכיפה); הכלל בהק"א 33-05-07 שאין לנו. לפני הפיצול היא קיבלה „נקבע ביחידה שלך" — טענת-מיקום
שקרית על הוראה מטכ"לית שחסרה לנו; עכשיו „ייתכן שיש הוראה מטכ"לית שאינה אצלנו".
⚠ **rs041 אינה פער-דלתות:** `medical_scope` כן תופסת את הטקסט, אבל התשובה אינה נושאת את
סימן-החוסר, ולכן `report_goal.door()` מחזיר None לפני שהמשפחה נבדקת. זה אותו שורש של באג-הצ'יפ
מ-18.09: **הסימן שהמודל כותב מכריע שלושה דברים** — הצ'יפ (`app._verdict_chip`: `_MARK_OOS` מול
`_MARK_MISS`), רצועת „לאן פונים", והאם דלת נבדקת בכלל. התיקון בשכבת-התצוגה/החיווט;
⛔ שינוי טקסט כלל-הסירוב בפרומפט פסול (נמדד 28.08, הסירובים הורעו).
⚠ **שני לקחי-מכשיר (20.09):** ‏(א) `night/report_goal.py` מודד **זרוע** (`grades_<tag>.jsonl`) ולא עץ —
דיווח בלי שם-הזרוע חסר-משמעות; ריצה על `realstyle` (ישנה, בלי קובץ-עיון) הראתה 45→49 ונקראה
בטעות כנסיגה. **לנקוב בזרוע בכל דיווח, ולהשוות מזווג בלבד.** (ב) `refused_flag=False` ב-probe
פירושו „הזרוע הזאת לא סירבה", לא „הפקודות עונות" — **הבוררות גוברת**; נאכף ב-`night/doorgate.py`
(חינם, שער אפס-תפיסות על 388 שאלות שנענו).

**⏳ לא אומת, ולא ניתן לאמת כרגע:** המחיר הרשום לשאלה אחרי `5m` (החיזוי ~$0.12 במקום $0.144).
נקרא 20.09 מהמכונה (`flyctl ssh console`, באישור המשתמש): **ביומן 154 שורות ואפס שורות-שאלה** —
`grep cost_usd` מחזיר 0; הכול שורות-משוב, רובן מ-`test-session-flood`. היומן מתאפס בכל פריסה, ועשן
התקרה לא הותיר בו שורה. ⇒ **האימות דורש שאלה אמיתית אחת דרך האפליקציה אחרי v154** (~$0.12
מהליג'ר), ואז לקרוא את השורה. עד אז המספר הוא תחזית.
**⚠ למי שפורס בהמשך — מתוקן:** ‏`flyctl` אינו קורא את `~/.fly/config.yml` („no access token
available"), אבל **האסימון בקובץ תקין** (665 תווים) — עקיפה: `FLY_API_TOKEN=$(sed -n
's/^access_token:[[:space:]]*//p' ~/.fly/config.yml)`. ⚠ אל תחלץ אותו ב-`grep -oE '[A-Za-z0-9_-]+'`:
האסימון מכיל `/` ו-`+` והוא ייחתך. ‏`config.yml.bak-19sep` ושלושת ה-`.corrupt-*.bak` נושאים אסימון
ריק. ‏`ssh console` עצמו הפכפך — „The handle is invalid" בערך במחצית הקריאות; פשוט לחזור עליה.
**ממתין למיזוג אחד, בענף `claude/head100` (סשן head-100), לא ב-main ולא בפרודקשן:**
(1) **wave-6**, אצירה ידנית ב-$0: `HKA-33-05-01-S5` (אכיפת הופעה, 7 סעיפים, חסר-ספרות) +
`FOI-FOOD-2025` (מענה ההזנה — טבעונים/מהדרין/צליאק; מתויג „מסמך מסביר, אינו פקודה"); ביקורת-נאמנות
בלתי-תלויה מול ה-PDF — 6 ממצאים, כולם תוקנו (המהותיים: סייג „הומר לזכאות תקציבית" חזר לסעיף-הכמות;
חובת מפקד-היחידה קיבלה סעיף משלה). **בהחלטת המשתמש 19.09 נאצרים עכשיו עוד שניים:** הק"א 32-03-10
קידום חוגרים כ**מבנה בלי מספרים** (הנוסח מאי 2009, בעדכון — בלוק חסר-ספרות כפוי עם הערה) וקרפ"ר
300.001 זכויות החולה („כן" מפורש על הסימון „שמור"). נשארו בחוץ: היתר-הזקן (רק מכתב-לוואי), דחייה/פטור
(מלש"בים), המפתח, ושבעת הסרוקים.
(2) **ידית-ההוזלה** `RETRIEVE_FULL_BLOCK_MAX_WORDS` (`claude/cost-levers` `bc0dc7e`, כבוי בקוד) + תיקון
נוסחת-המחיר ביומן (כתיבת-מטמון-שעה 2×). נמדד חינם, קריטריון מראש (`night/COST_WINDOW_CRITERION.md`):
**תקרה 3000 ו-2000 עוברות באפס אבודים** — שער 390, סרגל 30/82 + 36/82, head-100 76 + 72, וגם עם הנתב
(37/82, 12/20); על עץ wave-6 עם תקרה 2000: אפס אבודים, חלון head-100 ממוצע 3,009 / מקס' 4,689 (היה
12,273). **חיתוך הבלוקים הלא-מובילים (450/5, 360/4, 300/3) נדחה** — כולם מאבדים rs067 + rs009 ושניים
בנעול. גם `DOC_BLOCKS=8` נדחה (head-100: +22–25% מילים ל-+1 נעול). פרופיל המחיר: `night/PLAN_COST.md`
(שאלה קרה $0.144: חלון 49%, כתיבת פרומפט-המערכת למטמון-שעה 37%, פלט 14%; עברית 3.9–4.5 טוקנים למילה).
**הסדר שסוכם — כל צעד באישור המשתמש:** מיזוג `claude/head100` ל-main ← חבילה מלאה על העץ הממוזג ←
`fly.toml`: `RETRIEVE_FULL_BLOCK_MAX_WORDS="2000"` ← **פריסה אחת** (לבדוק `flyctl releases` קודם — גם סשן
המכשיר פורס) ← שאלת-עשן חיה שבלוק-ענק מוביל בה (פ״מ 33.0302) ← `arm dry` ← **הריצה בתשלום של
head-100** ($4.5–5.3; המדידה הראשונה ברמת-התשובה מאז 17.09, והיחידה שרואה אם בלוק-מוביל קצר פוגע
בתשובות מלאות). **פתוח אצל המשתמש:** `SYSTEM_CACHE_TTL` שעה→5 דקות (מחיר בלבד, ~−$0.02 לשאלה קרה
בתנועה דלילה; אם כן — נכנס לאותה פריסה).
**⛔ עד ה„סיים" של head-100:** אין כתיבה ל-`storage/json_store` / `embedding_cache.npz`, אין הוצאה
מהליג'ר, אין פריסה — מאף סשן אחר. **אחרי הריצה בתשלום, לא לפניה:** אצירה מחדש של הבלוקים חסרי-הספרות
(PM-33.0352 ו-35.0221 ראשונות — יעדי head-100; ואז 20 המתוקנות) וגל-ספרות שני; „המעבר השני כהמשך-שיחה
ממוטמן" (~−15%) נבנה כבוי ונדלק רק עם ראיה ברמת-התשובה.


**📍 19.09, מסשן המכשיר (לא נוגע בשאר STATE): main = `054f9d2` (לא נדחף), פרודקשן v154.**
**v154 — ⚠ הלקח המרכזי: האפליקציה המותקנת היא עולם CSS אחר, וכל המדידות עד אליו נעשו בלשונית.**
`html.cai-standalone .stMain` שומר `--cai-sbh` (שורת הכתיבה, ברירת-מחדל 134px) על הגליל, והמשתנה
יושב על תגית ה-html ⇒ שורד ממסך הצ'אט אל מסכי הכניסה. מסך הפתיחה: 194px לגרירה במותקן מול 64
בלשונית; אחרי `MAIN_SB_PADDING` — 64 מקומית, **19 בפרודקשן**, ומסך התפקיד אינו גליל כלל.
**ובאותה מידה חשוב — שלושה פגמים בבודק עצמו:** (א) `const W = innerWidth` — במסך נייד החלון
הוויזואלי מתרחב עם התוכן, כלומר הבדיקה שותקת בדיוק כשמשהו גולש (שני פיתיונות נפלו ברגע
ש-`is_mobile` נדלק) ⇒ חלון-הפריסה; (ב) במותקן html/body/המכולה `overflow:hidden` ואזור-גלילה
אינו עובר אב חותך ⇒ `overflow-x` שואל גם את body וגם את גליל האפליקציה; (ג) הסריקה שלחה שאלות
ב-Enter, שבמכשיר מגע הוא ירידת שורה מכוונת ⇒ שלושת מסכי התשובה **לא נסרקו** ונרשמו „דילוג";
נשלח בחץ. `_browser.phone_context()` + `--standalone` ב-`sweep.py` וב-`audit_selftest.py`.
בדיקה-עצמית במותקן **26/26 פיתיונות, 27/27 מוטנטים**; סריקה שם **10 ממצאים — זהים ללשונית**.

**📍 קודם באותו יום: v152 ו-v153 (main היה `80aa77e`).**
מעבר ל-v151, שתי פריסות ממסך אחד — סרטון שהמשתמש צילם על האייפון.
**v152 (`bf599ed`) — ארבע מחוות ניווט:** (1) מסכי הכניסה נשאו 112px ריפוד של שורת-הכתיבה
שאין בהם ⇒ 16px + `overscroll-behavior:none` (גרירה 135px→45px); (2) הכותרת חזרה 146ms אחרי
שהמגירה שחררה אותה (`.18s` מאחורי השהיה `.14s`) ⇒ בלי השהיה, 10ms; (3) כלי נפתח בשלבים ⇒
הווילון הקיים, עם תנאי משלו — יורד רק כשלחלון יש **תוכן**; (4) החלקת-החזרה: `min(-dx,w)` היה
חסם ⇒ ריסון 30% חופשי + 0.1, הרצועה מאחורי הגיליון נצבעת ברקע שלו ואינה לחיצה, המסך **יוצא**
לכיוון האצבע (10.08 אסר את זה כשמאחור היה שחור), והחדש עולה כשה**כותרת** משתנה ולא בשינוי
ה-DOM הראשון. `tests/test_nav_motion.py` (10 בדיקות, 11 מוטציות נתפסו).
**v153 (`80aa77e`) — יעדי מגע בחלונות הכלים:** שאלות 328x43→44 (`min-height`), כפתור הסגירה
44x34→44x44 עם המרג'ין השלילי שעוקב אחרי הגובה (אחרת `.cai-mhead` יורדת 10px); +2 בדיקות
ב-`test_design_invariants.py`, 3 מוטציות. **סריקת `tools/ui_audit`: 21→10** — נשארו 👍/👎 24x24,
„כל-זכות" 17px, שתי תוויות מתחת לסרגל השמירה הדביק, וכפתור מושבת (כולם קיימים מלפני; המשתמש
בחר לא לגעת בהם עכשיו). חבילה 67/67 ‏(68/68 על העץ הממוזג). **אומת חי:** ההחלקה — יציאה
1125ms, כותרת חדשה 1521ms, נראות 1547ms (החלון הריק ~220ms); כלי — הווילון יורד 1157–1192ms
על חלון מלא; ארבעת הכלים — סגירה 44x44 וכותרת ב-top 59.

**0. ✅ העוגנים נפרסו — 18.09, גרסה 149.** חבילת-הבדיקות על main ‏(`dce1680`) ירוקה
(65/65), `flyctl deploy --remote-only -a commandai` מ-`D:/_deploy_wt_blocks6`, אומת חי:
המכונה בריאה (1/1), 9 הדגלים + `SYSTEM_CACHE_TTL=1h` עליה, ובתמונה 294 פקודות / 1,962 עוגנים
ב-156 פקודות — זהה ל-main. גרסה 148 (דקות קודם) היא פריסת שער-התנאים של הסשן המקביל
(`00e5268`) והיא כלולה ב-149. עשן-אחזור חינמי על אותו עץ עם דגלי-הפרודקשן (בלי נתב, בלי
HyDE — הרצפה): „נרדמתי בשמירה — ישפטו אותי אצל קצין שיפוט?“ ⇒ פ״מ 33.0302 מקום 1 ·
„יום ב' מול יום ג' מהרופא“ ⇒ 61.0104 מקום 1 · „לעבוד בעסק של אבא בסדיר“ ⇒ 33.0115 מקום 1.
שאלת-עשן מלאה דרך הממשק **לא הורצה**: שער-התנאים החדש דורש סימון „קראתי ואני מאשר“ — זה
של המשתמש, לא של הסשן; והרצת תהליך-Python שני על המכונה (2GB, ‏1GB פנוי) מסכנת את האפליקציה.
אין פריסה תלויה. ⚠ מסנן-ההרשאות דוחה פקודות משולבות: git מקומי לחוד, `deploy` לחוד,
`push` לחוד.

**גל-הספרות בוטל (18.09) — הקורפוס לא זז, ואין חסימה על מדידות.** הסשן „שיפור מערכת
התשובה“ עצר לפני כתיבה (אומת: אין שינוי תחת `storage/`). הסיבה, עם ראיה: **השיבוש בספרות
אינו הפיך** — ב-35.0314 סעיף 12 שמור כ-„19“, סעיף 13 כ-„18“, השנה 83 כ-38, ושני מספרי-סעיף
שונים שמורים שניהם כ-„11“; מפה שמאבדת מידע אי-אפשר להפוך, ולכן `night/unscramble.py` דחה
8/8 ב-17.08 — **לא להריץ אותו שוב.** היפוך-ריצות RTL מסביר רק 26 מ-180 המסומנות. קריאת-עמוד
בראייה (Haiku, $0.02) מחזירה מספרים נכונים, אבל עברית משובשת ובלי התאמה אחד-לאחד לשכבת-
הטקסט ⇒ שחזור-מספרים הוא פרויקט נפרד: אצירה מתמונות-עמוד במודל חזק + שער-אימות מול התמונה
(`night/numbers.py` מאמת מול ה-raw_text השבור). פרטים: `night/PLAN_DIGITS.md`.
**✅ 18.09 ~17:00 — גל-הספרות בפרודקשן: v151 = main `53d1a71`** (origin/main `8d078f8` מוסיף רק
קובצי-מדידה; הענף `c1e6a19`). מוזג ונפרס בידי הסשן head-100 באישור המשתמש. שערי העץ המאוחד:
**בדיקות 67/67 · `night.gate` 390/431 אפס נופלות חדשות · הסרגל הקפוא 30/82 תוכן + 36/82 פקודה
אפס אבודים · head-100 148/174 אפס אבודים.** הוכחה חיה על v151: „כמה זמן פיגור בתשלום מזכה
בפיצוי?" ⇒ „זכאי בתנאים" מפ״מ 35.0314 **עם המספרים** (פיגור מעל חודש; מקדמה מעל 70% תוך חודש
מבטלת; עד 6 חודשים ריבית החשב הכללי, מעל 6 חודשים מדד) — הספרות המתוקנות מגיעות לתשובה.
**מחיר לשאלה:** פרופיל נמדד ותוכנית-הוזלה מדורגת ב-`night/PLAN_COST.md` (18.09) — ממתין להכרעת
המשתמש איזו ידית ראשונה; כל ידית משנה את החלון המוגש ⇒ הודעת-החזקה כמו בגל-הספרות.
**פתוח אצל head-100, בהמתנה למשתמש:** הריצה בתשלום (אופציה A, ‏$5.0–5.8, `night/head100/PAID_RUN_PLAN.md`);
לפניה עוגנים ידניים ל-11 החמצות-פיתוח (כותב json_store + embedding_cache — עם הודעת-החזקה).
**📍 18.09 ~16:15, מהסשן head-100 (לא נוגע ב-STATE): main = `3855e3d` (נדחף), פרודקשן v150.**
מעבר ל-`07fdf8c`: `night/head100/` — 97 נושאי-חיילים × 2 ניסוחים עם ציטוטי-יעד מאומתים, חצי נעול
(`build.py`, `probe.py`, `compare.py`); `storage/glossary.py` +39 ערכי שפת-חיילים, ערכים רב-מילתיים
מקבלים עד שתי אותיות-תחילית („בצו 8"); `tests/test_glossary.py` +5; קריטריון נכתב לפני המדידה
(`night/head100/GLOSSARY_CRITERION.md`). מזווג, חינם, דגלי-פרודקשן, ב-`D:/_head100_wt`: שער
390→390 (אפס נופלות חדשות), הסרגל הקפוא 30/82 תוכן · 36/82 פקודה ללא שינוי ואפס אבודים, head-100
**140→148 מ-174** (הנעול 69→72). חבילה 66/66 על העץ הממוזג לפני הפריסה.
**הריצה בתשלום לא התחילה** — המשתמש ביקש להחזיק; דורשת „לך" מפורש + תמחור מחדש („$5" היה נמוך;
`night/head100/PAID_RUN_PLAN.md` בענף `claude/head100`, ‏`6bd90a1`, לא ב-main). **סדר חובה לפניה:**
גל-הספרות (למטה) מקומט → ממוזג ל-main → נפרס, כי הריצה מודדת את העץ שהפרודקשן מגיש; ואז ריצה
אחת של שער + sectprobe + head-100 על העץ המאוחד (388 של הענף מול 390 של main — עצים שונים).
**✅ 18.09 ~16:00 — גל-הספרות נכתב, נמדד, ומדידות פתוחות מעכשיו (כולל הריצה בתשלום).**
20 פקודות (`digits_fixed` בכל אחת): 1,408 מילים תוקנו, ~780 נדחו בכוונה (מבנה-ריצות שונה —
פיסוק שמופה לספרות; נשארו כמו שהיו), 38 אי-הסכמות בין שתי הקריאות הושארו. ‏31.0308 לא נגעה
(ה-raw_text השמור אינו חילוץ ה-PDF). **השערים:** צ'אנקים 10,408→10,408 ועוגנים 1,962/156 ללא
שינוי · `routed_sectprobe` מול `sect7`: **אפס אבודים, אפס נוספים** (50/82 פקודות, 37/82
סעיפים, 12/20 אמיתי; חלון ממוצע 3,399→3,397, חציון 3,134, מקס' 12,040) · `night.gate`
**388/431 → 388/431, אפס רגרסיות** (`gate_before_digitfix.json` = ה„לפני") · חבילת-הבדיקות
57/60 — **שלוש הנופלות הן בדיקות-`app.py` שנשברות מה-diff הלא-מקומט של סשן אחר**
(`_verdict_chip(content, question)` → `_verdict_chip(content)` בעותק-העבודה; אותן שלוש עוברות
ב-`D:/_sectprobe_wt` על app.py המקומט) — לא הקורפוס. ‏`digits.py`: 180→171 מסומנות (המדד
מבוסס שנים בנות 4 ספרות ולא רואה מסמכי-83). עלות: $0.27 + $0.26 שתי קריאות + $0.02 ניסוי.
**מה עוד לא נעשה, ומכאן הצעד הבא:** הבלוקים המאוצרים של 20 הפקודות עדיין חסרי-ספרות —
אצירה מחדש היא שינוי-קורפוס נוסף (עוד חלון „לא למדוד"), ודורשת הכרעות: `night.curate` מכוונת
רק לחסרות-בלוק ומחזיקה `NEVER={3.0502, 33.1010}` בגלל הספרות שתוקנו עכשיו — פתיחה מכוונת;
`digest` מוסיף ולא משכתב. עד אז, המספרים המתוקנים מגיעים לחלון רק דרך צ'אנקי-הטקסט.
פירוט: `night/PLAN_DIGITS.md`. קוד: `night/digitfix.py` + `tests/test_digitfix.py` (9).
_היסטוריה של הגל (מיושן):_ **⏳ 18.09 אחה"צ — גל-ספרות בפועל, בסשן „שיפור מערכת התשובה": `night/digitfix.py` (תיקון לפי
מיקום — גזירת כל מילה-עם-ספרות לפי המלבן שלה, קריאה מהתמונה, השתלה במקום; `night/PLAN_DIGITS.md`).**
נבדק חינם על 35.0314: 47 תיקונים, 0 סירובים, הטקסט הלא-ספרתי זהה. **עכשיו:** קריאה ב-Haiku
של 21 היעדים (~$1.5, באישור המשתמש) — קריאה בלבד, הקורפוס עדיין לא זז. **אחריה, בסשן ההוא:**
בדיקת-עין, `--apply` ל-`raw_text` של עד 21 פקודות + `index_document` לכל אחת (upsert מקומי),
ואז 66 בדיקות, `routed_sectprobe` מול `sect7`, `night.gate`, מילות-חלון ממוצע/מקסימום.
**⇒ עד שהבלוק הזה יתעדכן ל-„✅ נכתב": לבנות כן (ניסוחים, יעדים, מילון, דלתות — לא תלויים
בטקסט), למדוד לא — כל מדידה שאמורה לשמש קו-בסיס, ובוודאי הריצה בתשלום, אחרי.** לפני
הכתיבה הראשונה יעודכן כאן „✍ כותב", ואחרי השערים „✅ נכתב" עם המספרים.

**ממתין להחלטת המשתמש, אצל הסשן ההוא:** 7 הפקודות בלי בלוק מאוצר (3.0502, 36.0301, 31.0252,
33.1010, 36.0313, PM-33.0109, PM-33.0342) אינן קיימות עבור האחזור; אצירת `key-facts-nodigits`
תכניס אותן למרחב-החיפוש ב-~$0.2–1, בלי קוד חדש. זה שינוי-קורפוס ⇒ שער + מכשיר-עם-נתב
לפני ואחרי, ואז כן מחכים עם מדידה מזווגת.
**המכשיר-עם-הנתב נכנס למאגר:** `python -m night.routed_sectprobe <out.json> --against
night/out/sect7_routed_prodflags.json` — חינם; קוד-יציאה 1 אם יעד שהוגש אבד. קו-הבסיס
הנוכחי (דגלי-פרודקשן מלאים, כולל מילון): **37/82, 12/20**; רשמי בלי-נתב 29/82; שער 388/431.

**⚠ זנב-עלות שנמצא 18.09 (לא רגרסיה — מחיר):** `RETRIEVE_FULL_BLOCKS=1` מגיש את **כל** הבלוק
של הפקודה המובילה, בלי תקרה. ארבע פקודות נושאות בלוק ענק (פ״מ 33.0302 ‏9.8K מילים,
HKA-31-08-01 ‏7.6K, פ״מ 34.0101 ‏4K, חוק השיפוט 3.2K), ושלוש מהן קיבלו עוגנים ב-18.09 ⇒ הן
מובילות יותר. rs012: חלון 2,836 → **12,040 מילים** (וזה מה שהפך אותה למוגשת). ממוצע החלון
על 82 היעדים +3.4%, החציון לא זז (3,134); שאלה שפ״מ 33.0302 מובילה בה עולה פי ~2.5–3 (הערכה לפי יחס-המילים; לא נמדד בדולרים).
מועמד (חינם למדידה, דגל כבוי): תקרת-חיתוך גם לבלוק המוביל, כמו ב-DOC_BLOCKS (600 מילים ⇒
6 סעיפים) — הקריטריון: rs012 ושאר המוגשות נשארות מוגשות.

**1. המשימה הבאה שלי: דלתות לשאלות שאין עליהן פקודה** (`out_of_scope.py`,
`scope_routes.py`, `tests/test_out_of_scope*.py`; המדד ב-`night/report_goal.py`). זה
המנוף החינמי הגדול שנשאר: הפער served→goal הוא **15 חלקים** (served 61 → עד 76 מ-97).
רשימת-העבודה — 11 שאלות שמגיעות רק לדלת ברירת-המחדל ואחת בלי דלת:
rs003 תאורה בגדר · rs036 טלפון בזמן משמרת · rs016 לדבר עם חייל מיחידה אחרת · rs028
אישור על איחור · rs061 השכמה · rs032 טעות בדוח · rs001 איחור, מה לומר למפקד · rs049
שעות נוספות · rs031 הוראות סותרות משני מפקדים · rs053 ילד מבית-ספר (אזרחי) · rs058
לבוש בעיר (NOT_IN_CORPUS) · rs041 ימי מחלה על שפעת (בלי דלת כלל).
**הכלל הקשוח:** דלת שגויה גרועה מאין-דלת — אפס תפיסות על כל השאלות שנענו (595
המדודות + הסרגל), וכל משפחה נבדקת על שאלות שלא השתתפו בכתיבתה (גל של 10 שאלות
טריות, ~$0.4; בליג'ר $8.77). משפחת `unit_routine` תופסת היום 5/21 — נבנתה בדיוק-תחילה.

**2. מסלולים מקבילים:** הרחבת-הקורפוס — בסשן אחר, ההמלצה בסעיף „הרחבת הקורפוס"
למטה · מענה חופש-המידע עד **29.09** (5 חלקים; $20–30, דורש תקרה) · ניסוחי-חיילים
מיומן-הפיילוט ⇒ עוגנים ממוקדים (עוגן שפוגע בניסוח: מקום 28→1; שלא פוגע: אפס).

**3. החלטות קטנות שמחכות למשתמש:** `RETRIEVE_V2_TOP_K=12` (+2 סעיפים, +12% מילים) ·
`eval.py --facts` על קיצור-ההיסטוריה (~$2; נפרס בלי מדידה בתשלום) · Sonnet עם כלל-סירוב
מחמיר (~$1.7; Sonnet כפי-שהוא נפסל) · דליפת תחום-התפקיד: ב-4 יעדים הפקודה שעונה מחוץ
ל-`_docs_for_role` של השואל (rs007: 35.0206 מתויגת reserve/commander, נשאלה כ-soldier).

**עצי-עבודה:** `D:/_sectprobe_wt` (ענף `claude/sectprobe-content` = קצה ענף-העבודה; כאן
מודדים — אין בו `.env`, אז אי-אפשר לשלם בטעות) · `D:/_deploy_wt_blocks6` (main; מכאן
פורסים) · `D:/app_soldier` משותף לסשנים אחרים — `app.py`, `.dockerignore`, `CORPUS_GAPS.md`,
`DOWNLOAD_LINKS_wave5.md`, `sources_pending/` אינם שלי; זרועות בתשלום רצות מכאן (יש `.env`).

## המטרה ואיפה אנחנו

**„כל שאלה מקבלת תשובה מלאה ונכונה", יעד 90%, נמדד על `served`.**
הסרגל: 72 שאלות בסגנון אמיתי, 97 חלקים, מבוררות.

| | נענו מהפקודות (strict) | + הופנו לדלת מאומתת (served) |
|---|---|---|
| 10.09, לפני הסבב | 34/97 (35%) | 34/97 (35%) |
| clean3, אחרי עיון-מדרג (12.09) | 41/97 (42%) | 55/97 (57%) |
| **blocks6, אחרי עיון-מדרג (17.09) — בפרודקשן** | **47/97 (48%)** | **61/97 (63%)** |
| תקרה עם הקורפוס הנוכחי | 64% | ~96% |

הזרוע blocks6 מול clean3: **+7 / −1 תשובות מלאות אחרי עיון (29→35 מ-72)** — הסבב
הראשון מאז 28.08 שעובר קריטריון מלא.

## מה בפרודקשן (גרסה 149, 18.09; הדגלים מגרסה 144, 17.09)

`RETRIEVE_DOC_BLOCKS=6` — הבלוקים המאוצרים של 6 הפקודות הראשונות בדירוג עמוק
מצטרפים **אחרי** חלון-8 הרגיל (הרחבה ב-`widen_context`); הבריכה העמוקה מדרגת
ואינה מוגשת. יחד עם `RETRIEVE_LACK_CLAUSES=3`, `RETRIEVE_SECOND_PASS_KEEP_RULING=1`,
`ANSWER_V2=1`, על גבי HYDE / GLOSSARY / FULL_BLOCKS / ROUTER_SLOTS=2 / SECOND_PASS=4.
אומת חי (שאלת-העשן + rs060 שנענתה לראשונה). **מחיר: $0.20–0.25 לשאלה** (הקלט ~75%),
~80–100 שאלות לחודש ב-$20. גרסה 146 (17.09): `SYSTEM_CACHE_TTL=1h` — פרומפט-המערכת
במטמון-שעה (הימור-תנועה הפיך). ⚠ שיחה ארוכה יקרה: תור שלישי $0.31, כי ההיסטוריה
נושאת ~11K טוקנים לחילוף. **גרסה 147 (17.09): ההיסטוריה מוחזרת חשופה** (תורים ישנים
בלי קטעים, החילוף האחרון שלם): תור רביעי $0.31→$0.16, ההמשך מבין (HANDOFF 17.09 ד).
**גרסה 149 (18.09): 68 העוגנים בשפת-חייל** (אחזור בלבד; 1,962 עוגנים ב-156 פקודות על
המכונה) — יחד עם שער-התנאים של הסשן המקביל (גרסה 148, `00e5268`).

## הממצא שפתח את הסבב (16.09)

מכשיר-הסעיף המילולי (`night/sectprobe.py`) היה עיוור לבלוקים שמנסחים מחדש;
כלל-התוכן (`CONTENT_MIN=0.30`) תיקן זאת. ייחוס על 82 יעדים: **ב-35 הפקודה
מדורגת והבלוק מכיל את הכלל, והוא לא הוגש** — זה מה שהבלוקים סגרו. הבריכה של
40 צ'אנקים (12.09) הייתה „חלון רחב" מוסווה: פי 4 מילים, +5 סעיפי-פיילוט בלבד.

## מה נשאר, לפי מנגנון (82 יעדים, מכשיר-תוכן)

| | כמה | מי |
|---|---|---|
| הפקודה לא מדורגת בטופ-25 אף שהבלוק מכיל | 20 | **המנוע — הצעד הבא הגדול** |
| מדורגת, מעבר ל-6 הבלוקים | 9 | K או מדרג-על |
| הבלוק נחתך (>600 מילים → 6 סעיפים) | 4 | נמדד: +2 ב-TOP_K=12, +4 ב-1200/12 |
| הבלוק לא מכיל את הכלל | 8 | אצירה |
| המסמך חסר (חופש המידע) | 5 | מענה עד 29.09 |

## ⏰ הצעד הבא

0. ~~קיצור-ההיסטוריה~~ נפרס (v147). ~~זרוע Sonnet 5~~ **נמדדה 17.09 ולא עברה:** אחרי
   עיון −3 מלאות (5 אבודות / 2 רווחים) ושלוש תשובות בטוחות על שאלה אחרת — נשארים
   על Opus. פתוח: Sonnet עם כלל-סירוב מחמיר (~$1.7), או Sonnet במעבר הראשון בלבד.

1. ~~חיתוך-בלוק~~ **נמדד 17.09:** TOP_K=12 מביא +2 סעיפים ב-+12% מילים; 1200/12 +4 ב-+42%.
   הכרעה פתוחה (משתני-סביבה בלבד). HANDOFF 17.09 (ב).
2. ~~חזית המנוע / מדרג-על~~ **נמדד 18.09 ונסגר:** התקרה בסגנון-אמיתי +1; בלוקי-נתב
   ונתב כותרות-סעיפים מתחת לקריטריון. עם הנתב בלולאה הפרודקשן מגיש **35/82** (לא 27).
   **המנוף הבא: תוכן** — עוגנים בשפת-חייל לפקודות שלא מדורגות, אצירה, FOI. ליג'ר $8.79.
   **18.09 (ב): 68 עוגנים נכנסו** (+2 סעיפים, שער 388/388, **נפרס 18.09 — גרסה 149**) — עוגן עובד רק כשהוא
   פוגע בניסוח; המנוף האמיתי = ניסוחי-חיילים מיומן-הפיילוט. דליפת תחום-תפקיד: 4 יעדים.
3. 29.09: מענה חופש-המידע → digest → זרוע.

## הרחבת הקורפוס — ההמלצה (18.09, לסשן שמוריד)

**מה זה יקנה ומה לא.** הסרגל לא יזוז: מ-97 החלקים רק 5 חסרים בגלל מסמך, וכולם
מסמכי חופש-המידע, לא פקודות פומביות; 19 הם ענייני-יחידה בלי פקודה; 28 כבר בקורפוס.
מה שכן נקנה: כיסוי לשאלות שהסרגל לא שואל (דת, מזון, השכלה, תוספות-קבע) — ערך
אמיתי למשתמש, עיוור למדד.

**מה לעשות, בסדר הזה:**
1. **להוריד הכל** (142, רשימה ב-`CORPUS_GAPS.md`) לתיקיית-ההמתנה `sources_pending/`.
   חינם ולא מזיק. **לא לקלוט בזמן ההורדה.**
2. **לקלוט גל אחד בלבד: 20–30 הפקודות הרלוונטיות לחייל** (מתוך 31: פרקים 34, 56,
   37, 35, 36, 32 + 31.0110, 33.0915, 21.0104; 3 מבוטלות — לא). ~$0.10 לפקודה
   לאצירה ועוד סקירת-תוכן (ב-17.08 סקירה מצאה 5 פגמים ב-13 סעיפים ששום שער לא תפס).
   תקציב: ~$3–4 מתוך $8.79; מסמכי ה-FOI ידרשו $20–30 בנפרד.
3. **את 111 האחרות (תעסוקת אזרחים, בינוי, אפסניה מבוטלת, מטה) — לא לקלוט.** אף
   חייל לא ישאל עליהן, וכל פקודה מוסיפה תחרות בדירוג.

**שערי-חובה לגל** (חינם, לפני ואחרי): `python -m night.gate` — היום **388/431**, אפס
רגרסיות מותר; והמכשיר עם הנתב (`sect5_routed_base.json` / `sect6_routed_anchors.json`:
**37/82** סעיפים, 12/20 סגנון-אמיתי) — אפס סעיפים אבודים. **כלל-עצירה:** רגרסיה אחת
או סעיף אבוד אחד ⇒ הגל לא נכנס עד שמבינים למה (18.09: תשעה עוגנים לפקודה אחת
הפילו שאלה של פקודה אחרת — התחרות הגלובלית אמיתית).

**⚠ מלכודות:** (א) הרצת `app.py` (AppTest, preview, כל boot) מעכלת כל PDF חדש
ב-`pdf-ldf_law/` דרך ה-API **בתשלום ומחוץ לליג'ר** — לכן ההמתנה ב-`sources_pending/`
ולא שם, ו-`backend.ensure_pdfs_ingested` מנוטרל בכל רתמה. (ב) `storage/embedding_cache.npz`
הוא בינארי ונכתב מחדש בכל שינוי-קורפוס; הוא עודכן ב-18.09 (68 עוגנים, קומיט
`ad56ca4`) — **למשוך את הענף לפני קליטה, ורק סשן אחד נוגע בו בכל רגע.** (ג) פקודה
בלי בלוק `key-facts` אינה קיימת לחיפוש (`RETRIEVE_CURATED_ONLY`) — הורדה בלי אצירה
לא מזיזה כלום. (ד) אל תריץ `night.anchors --apply` או `_reingest.py` באמצע מדידה.

**ומה חשוב יותר מקורפוס:** צוואר-הבקבוק היום הוא ניסוחים של חיילים אמיתיים (עוגן
שפוגע בניסוח מקפיץ פקודה ממקום 28 ל-1; עוגן שלא — אפס). יומן-הפיילוט ייתן אותם.
גל-קורפוס קטן **במקביל** להכנות לפיילוט, לא במקומו.

## כללים, כלים, בדיקות

הכללים: `CLAUDE.md`. הבדיקות: `venv\Scripts\python.exe tests\run_all.py` (59 קבצים,
בלי API). הכלים: `night/sectprobe.py` (סעיף-בחלון, מילולי + תוכן) ·
`night/arm_second.py` (זרוע מזווגת) · `night/review.py` (עיון שלב 6) ·
`night/report_goal.py` (strict/served/goal, כפי שדורג ואחרי עיון).
⚠ `night/numbers.py` מצל על `numbers` של הספרייה הסטנדרטית — אל תכניס את
`night/` ל-`sys.path`. ⚠ באץ' יכול לקחת שעות; כרטיס-התביעה + `night.collect`
מסיימים אותו אחרי שהסשן מת.
