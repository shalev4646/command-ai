# העברה — שער התנאים, מועבר על main

**ענף:** `claude/tos-gate-on-main` · **עץ:** `D:/_tos_port_wt` · **בסיס:** main `92c7bad`
**נכתב:** 17.09.2026, סשן מקומי · **לא נפרס ולא נדחף** — שניהם רק במילה של המשתמש.

---

## 1. מאיפה זה בא

סשן ענן בנה שער תנאים בענף `claude/cool-cori-s94h5t` (המסמך שלו: `HANDOFF_TOS_GATE.md`
באותו ענף). בבדיקה מקומית התברר:

- **הענף לא נפרס.** v144→v146 יצאו מ-main; המסך הראשון החי הוא קבלת-הפנים של main.
- **אסור לפרוס אותו כמו שהוא.** הוא נבנה על `114b4e4` (11.09) — פריסה שלו מגלגלת 36+
  קומיטים אחורה: קבלת-הפנים (`9dda505`), שחזור השיחה, כפתור הדיווח, שיפורי האחזור.
- **שני מימושים לאותו מסך.** main בנה ב-12.09 מסך קבלת-פנים בזרימה (`consent_given`);
  הענף בנה כרטיס צף (`cai_name_card`) עם `tos_ok`.

**החלטת המשתמש:** להעביר את הערך של הענף על מסך קבלת-הפנים של main. הכרטיס הצף נזנח.

## 2. מה היה שבור בפרודקשן — כל אחד אומת בקוד ובדפדפן

1. **התנתקות** נחתה במסך תפקיד בלי שדה שם (הדיווח שפתח הכל): `consent_given` שרד את
   `_reset_identity`, והשער קרא רק אותו. צולם.
2. **אודות** הציג לכולם „אישרת את התנאים · בהתקנה הראשונית · גרסה 2.4" — בזמן שהתנאים
   לא הופיעו באף מסך, ותיבת ההסכמה לא הזכירה אותם.
3. **מחיקת הנתונים** — לחיצה אחת, והמכשיר „המאופס" שמר `"ok": true`.
4. **התנתקות ושתי שורות „נקה היסטוריית שיחות"** — לחיצה אחת; מאז שחזור השיחה (v143)
   השיחות שמורות במכשיר ונמחקות לתמיד.
5. **ניגודיות:** 15 כשלי AA אמיתיים (ועוד כפתור מושבת, פטור), ויעד-מגע 22px בכתובת
   התמיכה — בזמן שהצהרת הנגישות טוענת AA.
6. **הצהרת הנגישות** נפתחה גלולה 37px, הכותרת וכפתור החזרה מתחת לדהייה העליונה.

## 3. מה נבנה (הכל ב-`app.py`)

| | |
|---|---|
| רשומת אישור | `TOS_VERSION`, `tos_ok` (גרסה), `tos_date` (יום, שעון ישראל); עוגייה `"tos"`/`"tosd"`, מאומתות ב-`_tos_version_from` / `_tos_date_from`; משוחזרות גם במסלול ה-localStorage |
| הדגל הישן | `consent_given` **לא** נחשב אישור — כל מכשיר קיים יתבקש לאשר פעם אחת, עם התנאים מולו |
| השער | `_welcome_gate = not name_asked or tos_ok < TOS_VERSION` |
| מסך הפתיחה | שורה נפתחת „תנאי השימוש המלאים" (`<details>` מתוך `_TOS_SECTIONS`, הועבר מעל השער), התיבה „קראתי ואני מאשר את תנאי השימוש", השם נזרע מ-`profile_name`, התיבה מ-`tos_ok`; `_record_terms_approval()` רק מאחורי הסימון |
| התנתקות | שומרת את האישור; נוחתת במסך הפתיחה — שם ריק, תיבה מסומנת |
| מחיקה | מאפסת אישור ותאריך; מסך הפתיחה עם תיבה ריקה |
| שאלות לפני מחיקה | `_confirm_open` / `_confirm_ask` / `_confirm_action` — התנתקות, מחיקה, שתי שורות הניקוי, ביטול האישור; `_settle_confirms` מבטל שאלה שנשארה פתוחה כשעוזבים את המסך |
| אודות | `_tos_banner` — „אישרת את התנאים · גרסה 1 · אושרו ב-DD.MM.YYYY"; ביטול אישור מאחורי שאלה, השם נשמר |
| ניגודיות | `.cai-consent-more`, `.cai-fld-label`, שורות השפה, `.cai-tos-sub`, `.cai-set-foot`, `.cai-analytics-sub`, `.cai-pf-savenote .clean`, `.cai-pa-disc` → AA; `.cai-contact-mail` 44px |
| גלילה | `settingsTop()` במנוע המחוות — מסך הגדרות נפתח מלמעלה |
| כרטיס השאלה | הכפתור האדום ישב 11px נמוך מבן-זוגו — אופס בתוך השורה (מדוד: מרכזים זהים) |

## 4. הוכחות

- **אדום לפני ירוק:** 41 בדיקות חדשות (15 + 16 בשני הקבצים החדשים, 9 בציות, אחת
  בכניסה). כל אחת נכשלה קודם — על main, או על הקוד שלפני התיקון שלה — מהסיבה שלה, בהן „logout does not
  reach the welcome screen" ו-„the chats went on the first tap".
- **חבילה מלאה:** 62/62 קבצים (60 לפני + 2 חדשים), אפס שינוי בקורפוס.
- **מוטציות:** 38/38 נתפסו (`scratchpad/mutate.py` בסשן; לא בריפו). כל בדיקה חדשה תפסה
  לפחות אחת. בדיקה אחת שהועברה מהענף (סדר-טקסט) **פספסה** עקיפה
  `st.button(...) or _confirm_action(...)` — הוחלפה; בדיקת ה-AST ב-`test_compliance_screens.py`
  היא שתופסת אותה.
- **דפדפן, מול קוד שבור:** בלי `settingsTop()` — scrollTop=37 והכותרת מכוסה 60%;
  `.cai-tos-sub` ב-.4 — 3.41:1 חוזר.
- **`tools/ui_audit`:** 26 פיתיונות + 26 מוטנטים; סריקה של 26 מסכים (ראו README שם).

## 4א. מה הסקירה מצאה (18.09) — חמישה, כולם תוקנו

כל אחד אומת מול הקוד, נכתבה לו בדיקה שנכשלה קודם, ואז מוטציה שמחזירה את הפגם
(M34–M38, כולן נתפסו).

1. **שדה השם בשער חתך שם שמור ל-20 תווים.** הוא נזרע מ-`profile_name` (עד 40,
   „שם מלא" בהגדרות בלי הגבלה), והדפדפן מחזיר את השדה בשליחה — כך ששם ארוך היה
   נחתך לצמיתות **בדיוק במסך שכל מכשיר קיים עובר פעם אחת**. `max_chars=40`.
2. **תיבה מסומנת שאיש לא נגע בה חידשה את תאריך האישור.** אחרי התנתקות התיבה
   מציגה את האישור של המכשיר; מעבר מעליה אינו אישור חדש, והתאריך היה קופץ להיום.
   נרשם רק אישור חדש באמת.
3. **במסלול שבלי עוגייה השם לא הגיע לשדה.** הבדיקה מהמכשיר עונה ריצה אחת מאוחר,
   אחרי שהשדות כבר נזרעו ריקים. `cai_gate_reseed` מרשה זריעה אחת נוספת.
4. **`_tos_version_from` קיבל כל מספר חיובי** — עוגייה עם „גרסה 999999" הייתה
   עוברת כל עדכון עתידי של התנאים. מוגבל ל-`TOS_VERSION`.
5. **המפתח הישן `"ok"` נמחק מהעוגייה של כולם.** אם הגרסה הזו תוחזר לאחור, הקוד
   הישן לא ימצא הסכמה באף מכשיר וישאל את כולם שוב. הוא נכתב עכשיו מתוך `tos_ok`
   ואינו נקרא — גשר לחזרה לאחור, שאפשר להסיר אחרי שהגרסה מתייצבת.

## 5. סריקה — מה נשאר (21 ממצאים)

- **4 אמיתיים, קיימים בפרודקשן, לא נגעתי — מחכים להחלטת עיצוב:**
  - 👍/👎 בכל תשובה 24×24 (`st.feedback`)
  - הקישור „כל-זכות — פנייה לנציב" בתשובת „לא נמצא" — 17px
  - כפתורי השאלות בכלים — 43px (פיקסל אחד חסר)
  - כפתור הסגירה של חלונות הכלים (של Streamlit) — 34px
- **צפוי / פטור:** שורת „הורה לילדים / נשוי/אה" מתחת לסרגל השמירה הדביק; „שליחת דיווח"
  מושבת (`disabled`).

## 6. לא אומת / פתוח

1. **iOS.** הבדיקה החוסמת (מקלדת מול שדה השם) שייכת עכשיו למסך הפתיחה של main — והוא חי:
   ספארי, חלון פרטי, `commandai.fly.dev`, הקשה על „השם הפרטי שלך". ההעברה מוסיפה שורה
   רק *מתחת* לשדה.
2. **פריסה.** באישור המשתמש בלבד. לפני: מיזוג ל-main; תיאום עם הסשן המקביל שפורס
   מ-`D:/_deploy_wt_blocks6`; בדיקת LF. **כל משתמש קיים יראה את מסך הפתיחה פעם אחת.**
3. **הרשומה נשמרת רק במכשיר.** אין תיעוד בצד השרת; הוספה ללוג השימוש מחייבת עדכון
   מדיניות הפרטיות והחלטה.
4. **כלי המפקדים והמילואים** לא נסרקו (הסריקה נכנסת כחייל).
5. `role_picked_here` עדיין קיים ב-main; לא נגעתי.

## 7. איך מריצים

```
venv\Scripts\python.exe tests\run_all.py
venv\Scripts\python.exe tests\test_terms_gate.py
venv\Scripts\python.exe tests\test_terms_flow.py
venv\Scripts\python.exe tools\ui_audit\run_stub.py --port 8851
venv\Scripts\python.exe tools\ui_audit\audit_selftest.py --url http://127.0.0.1:8851/ --mutants
venv\Scripts\python.exe tools\ui_audit\sweep.py --url http://127.0.0.1:8851/
```

## 8. מלכודות

- **הרנס שקופץ ישר לצ'אט** צריך `tos_ok = TOS_VERSION` וגם `name_asked = True`
  (`consent_given` כבר לא פותח דבר).
- **`Path.write_text` ב-Windows כותב CRLF.** `.gitattributes` מחייב LF כי פריסת CRLF
  הפילה את Fly, ו-`flyctl deploy` שולח את התיקייה, לא את git.
- **`app.py` מטליא את `static/index.html` המשותף ב-venv בכל boot** — `run_stub.py` עובד
  על עותק פרטי; אל תריץ ענף אחר מה-venv המשותף בלי זה.
- **`D:\app_soldier` שייך לסשן אחר.** עבודה על ענף — בעץ נפרד.
