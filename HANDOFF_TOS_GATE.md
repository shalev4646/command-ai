# העברה לסשן חדש — שער אישור התנאים

**ענף:** `claude/cool-cori-s94h5t` · **קומיט:** `fc0e69a` · **נכתב:** 17.09.2026

הסשן הקודם רץ בענן ולכן לא יכול היה לא להריץ את האפליקציה מול המכשיר ולא
להגיע ל-`commandai.fly.dev`. המסמך הזה נכתב כדי שסשן מקומי ימשיך בלי לאבד דבר.

---

## 1. איפה זה התחיל

שאלה של המשתמש: *„כשלוחצים על התנתקות זה מחזיר רק לבחירת תפקיד ואז אי אפשר
לכתוב שם."*

הבירור העלה שהמשתמש זכר שיש מסך אישור ציות בכניסה. **לא היה כזה.** מה שכן
היה: מסך „אודות ותנאי שימוש" הציג באנר ירוק קבוע בקוד —

> אישרת את התנאים · בהתקנה הראשונית · גרסה 2.4

לכל משתמש, בכל ביקור, **בזמן שלא היה באפליקציה שום מסך אישור.** האפליקציה
הצהירה בשם המשתמש שהוא הסכים למשהו שמעולם לא הוצג לו. זה החצי החמור של
משפחת הפגמים ש-`tests/test_compliance_screens.py` נכתב בשבילה.

---

## 2. מה נבנה — 11 קומיטים

סדר המסכים **התהפך**: כרטיס שם+תנאים ← בחירת תפקיד ← צ'אט.

| קומיט | מה |
|---|---|
| `ed438cc` | `tos_ok` בעוגיית המכשיר — תשתית, בלי שינוי גלוי |
| `17a3984` | הבאנר נגזר מ-`tos_ok` בשלושה מצבים; השקר הוסר |
| `47adb11` | תיבת אישור בשער — ריקה, חובה לסמן, אי אפשר לדלג |
| `ddb259a` | היפוך הסדר: השם והתנאים לפני התפקיד |
| `1b79f91` | התנתקות שומרת את האישור; מחק־הכל מרוקן אותו |
| `8e9ccf3` | ביטול אישור מאחורי „אתה בטוח?"; השער בודק גם גרסה |
| `01d16f9` | טסטים — שלושה נכשלו במוטציה ותוקנו |
| `2d68129` | מחיקת הנתונים מאחורי אותו אישור, דרך עוזר משותף |
| `ad8a0a0` | שני פגמים שרק הרצה בדפדפן חשפה |
| `e9fb8b2` | `tools/ui_audit/` + שלוש נקודות עיוורות שהוא הסתיר |
| `fc0e69a` | ניגודיות 8→0, יעדי מגע 31→0 |

### התנהגות שנקבעה במפורש

- **מכשיר חדש** → תיבה **ריקה**. אישור מסומן מראש אינו הסכמה.
- **התנתקות** → נוחת בכרטיס השם, תיבה **מסומנת**, שם ריק. האישור שייך למכשיר.
- **מחק־הכל** → תיבה **ריקה**. מכשיר בברירת מחדל לא אישר דבר.
- **ביטול אישור** → השער נפתח מחדש, **השם נשמר**. ביטול תנאים אינו התנתקות.
- **`TOS_VERSION` עולה** → מבקש אישור מחדש.

---

## 3. מה אומת ומה לא — קרא את זה לפני שתבטיח משהו

### אומת
- **24 קובצי בדיקה, 202 טענות**, אפס כשלים.
- **15 מוטציות, 15 נתפסו.** כל בדיקה הורצה מול קוד שבור בכוונה. **ארבע לא
  תפסו ותוקנו** — הבולטת: בדיקת האישור עברה קוד שבו הכפתור הפותח מוחק ישירות
  וענף האישור נשאר מת, כלומר מחיקה בלחיצה אחת.
- **31 בדיקות בדפדפן** — Chromium 390×844, כל מסלולי השער, ההתנתקות, הביטול
  והמחיקה.
- **סריקת 13 מסכים** — ניגודיות ויעדי מגע באפס.

### ❌ לא אומת
- **iOS. בכלל.** נבדק ב-Chromium headless. לאפליקציה הזו יש היסטוריה של באגים
  שרק WebKit מייצר — ראה את קומיטי „הקומפוזר" ו„הפתיח" בענף.
- **מסכי התשובות והכלים.** הסריקה רצה עם כפיל ל-`backend`, אז רק 13 מסכי
  המסגרת נבדקו.
- **שהפריסה בפועל מכילה את הקוד הזה.** לא הצלחתי לאמת.

---

## 4. ⭐ הדבר האחד שצריך אצבע על מכשיר

בקומיט `ddb259a` הכרטיס ירד מ-**`15vh` ל-`8vh`** וקיבל גלילה, כי הוא גדל עם
תיבת התנאים ויכול לגלוש בטלפון נמוך.

ההערה המקורית בקוד אומרת ש-`15vh` נבחר **כדי שמקלדת iOS לא תכסה את שדה השם.**

ההזזה למעלה אמורה לשרת את אותה מטרה טוב יותר. **זה לא הוכח.**

> **הקש על שדה השם באייפון. האם המקלדת מכסה אותו?**
> זו הבדיקה היחידה שחוסמת. כל השאר עבר.

---

## 5. איך להריץ מקומית (Windows, `D:\app_soldier`)

```powershell
cd D:\app_soldier
git fetch origin
git checkout claude/cool-cori-s94h5t
venv\Scripts\python.exe -m streamlit run app.py --server.address 0.0.0.0
```

הטלפון: `http://<IP מקומי>:8501` בספארי, אותו Wi-Fi.
⚠️ ב-LAN רגיל `isSecureContext` שקר, ולכן ה-Service Worker לא נרשם. האפליקציה
עובדת מלא; רק בלי מטמון PWA. בדיקת המקלדת לא מושפעת.

### כלי הסריקה
```
python3 tools/ui_audit/run_stub.py &        # 8501, backend מזויף
python3 tools/ui_audit/audit_selftest.py    # ⚠️ הרץ קודם
python3 tools/ui_audit/sweep.py             # 13 מסכים
```
`audit_selftest.py` אינו טקס. הבודק דיווח **אפס ממצאים ב-13 מסכים** בזמן שהיה
עיוור לשני באגים שכבר נמצאו בעין. שלוש נקודות עיוורות מתועדות ב-README שלו.

---

## 6. פתוח

1. **אין תיעוד מתי אושרו התנאים.** `tos_ok` שומר גרסה, לא תאריך. לאישור תנאים
   זה בערך חצי מהערך המשפטי. תוספת קטנה לעוגייה ולבאנר.
2. **„נקה היסטוריית שיחות" עדיין לחיצה אחת** — מוחקת הכל בלי אישור ובלי ביטול.
   `_confirm_action` כבר קיים; התוספת היא שש שורות.
3. **מסכי התשובות והכלים לא נסקרו** (סעיף 3).

---

## 7. מלכודות

- **אל תריץ `push.bat`** בזמן שאתה על הענף — הוא עושה `git add . && push`.
- **`main` תקוע ב-30.08**, הענף מקדים ב-48 קומיטים. הפריסה יוצאת מהתיקייה, לא
  מהגיט, ולכן `git branch --show-current` הוא מה שקובע מה נשלח. שווה לסדר.
- **הפריסה מגיעה לכולם מיד** — אין שלב ביניים. ה-Service Worker מתעדכן לבד
  (`skipWaiting` + מחיקת כל מטמון שאינו החדש), אז אין צורך בהתקנה מחדש.
- **סרטון 17.09 16:23 הוא של הבנייה הישנה.** „התנתקות מחזירה לתפקיד" שם הוא
  הבאג המקורי, לא רגרסיה.

---

## 8. הקבצים

| קובץ | מה |
|---|---|
| `app.py` | `TOS_VERSION`, `_name_gate`, השער, `_reset_identity`, `_wipe_all`, `_confirm_action`, `_confirm_pending`, `_settings_about`, `_settings_privacy` |
| `tests/test_terms_gate.py` | 10 בדיקות התנהגות; מחלצות פונקציות מ-`app.py` ומריצות מול `session_state` מזויף |
| `tests/test_compliance_screens.py` | +5 בדיקות: הבאנר, התנאים בנקודת האישור, גרסה, אישור המחיקה |
| `tools/ui_audit/` | הרצת האפליקציה בדפדפן + בודק פגמים + מבחן-עצמי |

**בדיקת שנייה אחת שהקוד באוויר:** המסך הראשון הוא „איך קוראים לך?" ולא
„בחר את סוג הכניסה שלך".
