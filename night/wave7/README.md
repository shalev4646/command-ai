# wave-7 — הכנות בלי כתיבה (22.09.2026)

**שום דבר בתיקייה הזאת אינו מוחל.** החלה כותבת `storage/json_store` ו-`storage/embedding_cache.npz`,
והיא ממתינה להודעת „הקורפוס פתוח" מהסשן המנהל (הריצה בתשלום של סשן א' מודדת את הקורפוס כפי שהוא).

| קובץ | מה זה | השערים לפני החלה |
|---|---|---|
| `anchors_phrasing.json` | 73 עוגנים ב-23 פקודות, מניסוח ה-dev של כל נושא + תוכן הסעיף; הניסוי של `night/ANCHORS_PHRASING_CRITERION.md` (ניסוחי c נעולים ב-`5f81378`) | `night.anchors.check` (73/73 ב-22.09), מבחן-מראה Jaccard<0.40 מול c/held/סרגל; אחרי החלה: c ≥ +5, סרגל ≥ +3, אפס אבודים ב-held/שער/נתב |
| `defs/*.json` | טיוטות בלוקים ידניים (`key-facts` / `key-facts-nodigits`) ל-7 הפקודות שאין להן בלוק — ולכן אינן קיימות לאחזור תחת `RETRIEVE_CURATED_ONLY` | `python -m night.wave7.check_defs` (חינם: `night.curate.check` מול ה-raw_text הקיים, במצב digit-free כשנדרש); ביקורת-נאמנות בלתי-תלויה מול ה-PDF; אחרי החלה: שער 390/431, סרגל ונתב אפס אבודים, ממוצע/מקסימום מילות-חלון |

## הטיוטות — מדיניות הספרות (לפי `digits_fixed` של 18.09)

| פקודה | מילים | ספרות שנדחו ב-digitfix | הבלוק |
|---|---|---|---|
| PM-33.0109 משמעת — מניעת נזקים לרכוש | 180 | 0 | `key-facts`, עם ספרות (מספרי-סעיף בלבד) |
| 36.0313 תוספת צבא קבע | 192 | 2 | `key-facts`; **אחוזי-התוספת לאימות בעין** לפני החלה |
| 36.0301 תוספת פעילות ב-3 רמות | 1,549 | 2 | `key-facts`; המספרים (14 ימים, 3 חודשים, 48 חודשים, 7%, טפסים 869/3092) לאימות בעין |
| 33.1010 ריצוי עונש מאסר | 2,687 | 7 | `key-facts`; השנים בטקסט משובשות („5411") — לא צוטטו; מועדים (שבוע, 72 שעות, 3 ו-7 ימים) לאימות בעין |
| PM-33.0342 קביעת „תעודה" | 247 | 55 | `key-facts-nodigits` — רשימת המסמכים בלי מספרי-טפסים |
| 3.0502 מענק שחרורין משירות קבע | 1,857 | 97 | `key-facts-nodigits` — המבנה בלי ספי-החודשים והשיעורים |
| 31.0252 מינוי קצינים במילואים | 1,518 | 116 | `key-facts-nodigits` — הסוגים, הסמכויות והתהליך בלי תקופות |

⚠ 3.0502 ו-33.1010 יושבות ב-`NEVER` של `night.curate` (ספרות) — הוצאה משם היא החלטת המשתמש.
⚠ מנגנון-ההחלה לבלוק על פקודה קיימת (הוספת section ל-JSON + `index_document`) **לא נכתב** — `night/wave6/apply.py`
מיועד למסמכים חדשים עם PDF. ייכתב כשיוחלט להחיל, ואז לפי אותם שערים.
