# גל-הקליטה המשולב (wave-8) — קריטריון, נכתב לפני המדידה (26.09.2026, סשן א')

הכרעת המשתמש (26.09, דרך הסשן המנהל, מילה במילה: „תתחיל אני מאשר"): (1) `RETRIEVE_DOC_BLOCKS=7` ב-v158, ושתי הפקודות
שהוחזקו בחוץ (PM-33.0109, 33.1010) נכנסות; (2) רשימת-הקליטה: 9 הפקודות הקצרות + HKA-30-07-01 + KARPAR-701.001 +
KARPAR-302.006 + KARPAR-300.001 + HKA-32-02-10 / 32-02-27. סשן א' הוא הכותב היחיד לקורפוס (`D:/_run_wt`, ענף `claude/paid-run`).

## קו-הבסיס של הגל = K=7 (נקבע כאן, לפני שנמדד דבר)

`night/K7_CRITERION.md` מדד K=7 על העץ `6112fd8` (7 חסרות-הבלוק בפנים) ומצא: שתי השורות שנדחקו חוזרות, אפס אבודים, מילים +12–13%.
המשתמש קיבל את המחיר. מכאן:

* **הרשומה `k7base`** = העץ הזה **אחרי** החזרת PM-33.0109 ו-33.1010 (שני הבלוקים של wave-7, אותן הגדרות), עם **דגלי v158**:
  `RETRIEVE_GLOSSARY=1 RETRIEVE_FULL_BLOCKS=1 RETRIEVE_ROUTER_SLOTS=2 RETRIEVE_DOC_BLOCKS=7 RETRIEVE_FULL_BLOCK_MAX_WORDS=2000
  RETRIEVE_HOMONYMS=1 RETRIEVE_QUOTELESS=1` (HyDE כבוי, נתב משוחזר, בלי מפתח — כמו בכל מכשיר חינמי). `ANSWER_TERM_NOTE` הוא
  צד-התשובה ואינו נוגע למכשירים.
* **כל שלב של הגל נמדד מול `k7base`**, לא מול `w6cap` (K=6) — ההשוואה ל-`w6cap`/`merged` נרשמת למידע („מה K=7 עצמו שינה על
  הקורפוס הזה"), אינה קריטריון.
* המכשירים והרשומות (כולם חינם): `night.levers_measure <tag> RETRIEVE_DOC_BLOCKS=7 RETRIEVE_HOMONYMS=1 RETRIEVE_QUOTELESS=1 [BASE=k7base]`
  (סרגל 82 · head-100 174 עם/בלי המזוהמות · שער 431 · נתב 82) + `night.head100.probe_variant` על `targets_c.json` (c, 38) ועל
  `homonym_targets.json` (הסט הנעול, 12) + `night.real24_window night/out/real_questions.json <tag> BASE=k7base` (24 שאלות אמיתיות)
  + מילות-החלון הממוצעות מהרשומות (`agg.window_words`/`avg_words` של הסרגל, head-100, c).

## הקריטריון לכל כתיבה לקורפוס בגל (שלבים א, ג, ד — כל אחד נמדד בנפרד מול `k7base`)

1. **אפס אבודים** בכל מכשיר: סרגל (סעיף-תוכן ופקודה), head-100 dev / held / held-בלי-המזוהמות (held במצרף בלבד), נתב, שער
   (0 נופלות חדשות), c, הסט הנעול; real24 — אף שאלה אמיתית לא מאבדת את הפקודה המובילה שלה לפקודה שאינה מהנושא.
2. **מילות-חלון ממוצעות ≤ +5%** מול `k7base` בסרגל וב-head-100 (מסמכים חדשים נכנסים לחלון רק כשהם עונים; K כבר שולם).
3. **דחיקה = כלל-העצירה:** מסמך חדש שדוחק פקודה עונה משורה מדודה מוחזק בחוץ (`sections=[]` + הערה, כמו 23.09) ומדווח; לא
   „מתקנים" את השורה, לא מזיזים K שוב.
4. **נאמנות לפני אחזור:** כל מספר בבלוק חדש מתמונת-העמוד (`numbers_seen`), שני השערים (`curate.check`, `numbers`) נקיים בכתיבה;
   ביקורת-עמוד עצמאית של סשן ב' על כל בלוק לפני המיזוג (שלב ו).
5. לרישום: מה נוסף (יעדים חדשים בחלון), מה זז ב-real24, ומילים.

**שלב א (החזרת השניים) — התוצאה הצפויה לפי K7_CRITERION:** rs060 חוזרת לסרגל (30/82), held +2 מול K=6; אם משהו אבד מול `merged`
(K=6 בלי השניים) — זו דחיקה חדשה ונרשמת, אבל `k7base` נשאר קו-הבסיס של הגל בכל מקרה (הכרעת המשתמש כוללת את השניים).

## מה עולה כסף ומה לא

קליטה-מטקסט (`INGEST_FROM_TEXT=1`, `text_ingest`) · `apply_defs --write` · `index_document` · כל המכשירים — **$0**. `night.curate` על
מסמכי-FOI ללא הגדרה ידנית — ≈ $0.07–0.39 למסמך (≈ $1.4 לארבעה); **יוצא רק על שורת-אישור של המשתמש בצ'אט של סשן א'**; ברירת-המחדל
כאן: אצירה ידנית מתמונות-העמוד, $0. `night.probes` (≈ $0.01 לפקודה) — לא בגל הזה אלא אם המשתמש מבקש.

הדיווח למנהל: אחרי שלב א ואחרי שלב ג — שורה ראשונה = המספר (אבודים מול `k7base`), ואז הטבלה.
