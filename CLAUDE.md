# CommandAI — קרא את זה קודם

אפליקציית Streamlit שעונה לחיילים על שאלות **אך ורק מפקודות מטכ"ל**, עם
ציטוט ומספר סעיף. אין ייעוץ, אין ידע כללי, ואין תשובה בלי מקור.

**המצב הנוכחי וההוראות לצעד הבא: `STATE.md`.** הוא מסמך עמוד אחד שמתעדכן
בכל צעד. `HANDOFF.md` הוא היומן הכרונולוגי (890+ שורות) — קרא בו רק את
הסעיף שרלוונטי לך; `night/PLAN_ROUND4.md` היא התוכנית הפעילה.

## ⛔ כללי העבודה של הפרויקט — הם הסיבה שהוא מתקדם

1. **דגל כבוי עד שמדידה מזווגת אומרת שהתשובות זזו.** כל כפתור-אחזור
   ב-`backend.py` נכנס כ-`RETRIEVE_*` עם ברירת-מחדל 0. „כבוי = בייט-לבייט
   זהה" נעול בבדיקה.
2. **חינם לפני כסף.** `night/sectprobe.py` („הסעיף שעונה מגיע לחלון") ו-
   `night/titleprobe.py` רצים בלי מודל ובלי תשלום. מה שלא זז בהם — לא
   קונים לו זרוע.
3. **קריטריון נקבע לפני המדידה, בכתב.** לא אחריה.
4. **הסרגל קפוא:** 72 שאלות בסגנון אמיתי, 97 חלקים, מבוררות אחת-אחת
   (`night/out/adjudication_realstyle.json`). **שונות-מדגם 12 נקודות** ⇒
   רק השוואות מזווגות. רצפת-הרעש של המדרג נמדדה: **±3 תשובות מלאות**.
5. **דלת שגויה גרועה מאין-דלת** (`out_of_scope.py`). כל משפחה נבדקת על סט
   שלא השתתף בכתיבתה, אחרת הבדיקה היא מראה.
6. **הבדיקות רצות בלי API ובלי רשת.** `venv\Scripts\python.exe tests\run_all.py`.
   6 כשלים קבועים בסביבת הסשן (huggingface.co חסום) — לא רגרסיה.
7. **אל תריץ** `night.anchors --apply` או `_reingest.py` באמצע מדידה.

## היכן מה

| | |
|---|---|
| `backend.py` | אחזור, פרומפטים, קריאת המודל. כל הדגלים בראשו, עם המדידה שהצדיקה אותם |
| `app.py` | Streamlit. צ'יפ-פסיקה, רצועת „לאן כן פונים", מקורות |
| `out_of_scope.py` | משפחות-דלת לשאלות שאין עליהן פקודה |
| `storage/vector_store.py` | האינדקס והדירוג (MiniLM ONNX) |
| `storage/clause_index.py` | אינדקס כותרות-הסעיפים (ארבע-גרמים) |
| `storage/clause_embed.py` | אינדקס ברמת הסעיף עם מנוע מתחלף (RETRIEVE_V3) |
| `night/` | כלי המדידה: sectprobe, titleprobe, arm_second, grade, report_goal, review |
| `night/out/` | תוצרי המדידות והבוררות |

## ⛔ מה שנמדד ונפסל — אל תבנה שוב

`RETRIEVE_V2` (כותרות בלבד, 1.58 מול קריטריון 2) · `RETRIEVE_V3` (33/82 מול
50) · `RETRIEVE_V3_ONLY` (18/82, אסון) · `SCOPE_UNIT_ROUTE` (הפך 2 פסיקות
לסירוב) · כוונון לפי נושאים · נתב k=5 · Sonnet כנתב · קיצור כלל-הסירוב.
הרשימה המלאה עם הסיבות: ראש `HANDOFF.md`.

## עברית

המשתמש כותב עברית ואנגלית; **התשובות תמיד בעברית**, גם כששואלים באנגלית.
