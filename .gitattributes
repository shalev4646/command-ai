# Shell scripts run inside the Linux container — a CRLF checkout (Windows
# autocrlf, including fresh worktrees) turns the shebang into "bash\r" and
# crash-loops the Fly machine (2026-07-27). Pin them to LF everywhere.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
